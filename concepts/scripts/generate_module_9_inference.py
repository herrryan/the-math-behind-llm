import os

os.makedirs("/Users/guofei/workspace/the-math-behind-llm/concepts/docs/module-9", exist_ok=True)

chapters = {}

# -----------------------------------------------------------------------------
# Concept 22: Prefill vs Decode
# -----------------------------------------------------------------------------
chapters["module-9/22-prefill-and-decode.md"] = """# 概念 22 - 暴风吸入与逐字斟酌：预填（Prefill）与解码（Decode）的物理节律

---

## 老师引言：为什么大模型读得飞快，写起来却一顿一顿的？

如果你经常使用 ChatGPT、Claude 或 DeepSeek，你一定观察过这个奇妙的现象：
当你复制粘贴一段长达 5000 字的长篇大论扔给模型，然后按下回车——模型几乎在**一眨眼的瞬间（不到半秒）**，就完全“读完”了这 5000 字！
但紧接着，当它开始给你回答时，屏幕上却是一个字、一个字地往外蹦（每秒钟吐出 30 到 50 个字），像个打字员在键盘上逐字敲击。

为什么大模型“读”的时候像狂风卷落叶，而“写”的时候却慢如溪水长流？
今天，老师就带你走进大模型推理生命周期中最基础也最关键的双重节奏——**预填阶段（Prefill）** 与 **解码阶段（Decode）**。

---

## 生活实体模型：速读题干的学生与考场交卷的考生

想象一位参加全国高考的学霸坐在考场里：

### 1. 预填阶段（Prefill）：暴风吸入题干
试卷发下来，第一道阅读理解题有整整 3000 字的背景材料。
学霸把试卷平铺在桌面上，双眼一扫，两目十行，大脑里的数万个神经突触全速运转：
- 题干里的这 3000 个字，**早就白纸黑字印在纸上了**；
- 他的大脑可以**同时、全量、并行地**将整篇文章的所有词语及其相互关系一口气吞下，并在脑海草稿纸上瞬间建立起整篇文章的认知地图。
这个“把已知题目瞬间吃透、在大脑中建好上下文记忆”的过程，就叫做**预填（Prefill）**！

### 2. 解码阶段（Decode）：如履薄冰逐字作答
题干看完了，接下来学霸必须开始动笔在答卷上写下他的分析长文。
但问题来了：**他能同时把第 1 句话、第 5 句话和第 10 句话一口气写在卷子上吗？**
绝对不能！
因为他必须先想出第 1 个字；根据第 1 个字的意思，才能推敲出第 2 个字；等第 1 句话写完，他才能决定第 2 句话怎么开头！
每一个新字，都必须依赖前面所有已经写下的字。
这个“每次只能闭目沉思、吐出唯一一个新字，再把它加入上文继续构思下一个字”的漫长过程，就叫做**逐字解码（Decode）**！

---

## 它在大模型里到底怎么工作？

在大模型的硬件芯片（GPU）上，这两个阶段所面临的物理瓶颈完全天差地别：

```
┌────────────────────────────────────────────────────────────────────────┐
│                        大模型推理的两副完全不同的面孔                  │
├────────────────────────────────────────────────────────────────────────┤
│ 阶段一：预填阶段 (Prefill)                                              │
│   输入: 用户发来的整段长提示词 (例如 4000 个已知 Token)                │
│   特征: 答案都在眼前，整块矩阵一起算 (大尺寸 GEMM)                     │
│   硬件状态: GPU 的成千上万个计算核心瞬间被喂得饱饱的 (算力受限 Compute-Bound)│
│   耗时: 极短，几乎不卡顿                                               │
├────────────────────────────────────────────────────────────────────────┤
│ 阶段二：解码阶段 (Decode)                                               │
│   输入: 每次仅仅输入刚刚生成的 1 个最新 Token                          │
│   特征: 必须等上一个字吐出，才能算下一个字 (细长向量矩阵乘 GEMV)       │
│   硬件状态: 算力核心饥肠辘辘，全程在干等显存把庞大权重搬运过来 (访存受限)│
│   耗时: 取决于要生成多少字，字数越多耗时越长                           │
└────────────────────────────────────────────────────────────────────────┘
```

硬件科学家用一个非常形象的指标来衡量这两个阶段的物理状态——**算术强度（计算访存比）**：
- 在 **Prefill** 阶段，模型把数百亿参数从显存读入芯片一次，就能同时为 4000 个词语做几万亿次并行乘加，算力利用率高达 80% 以上！
- 在 **Decode** 阶段，模型为了算仅仅 **1 个新字**，仍然不得不把整整几百亿参数从显存里完整搬出来一遍！GPU 的计算核心只花了 1% 的时间做计算，剩下 99% 的时间都在百无聊赖地干等数据总线搬运。

---

## 如果拿掉它，世界会怎样？

如果我们无法理清 Prefill 和 Decode 的物理区别：
- 工程师就会把读长文章的任务和写简短回答的任务混杂在同一张显卡里，导致正在流畅打字的系统突然因为别人发来一篇长文而剧烈卡顿好几秒；
- 硬件厂商就会盲目堆叠计算核心，却不知道解码阶段的真正痛点是显存总线太狭窄。
正是因为认清了这两个阶段的本质差异，才催生了现代所有大模型推理加速技术的爆发！

---

## 老师划重点

!!! tip "核心心法"
    **预填（Prefill）是全量并行的饕餮盛宴，榨干 GPU 的算力核心；解码（Decode）是单字串行的漫长独木桥，受制于显存的搬运带宽。大模型的所有推理优化，本质上都是在向这两个物理瓶颈发起进攻！**
"""

# -----------------------------------------------------------------------------
# Concept 23: KV Cache & The Memory Wall
# -----------------------------------------------------------------------------
chapters["module-9/23-kv-cache-and-memory-wall.md"] = """# 概念 23 - 显存记事本与带宽墙：KV Cache 膨胀与分组记忆瘦身（GQA / MLA）

---

## 老师引言：为什么大模型聊得越久，你的显存越容易被撑爆？

很多人以为，大模型运行时占用的显存大小是固定不变的。
比如一个 70B（700 亿参数）的模型，权重文件大约占 140 GB 显存，那是不是只要有 140 GB 显存就万事大吉了？

绝对不是！当你兴致勃勃地让大模型读一本几十万字的厚书，或者跟它多轮对话聊了一下午之后，你会惊恐地发现显存报警器突然拉响，程序直接崩掉并提示：`CUDA Out of Memory`！
那些凭空冒出来的几十上百 GB 显存到底被谁吃掉了？
答案就是大模型赖以生存的**思维记事本——KV Cache（键值缓存）**。

---

## 生活实体模型：鱼的 7 秒记忆与贴满书房的便利贴

想象你雇佣了一位极其聪慧但天生患有“顺行性遗忘症”的顾问：
这位顾问每思考完一句话，就会把刚才思考的精髓写在便利贴上。

### 1. 如果没有记事本（没有 KV Cache）：推倒重来的地狱
每次你问他一个新问题，或者让他接着上一句写：
他必须从你今天早晨对他说过的第一声“你好”开始，重新把几千句话的逻辑在大脑里从头到尾全部运算推演一遍！
- 第 1 个字，他算了 1 句话；
- 第 100 个字，他必须重新算 100 句话；
- 第 10,000 个字，他为了吐出这一个字，必须重复计算整整 10,000 句话的前置逻辑！
整个系统的计算量会呈二次方（$N^2$）失控爆炸，吐字速度会越来越慢，最后慢到几分钟才憋出一个字。

### 2. 备上随身记事本（有了 KV Cache）：只记增量
为了避免重复劳动，顾问想出了好办法：
每当他读完或写完一个词，就把自注意力机制里算出来的**“线索索引（Key）”**与**“知识内容（Value）”**工工整整写在一张便利贴上，贴在桌子旁。
当下一次要算新词时，历史便利贴直接拿来查阅，自己只需要为眼前这 **1 个最新词** 算一张新便利贴就行了！
计算量瞬间从二次方雪崩变成了轻松的线性增长！

### 3. 便利贴灾难：书房被纸条彻底淹没！
可是，便利贴虽然省下了大量的脑力重复计算，代价却是物理空间的恐怖膨胀：
在大模型里，每一层网络、每一个注意力头、每一个生成的词语，都必须留下一份 KV 便利贴。
当上下文长度达到 128k（13 万字）时，光是这叠便利贴的重量就高达 **数百吉字节（GB）**，甚至远远超过了模型本身的所有参数总和！

---

## 它在大模型里到底怎么工作？

为了不让显存记事本把服务器撑爆，科学家们发明了革命性的“记事本瘦身术”：

```
┌────────────────────────────────────────────────────────────────────────┐
│                        三代注意力记事本 (KV Cache) 演进                │
├────────────────────────────────────────────────────────────────────────┤
│ 1. 传统多头注意力 (MHA): 每个提问者配专职书童                         │
│    查询头 (Q):  [Q0]  [Q1]  [Q2]  [Q3]  [Q4]  [Q5]  [Q6]  [Q7]         │
│    键值头 (KV): [K0]  [K1]  [K2]  [K3]  [K4]  [K5]  [K6]  [K7] (超占显存)│
│                                                                        │
│ 2. 分组查询注意力 (GQA - LLaMA 3 标配): 多人共用一个共享书童          │
│    查询头 (Q):  [Q0   Q1   Q2   Q3]     [Q4   Q5   Q6   Q7]            │
│    键值头 (KV):         [KV 0]                    [KV 1]               │
│    ──► 显存占用直接砍掉 4 到 8 倍！性能几乎不降！                      │
│                                                                        │
│ 3. 多头潜在注意力 (MLA - DeepSeek 独门绝技): 压缩暗号记事本            │
│    不论上方有多少个查询头，写入显存的 KV 记事本被强行压扁成一个微小暗号│
│    ──► 显存占用暴降近 90%！长文本推理性价比之王！                      │
└────────────────────────────────────────────────────────────────────────┘
```

1. **标准 MHA（多头注意力）**：每一个提问头（Query Head）都拥有自己专属的 Key 和 Value。头数越多，记事本越厚。
2. **GQA（分组查询注意力）**：让 4 个或 8 个提问头共享同一本 Key-Value 记事本。显存占用直降至原来的八分之一，成为现代大模型工业界的事实标准。
3. **MLA（多头潜在注意力，DeepSeek 核心精髓）**：在把 Key 和 Value 存入显存前，用一把数学压缩钳将其压扁成一个超紧凑的潜在低维向量；只在需要使用的一瞬间动态还原。

---

## 如果拿掉它，世界会怎样？

如果没有 KV Cache，大模型的长文本推理将彻底不可用——生成一千字的回复可能需要好几个小时；
而如果没有 GQA 和 MLA 这些记事本压缩黑科技，我们就无法在单台服务器上体验 128k 甚至百万 Token 的超长文本分析，普通用户更不可能在个人电脑上跑起长对话助手。

---

## 老师划重点

!!! tip "核心心法"
    **KV Cache 是一场用空间换时间的精密交换：它用显存里的记事本消除了重复计算的历史噩梦；而 GQA 与 MLA 则是记事本的极限压缩术，把显存从无底洞般的内存墙中解救了出来。**
"""

# -----------------------------------------------------------------------------
# Concept 24: PagedAttention & Virtual Memory
# -----------------------------------------------------------------------------
chapters["module-9/24-paged-attention.md"] = """# 概念 24 - 虚拟显存货架：PagedAttention 如何消除碎片、像操作系统一样管显存

---

## 老师引言：为什么明明显存显示还剩一半，系统却说装不下了？

在计算机工业界，常常发生一种让人捶胸顿足的怪事：
系统管理员打开监控面板，明明看到昂贵的 GPU 显存还剩下整整 40 GB 的空闲空间，然而当一个新的用户发来一条只有几十个字的消息时，系统却当场报错崩溃，拒绝提供服务。

这就像你去停车场停车，明明放眼望去到处都有空位，保安却挥着旗子把你轰走：“没车位了！”
这种荒谬的显存浪费被称为**内存碎片化（Memory Fragmentation）**。
而彻底终结这一顽疾、让大模型并发吞吐暴涨数倍的超级英雄，就是如今大名鼎鼎的 **PagedAttention（分页注意力）**。

---

## 生活实体模型：大学图书馆占座与智能寄存柜

### 1. 传统显存分配：霸道的大学图书馆占座
在 PagedAttention 诞生之前，推理引擎管理 KV Cache 便利贴的方式极其古板：
当一个用户连接进来时，系统根本不知道他最终会聊几句。
系统心想：“万一他要聊满 2048 个字呢？”
于是，系统二话不说，**一口气在显存里给这位用户霸占了一整条必须完全连续的 2048 个空槽位**！
- 结果这个用户只问了句“你好”，得到了“你好呀”三个字，就心满意足地离开了；
- 剩下的 2040 多个连续空槽位，在对话没有彻底结束前，任何其他人都绝对不能用！
更要命的是，显存里东空一块、西空一块，全是散碎的小缝隙。哪怕空闲容量总和很大，却连一个完整的连续大长条都拼不出来！**高达 60% 到 80% 的珍贵显存就这样被白白挥霍了。**

### 2. PagedAttention 革命：火车站的自动储物柜
现代操作系统的内存管理早在几十年前就解决了这个问题，这就是传奇的**虚拟内存分页机制**。
加州大学伯克利分校的研究团队灵光一闪：*为什么不把操作系统的分页思想借给大模型？*

他们发明了 **PagedAttention**：
- 不再提前霸占整条连续显存，而是把显存切成一个个固定大小的标准储物盒（例如每个盒子固定装 16 个词元的 KV 数据，称为一个“Page/块”）；
- 用户的对话说满 16 个字，系统就从散落的货架上随便抓一个空盒子给他用；
- 再说满 16 个字，再随便抓一个空盒子。这些盒子在物理显存里可以天南海北、互不相连，只需要一张轻巧的**目录指引表（Page Table）**把它们串起来！

```
┌────────────────────────────────────────────────────────────────────────┐
│                      PAGEDATTENTION 虚拟货架映射示意                   │
├────────────────────────────────────────────────────────────────────────┤
│ 用户逻辑感知: [ 词元 0~15 ] ──► [ 词元 16~31 ] ──► [ 词元 32~47 ]      │
│                     │                   │                   │          │
│ 映射指引目录表:    指向块 #3           指向块 #8           指向块 #1          │
│                     │                   │                   │          │
│ 物理显存散布:    [物理块 #1]         [物理块 #3]         [物理块 #8]         │
│                 (放第32~47词)       (放第0~15词)        (放第16~31词)        │
│ ──► 散落在天涯海角的碎盒子里，被目录表串联成一条丝滑的长文本！         │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 它在大模型里到底怎么工作？

PagedAttention 不仅把显存浪费率从 80% 狠狠砸到了 **1% 以下**，还附赠了一个近乎魔法般的超级特异功能——**写时复制（Copy-on-Write）与前缀共享**！

想象一下，如果有 10 位用户同时在向大模型咨询同一篇 10,000 字的法律合同：
- 在过去，系统必须把这 10,000 字在显存里原样复制整整 10 份，显存瞬间告急；
- 在 PagedAttention 里，这 10 位用户的目录表全部指向**同一批物理盒子**！10 个人零成本共享同一份长篇记忆；
- 只有当某位用户自己产生独一无二的新回复时，系统才单独为他分配新盒子。

这就是当今全球几乎所有主流大模型服务框架（如 vLLM）底层的力量源泉。

---

## 如果拿掉它，世界会怎样？

如果没有 PagedAttention：
- 我们现在租用 GPU 云服务器的费用至少要翻三到五倍；
- 当很多用户同时提问时，服务器能承载的并发人数会缩水到原先的五分之一；
- 所谓的多人共享长上下文（如代码库检索、长文档客服问答）将变成企业根本无法承受的昂贵奢侈品。

---

## 老师划重点

!!! tip "核心心法"
    **PagedAttention 用操作系统经典的“分页解耦”思维，粉碎了连续显存的暴政。它把零散的货架碎片化为神奇，将显存浪费彻底清零，是高并发大模型推理服务的定海神针。**
"""

# -----------------------------------------------------------------------------
# Concept 25: FlashAttention
# -----------------------------------------------------------------------------
chapters["module-9/25-flash-attention.md"] = """# 概念 25 - 芯片片上的闪电速记：FlashAttention 为何将数据搬运视为终极死敌

---

## 老师引言：最快的计算，是压根不需要跑腿的计算

在计算机世界里，有一个反直觉却残酷无比的真相：
**GPU 做数学加减乘除的速度，比它从自己的显存仓库里取放数据的速度，快了整整几十甚至上百倍！**

如果你让 GPU 的计算核心算 100 亿次乘法，它只需要微秒级别就能完成；但如果你让它把这 100 亿个数字从显存大仓库搬到计算核心，再把算好的结果运回大仓库，它可能要在路上气喘吁吁地跑好几倍的时间。
现代计算的本质悲剧，不是算得慢，而是**“快递小哥在路上堵死了”**。
而 **FlashAttention（闪速注意力）**，就是一场向数据搬运发起的全面宣战。

---

## 生活实体模型：随手案板与三公里外的冷库

想象你是一位刀工出神入化的五星级大厨：
在你的手边，有一个面积虽然很小、但一伸手就能抓到的**超高速案板（芯片片上 SRAM 缓存，带宽每秒近 20 TB）**；
而在后院三公里外，坐落着一个面积巨大、什么都能装下的**大冷库（显存大仓库 HBM，带宽只有每秒 3 TB 左右）**。

### 1. 传统注意力的笨办法：跑步两小时，做菜五分钟
传统计算自注意力机制（Q乘K、算Softmax、乘V）时，步骤是极其笨拙的：
1. 大厨跑到三公里外的冷库，抱出一箱 Query 和一箱 Key 回到案板；
2. 案板上切了一刀，算出了一个巨大的中间相似度打分大方阵；
3. 大厨**竟然又把这个巨大的大方阵哼哧哼哧搬回了三公里外的冷库**！
4. 歇了口气，大厨又跑去冷库把这个大方阵搬出来，在案板上算归一化概率；
5. 算完概率，**又特么搬回了冷库**！
6. 最后，又跑去冷库搬出 Value，把概率方阵从冷库搬出来，两者相乘得到结果。
整顿饭做下来，大厨有 90% 的时间都在大冷库与厨房之间疯狂来回折返跑！更糟糕的是，如果文章有一万字，那个中间大方阵的面积就会达到惊人的一亿个格子，冷库的大门都要被挤爆了。

### 2. FlashAttention 的智慧：就地做熟，绝不搬运中间残次品！
斯坦福大学的 Tri Dao 博士提出了一个颠覆性的想法：
*为什么我们一定要把半成品的中间大方阵搬回大冷库去？*
大厨在案板上只切一小块胡萝卜（一个小分块），直接在案板上完成局部打分、就地归一化、就地乘上局部 Value，**只把最终煮好的精美菜肴送回大仓库**！
中间那些庞大恐怖的半成品，在手边的案板上算完即焚，从始至终压根不需要在大冷库里留下一丝痕迹！

```
┌────────────────────────────────────────────────────────────────────────┐
│                   传统注意力 VS. FLASHATTENTION 访存对比               │
├────────────────────────────────────────────────────────────────────────┤
│ 传统做法 (搬运地狱):                                                   │
│   SRAM 片上案板 ──(搬运超大方阵)──► 显存 HBM 冷库 (慢速往返跑 3~4 次!) │
│   ──► 极其耗电、巨慢无比、显存随长度平方级暴涨！                      │
├────────────────────────────────────────────────────────────────────────┤
│ FlashAttention (就地消化):                                             │
│   从显存分批抓取小分块 ──► 在片上 SRAM 案板内闭环完成全部运算          │
│   ──► 中间方阵零显存写入！全程保持芯片内部最高时钟运转！              │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 它在大模型里到底怎么工作？

读者可能会问：“老师，归一化（Softmax）不是必须知道全局所有词的最大值和总和才能算吗？切成小块之后，你怎么知道后面的词会不会更大？”

这就是 FlashAttention 背后最神仙的技巧——**在线动态归一化（Online Softmax）**：
- 当大厨算完第 1 小块时，先记录下当前的局部最大值和局部和；
- 当读到第 2 小块时，如果发现出现了更大的新数字，大厨并不需要推倒重来，而是掏出一把“数学修正尺”，只用一行极简的小乘法，就能瞬间把第 1 小块的结果原地校准！
这种精巧的数学递推，使得在极小的片上案板（SRAM）里流式消化全局注意力成为了现实。

---

## 如果拿掉它，世界会怎样？

如果没有 FlashAttention：
- 大模型的训练成本至少要上升 3 到 5 倍，很多千亿规模的开源大模型根本无力完成预训练；
- 长文本处理会变得寸步难行——现在的 32k、128k 上下文窗口之所以能成为现实，正是因为 FlashAttention 把平方级的显存搬运灾难压平到了极低的水准。
它被公认为过去三年深度学习系统领域最伟大的工程里程碑之一。

---

## 老师划重点

!!! tip "核心心法"
    **FlashAttention 并不是发明了新的注意力数学，而是读懂了芯片物理硬件的脾气。它把庞大臃肿的中间结果锁死在片上高速案板里就地消化，消灭了一切无谓的显存折返跑。**
"""

# -----------------------------------------------------------------------------
# Concept 26: FlashDecoding
# -----------------------------------------------------------------------------
chapters["module-9/26-flash-decoding.md"] = """# 概念 26 - 撕开厚书的百人侦探队：FlashDecoding 与 Split-K 拯救空转的算力核心

---

## 老师引言：为什么 132 位大侦探齐聚一堂，却有 131 位在打瞌睡？

在前一章中，我们见证了 FlashAttention 的神奇提速。
然而，当工程师们兴高采烈地把 FlashAttention 搬到大模型的**日常对话逐字解码（Decode）**场景时，却傻眼了：
在长文本解码时，GPU 的耗时依然非常惊人，硬件监控屏幕上一片惨淡——整张顶级显卡上，居然有超过 **90% 的核心在无聊闲置**！

为什么在训练时所向披靡的 FlashAttention，到了单字生成的解码阶段就突然哑火了？
今天，我们就来解开这个著名的“流式多处理器饥饿难题”，并看看 **FlashDecoding（闪速解码）** 是如何用“撕书战术”扭转乾坤的。

---

## 生活实体模型：大厅里发呆的 132 位名侦探

想象你开了一家规模宏大的私家侦探事务所。在大厅里，整整齐齐坐着 **132 位极其敏锐的大侦探（相当于顶级 GPU 上的 132 个计算核心 SM）**。

一天，一位富商急匆匆跑进来，递上一张写着 5 个字的小字条（当前的最新 Query 词元），并重重放下一本厚达 10,000 页的家族历史长篇档案（长达上万词元的历史 KV Cache）：
*“帮我在家族档案里，找出与这 5 个字线索最相关的内容！”*

### 1. 传统并行的死穴：一人拼命，全员摸鱼
传统 FlashAttention 的分工规则是非常古板的：
它按照“用户数”和“注意力头数”来分配任务。
如果你是单人长文本对话，且采用了现代分组注意力（比如只有 8 个注意力头）：
所长看了看名册，只挑出了前 8 位侦探：
- 1 号侦探分到了 1 号头的 10,000 页厚书；
- 2 号侦探分到了 2 号头的 10,000 页厚书……
剩下的 **124 位大侦探** 摊开双手面面相觑：所长没给我们派活，我们只能翘着二郎腿喝咖啡看报纸！
那 8 位可怜的侦探孤零零地坐在桌前，一页一页翻了两个小时，累得满头大汗；整整 94% 的顶级侦探人力被彻底浪费了！

### 2. FlashDecoding 的破局：把一万页厚书当场撕开！
新任探长看不下去了，他大步走上前，直接把那本 10,000 页的厚书撕成了 100 本每本只有 100 页的薄册子：
- “1 号侦探看前 100 页！”
- “2 号侦探看 101 到 200 页！”
- ……
- “整整 100 位侦探，现在全部给我动起来，同时看各自手头的 100 页！”

不到 10 秒钟，所有侦探就看完了各自负责的小册子。
大家走上讲台，在黑板上写下自己那一册里的最高置信度和局部线索。
探长只用了最后 1 秒钟，把黑板上的汇总结果做了一次闪电般的统筹归约，最精准的答案出炉了！

```
┌────────────────────────────────────────────────────────────────────────┐
│                        FLASHDECODING 撕书并行流                        │
├────────────────────────────────────────────────────────────────────────┤
│ 1 个提问词元  vs.  10,000 页历史长缓存                                 │
│                                                                        │
│ 传统做法: 把整本万页长书丢给 1 个核心 ──► 剩余上百个核心全在空转睡觉！ │
│                                                                        │
│ FlashDecoding (Split-K 切分):                                          │
│   切片 0 (第 0~1000 页)   ──► 核心 SM 0  ──► 产出局部小摘要 0          │
│   切片 1 (第 1001~2000 页)──► 核心 SM 1  ──► 产出局部小摘要 1          │
│   切片 2 (第 2001~3000 页)──► 核心 SM 2  ──► 产出局部小摘要 2          │
│   ...                                                                  │
│   切片 N (末尾分块)       ──► 核心 SM N  ──► 产出局部小摘要 N          │
│                                                                        │
│ 终局闪电合并 (Reduction):                                              │
│   一个极轻量的小核心将所有局部小摘要瞬间融合 ──► 吐出唯一精准全局大答案│
│   ──► 全卡 132 个核心 100% 满负荷并发！速度狂飙近 8 倍！               │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 它在大模型里到底怎么工作？

在大模型的数学结构里，历史上下文的长度维度在注意力机制中被称为 **Key/Value 维度（简称 K 维度）**。
FlashDecoding 的技术精髓，就叫做 **Split-K（K 维度切分并行）**：
1. **分块并发**：把漫长历史时序均匀横向切片，让全卡所有空闲的计算核心（SM）同时接管不同时段的记忆；
2. **两阶段流水线**：
   - 阶段 1：各核心各自计算手头切片的局部最大值、局部指数和与局部加权向量；
   - 阶段 2：启动一个轻量级的全局规约算子（Reduction Kernel），利用统一数学尺度对齐所有切片，合并出全局绝对无损的真实注意力结果。

整个过程没有多产生哪怕一个近似误差，但硬件占空比瞬间拉满。

---

## 如果拿掉它，世界会怎样？

如果没有 FlashDecoding：
- 当你输入数万字的长文档让大模型分析时，生成第一个字之后的每一个字，都会随着历史越来越长而越变越慢；
- 无论硬件厂商制造出多么强大、拥有成百上千核心的旗舰显卡，在处理单人长文本问答时都会陷入“一核有难、九十核围观”的滑稽窘境。

---

## 老师划重点

!!! tip "核心心法"
    **FlashAttention 解决了批次与头数的空间并行，而 FlashDecoding 则撕开了漫长历史的时间维度。它把孤掌难鸣的单核长考，转化为全军出击的百人联查，让超长文本解码延迟暴降数倍。**
"""

# -----------------------------------------------------------------------------
# Concept 27: Continuous Batching
# -----------------------------------------------------------------------------
chapters["module-9/27-continuous-batching.md"] = """# 概念 27 - 永不停歇的过山车站台：连续批处理（Continuous Batching）与插队顺风车

---

## 老师引言：为什么传统批处理在推理服务里是极大的灾难？

在深度学习的课本上，我们被灌输了一个神圣的原则：**批处理（Batching）越大越好**！
把 64 个或者 128 个样本打包在一起塞给显卡，显卡的吞吐效率最高。
在模型“训练”时，这个原则颠扑不破；
然而，当工程师把这套“打包批处理”原封不动搬到生产环境的**在线推理对话服务**时，却引发了一场灾难性的排队海啸。

为什么同一个打包思维，在训练时是神器，在服务时却成了毒药？
这背后就是大模型从“静态死板批处理”走向“生命细胞级连续批处理”的伟大进化。

---

## 生活实体模型：游乐园过山车的两种运营模式

想象游乐园里有一辆四座的超级过山车：
队列里排着四位游客：
- 游客 A：只想体验 10 秒钟；
- 游客 B：想玩 30 秒；
- 游客 C：想玩 1 分钟；
- 游客 D：买了一张连续玩 50 分钟马拉松的终身套票！

### 1. 传统静态批处理（Static Batching）：荒唐的陪跑地狱
传统的批处理就像一个极其教条刻板的过山车检票员：
四个人同时系好安全带，过山车出发。
10 秒钟后，游客 A 已经玩够了，脸色发白想要下车。
但检票员死死把安全压杠锁住：“不行！我们是一个批次！必须等游客 D 把 50 分钟彻底坐完，所有人才能一起下车！”
于是：
- 游客 A 在座位上干坐了 49 分钟又 50 秒的无聊冷板凳；
- 围栏外，排着长龙的上百位游客在烈日下焦急张望，但检票员坚决不准任何人入场！
在这 50 分钟里，整辆车有三个座位长期坐着无所事事的“空人”，**高达 70% 的运力被凭空浪费了**。这在计算机系统里被称为**计算气泡（Bubble Waste）**。

### 2. 连续批处理（Continuous Batching）：永不熄火的流水线
聪明的游乐园换了一位天才调度员：
过山车在轨道上疾驰，但每飞驰一圈经过站台时，只轻巧减速 1 秒钟：
- 谁玩够了，咔哒一声解开安全带走下站台；
- 队列最前面的新游客立刻一个健步跳上空出来的座位，扣紧卡扣；
- 过山车呼啸着重新冲向轨道——**每一个座位在每一圈都被坐得满满当当！**

```
┌────────────────────────────────────────────────────────────────────────┐
│                        静态批处理 VS. 连续批处理                       │
├────────────────────────────────────────────────────────────────────────┤
│ 静态批处理 (粗粒度请求级):                                             │
│   槽位 0: [ 短请求 A (3字) ] ──► [ 空转等待... 空转等待... 空转等待... ] │
│   槽位 1: [ 长请求 B (50字) ─────────────────────────────────────────] │
│   ──► 槽位 0 必须全程陪跑！新请求全被堵在门外！                        │
├────────────────────────────────────────────────────────────────────────┤
│ 连续批处理 (细粒度单字迭代级):                                         │
│   槽位 0: [ 短请求 A (3字) ] ──► 结束下车 ──► [ 新请求 C 立即上车接替 ] │
│   槽位 1: [ 长请求 B (继续生成第4字...) ─────────────────────────────] │
│   ──► 槽位永不空转，气泡彻底归零，系统吞吐量翻倍暴涨！                 │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 它在大模型里到底怎么工作？

在大模型推理系统里，这一机制被称为 **连续批处理（Continuous Batching，由微软与首尔大学团队在 Orca 系统中首创）**。
它的调度时钟不再是“整篇对话”，而是细致到了**“每生成一个词元的离散瞬间”**：
在每一个前向步完成的毫秒瞬间，系统都会检查哪位用户输出了终止符 `<eos>`。一旦有人完成，他的槽位立刻被腾空，等待队列里的新请求在下一毫秒无缝插槽！

### 分块预填（Chunked Prefill）：巧妙搭上顺风车
但这里还有一个隐藏的痛点：如果新来的请求带着一篇 8000 字的超长论文（Prefill），它一旦插队进来，会不会把正在流畅打字的人全给卡停好几百毫秒？
答案是用 **分块预填（Chunked Prefill）**：
把这 8000 字切成每块 512 字的小包裹，每一圈让大象搬一个包裹上车。
更绝妙的是，原本深陷显存读取瓶颈的解码词元，**顺便蹭上了超长文档搬运权重的顺风车**！解码延迟不仅不增加，显存带宽还被榨取得淋漓尽致。

---

## 如果拿掉它，世界会怎样？

如果没有连续批处理：
- 现在的任何大模型商业 API（如 OpenAI 或国产各类 API）其服务器成本至少要翻三倍，因为大部分算力都在为短请求填充无效的空白 Padding；
- 用户端会遭遇极其严重的延迟抖动——有时秒回，有时因为排在别人长请求后面而莫名其妙被卡住几十秒。

---

## 老师划重点

!!! tip "核心心法"
    **连续批处理打破了整篇请求的时间枷锁，将调度粒度精细到了每一个词元跳动的刹那。随到随走、有空即补的细胞级运转，彻底挤干了显存气泡的最后一滴水分。**
"""

# -----------------------------------------------------------------------------
# Concept 28: Speculative Decoding
# -----------------------------------------------------------------------------
chapters["module-9/28-speculative-decoding.md"] = """# 概念 28 - 老教授与飞毛腿助教：推测解码（Speculative Decoding）如何打破单字串行枷锁

---

## 老师引言：猜下一个字，真的只能一个字一个字慢慢憋吗？

学完前面的章节，你可能会感叹：
“老师，大模型的自回归生成规则也太死板了吧！第 2 个字必须等第 1 个字，第 3 个字必须等第 2 个字……不管你的显卡有多么逆天，这根串行的独木桥永远横在眼前，难道我们就没有办法一次性吐出一整串词吗？”

在这个看似不可逾越的物理绝境面前，计算机科学家们亮出了整个大模型历史上最精妙、最具智慧的算法杰作——**推测解码（Speculative Decoding，投机采样）**。
它不仅让生成速度暴涨两到三倍，而且在数学上做到了**绝对无损——生成质量与原模型分毫不差！**

---

## 生活实体模型：大牌老教授与飞毛腿小助教

想象有一所顶级学府，里面住着一对性格迥异的师徒：

### 1. 泰斗老教授（70B 目标大模型）
老教授满腹经纶、学富五车，能解答人类文明的所有高深问题。
但他年事已高，说话极其缓慢，每走一步都要深思熟虑。他每憋出一个字，都需要花上整整 1 秒钟的大脑沉思。

### 2. 飞毛腿小助教（1B 投机小模型）
旁边跟着一位刚毕业的年轻小助教。小助教虽然学问不如老教授深湛，但他有个绝活——嘴皮子极溜，思维快如闪电！
小助教 0.05 秒就能噼里啪啦连蒙带猜连吐 4 个词：
*“从……前……有……座……”*

### 3. 并行验题的神来之笔！
如果让老教授独自写，写完这 4 个词需要老老实实花 4 秒钟。
但现在，老教授微微一笑，他做了一件不可思议的事情：
老教授没有一个字一个字慢慢写，而是**把小助教脱口而出的这 4 个候选词，一口气并排摆在眼前的桌子上**！
老教授深吸一口气，利用自己庞大的大脑，**在同一秒钟之内，同时并行审查了这 4 个词**：
- 词 1（*“从”*）：老教授点头，“同意，过！”
- 词 2（*“前”*）：老教授点头，“同意，过！”
- 词 3（*“有”*）：老教授点头，“同意，过！”
- 词 4（*“座”*）：老教授点头，“同意！既然都说到这了，我顺手再赐给你第 5 个词：‘山’！”

在老教授往常只能产出 1 个词的这 1 秒钟里，师徒二人一打配合，居然**瞬间落盘了整整 5 个高品质词元**！

```
┌────────────────────────────────────────────────────────────────────────┐
│                        推测解码 (SPECULATIVE DECODING)                 │
├────────────────────────────────────────────────────────────────────────┤
│ 小草稿模型 (轻量、极速):                                               │
│   串行狂奔预猜 4 个候选词: [ "今天", "天气", "真的", "非常" ]          │
│                                                                        │
│ 大目标模型 (厚重、高智商):                                             │
│   单趟前向传播并行审核全部 4 个词 (Prefill 模式，喂饱算力核心)         │
│                                                                        │
│ 拒绝采样过滤器 (精确无损闸门):                                         │
│   "今天" ──► 通过！                                                    │
│   "天气" ──► 通过！                                                    │
│   "真的" ──► 驳回！(小模型猜得不好，大模型打叉，当场改写为 "格外")     │
│   后续候选词全部作废                                                   │
│                                                                        │
│ 战果: 在大模型原本只够生成 1 个词的时间里，一口气斩获 3 个完美词元！   │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 它在大模型里到底怎么工作？

读者此时肯定会产生一个巨大的怀疑：
“老师，小助教毕竟水平有限啊，如果他猜出来的词质量不高，老教授老是妥协，写出来的文章岂不是变水了、变笨了？”

**这就是推测解码最神圣的数学奇迹所在：它绝不妥协！**
科学家们设计了一套精妙绝伦的**推测拒绝采样准则**：
- 如果小模型猜的词概率比大模型还低，大模型以极高概率直接将其一枪毙命，绝不姑息；
- 当发生驳回时，大模型会立刻从一种经过精密缩放的**“残差补偿分布”**中，无缝捞出一个替代词。

数学家已经从概率论上严格证明了一个定理：
**经过推测解码吐出来的整段文字，其最终的概率分布，与大模型独自一人一字一句写出来的文章完全一致，没有任何哪怕一丝一毫的智商折损！**

---

## 如果拿掉它，世界会怎样？

如果没有推测解码：
- 像 70B 乃至数千亿级别的超级庞然大物，在单用户场景下的打字速度将永远被物理显存带宽死死按在每秒十几个词的龟速水平；
- 所谓“实时语音对话”（需要模型在一两百毫秒内给出反应）将变得极难落地。
推测解码利用一个深刻的哲学原理——**“猜测是串行的，但核验是完全并行的”**，生生撕裂了自回归生成的物理铁律。

---

## 老师划重点

!!! tip "核心心法"
    **推测解码是大模型系统领域的四两拨千斤之作。它让敏捷的轻骑兵在前方肆意侦察探路，让厚重的中军大将并行收割成果，在绝对保证大模型智商不降的前提下，斩获数倍的物理生成提速。**
"""

# -----------------------------------------------------------------------------
# Concept 29: Quantization
# -----------------------------------------------------------------------------
chapters["module-9/29-quantization.md"] = """# 概念 29 - 瘦身保龄球与黄金气泡膜：模型量化（INT4 / FP8）如何把千亿巨兽塞进单卡

---

## 老师引言：为什么把数字压扁成整数，大模型却依然能写诗编代码？

在日常生活中，我们常常追求高精度：
秤体重最好精确到小数点后三位，测量距离最好精确到毫米。
在深度学习刚刚诞生的很长一段时间里，科学家们也是这样认为的：每一个神经网络参数都必须用昂贵的 32 位单精度（FP32）或 16 位半精度（FP16）浮点数来小心翼翼地保存。
这就导致一个 70B 参数的大模型，光是把参数塞进显存就需要 **140 GB**，非得两张几万块钱的专业计算卡才能带得动。

然而，近年来整个 AI 圈掀起了一场震撼的“逆向运动”——**模型量化（Quantization）**：
工程师们大刀阔斧，硬生生把那些精密复杂的 16 位浮点数，直接截断压缩成了只有 8 位（FP8/INT8）甚至是区区 4 位（INT4）的极简小整数！
模型体积暴缩了 75%，原本需要几张卡的大脑现在一张消费级显卡就能塞下。更不可思议的是——**它的逻辑推理能力竟然几乎没有下降！**
大模型的大脑到底是怎么承受住这种看似毁灭性的“降维打击”的？

---

## 生活实体模型：行李箱里的保龄球与脆弱的黄金王冠

想象你正准备搭乘廉价航空去环游世界：

### 1. 16 磅实心保龄球 vs 4 磅轻木球（INT4 量化）
你的大行李箱里塞满了 16 磅重的实心铁保龄球（代表 16 位的 FP16 浮点权重）。
箱子沉重无比，你根本拎不动，机场托运费更是贵得吓人。
这时，一位懂材料的工匠告诉你：
*“何必带实心铁球呢？换成 4 磅重的压缩木球（4 位 INT4），涂上一样的油漆，它们在球道上滚动的路线和撞击木瓶的效果有 99% 的相似度！”*
你把铁球换成了木球：
- 行李箱重量暴跌至四分之一；
- 你在航站楼里飞奔的速度快了整整 4 倍（解码期显存搬运速度翻倍）；
- 同样的后备箱，现在能塞下四倍多的玩具！

### 2. 致命意外：箱子里的一顶脆弱黄金王冠（离群异常值 Outliers）
但就在大家兴高采烈把所有东西都换成粗糙木头时，悲剧发生了：
某个大模型被粗暴量化成 4 位整数后，突然开始胡言乱语，连基本的加减法都不会算了！
科学家们打开大脑一检查，发现了令人惊异的现象：
在千亿个神经突触里，有 99.9% 都是普普通通的衣服鞋袜，数值很平缓；
但有极少数 **0.1% 的核心特征通道**，突然爆发出了高达数百倍的巨大数值！这就像行李箱里混进了一顶吹弹可破的纯金王冠！
如果你拿着大铁锤不分青红皂白把所有东西一起粗暴砸扁，王冠瞬间粉碎，大模型当场脑死亡！

### 3. 救命神器：SmoothQuant 与 AWQ（智能气泡膜）
为了保护这 0.1% 珍贵无比的黄金王冠，科学家发明了现代量化的两大绝招：
- **SmoothQuant（平滑等价量化）**：用一把数学放大镜，把王冠上的尖锐突起平摊压平，悄悄转移到原本平坦的衣服堆里，实现了权重与激活皆可量化的完美平衡；
- **AWQ（激活感知权重量化）**：精准识别出哪些权重直接连接着黄金王冠，为这 1% 的关键部位套上坚固的特种气泡膜保护壳，给足精度保护；剩下的普通木头则放心截断成 4 位！

```
┌────────────────────────────────────────────────────────────────────────┐
│                        对称 4 位整数 (INT4) 网格示意                   │
├────────────────────────────────────────────────────────────────────────┤
│ 原始 16 位浮点连续范围 (精度极高但极沉重):                             │
│   -7.0 ────────────────────── 0.0 ────────────────────── +7.0          │
│     │                           │                          │           │
│     ▼ 缩放投射 (Scale = 1.0)    ▼                          ▼           │
│ 离散的 4 位小整数格子 (只有 15 个格子, 极轻极快):                       │
│    -7   -6   -5   -4  ...  -1   0   +1  ...   +5   +6   +7           │
│   [存储仅需 4 个二进制位 0111]                                         │
│ ──► 显存消耗直降 75%，逐字解码搬运耗时瞬间压缩至四分之一！             │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 它在大模型里到底怎么工作？

在大模型推理硬件中，量化带来的提振是立竿见影的：
1. **显存占用斩断**：原来需要 8 张卡才能跑的 70B 模型，现在 1 到 2 张显卡就能轻松装载；
2. **打破显存带宽墙**：在自回归解码阶段，计算核心的大部分时间都在等显存搬运。现在搬运的包裹小了 4 倍，总线不再拥堵，**模型的吐字速度往往能直接迎来 2 到 4 倍的肉眼可见暴涨**！

---

## 如果拿掉它，世界会怎样？

如果没有现代模型量化技术：
- 开源大模型将沦为极少数科技巨头在专用机房里的昂贵游戏；
- 手机端、笔记本电脑端以及个人本地离线部署大模型（如 Ollama、llama.cpp）将成为不可能的空中楼阁。
正是量化技术，真正撕碎了算力与显存的贵族门槛，让千亿大模型走入了千家万户的普通电脑。

---

## 老师划重点

!!! tip "核心心法"
    **模型量化是对神经元冗余信息的聪明断舍离。它用微不足道的舍入误差，换取了 75% 的显存解放与数倍的生成提速；而对异常离群通道的精妙呵护，则是守住大模型心智火种的终极护城河。**
"""

# -----------------------------------------------------------------------------
# Concept 30: Distributed Serving & PD Disaggregation
# -----------------------------------------------------------------------------
chapters["module-9/30-distributed-serving.md"] = """# 概念 30 - 后厨切菜与前厅倒茶：分布式推理服务与预填解码解耦架构（PD 分离）

---

## 老师引言：为什么把两件好事放在一起做，反而会酿成大祸？

在现代工业制造中，流水线有一条铁律：**工种特性截然不同的两个岗位，绝对不能强行挤在同一张工作台上**。

然而，在过去很长一段时间里，大模型的服务架构却一直在犯这个大忌：
所有的云厂商都在同一张 GPU 显卡上，一边处理新用户发来的几千字长提示词（Prefill），一边给老用户吐出一个个新字（Decode）。
结果呢？系统内部天天打架，网络拥堵不堪，用户端体验极其糟糕。
直到近年来，**预填-解码物理分离架构（PD Disaggregation，亦称 PD 解耦）** 与 **张量并行（Tensor Parallelism）** 横空出世，才为大规模工业级大模型集群树立了终极标准。

---

## 生活实体模型：大饭店里的切菜大厨与奉茶侍者

想象一家声名远扬的超级大饭店，后厨里有两位核心员工：

### 1. 狂暴切菜大厨（Prefill 预填任务）
身形魁梧的大厨手握两把重型菜刀。每当外卖订单一到，他必须在短短 3 秒钟内，以雷霆万钧之势把 50 斤土豆、胡萝卜剁成极细的细丝！
这个岗位需要**爆发性的巨额力量（计算密集型，拉满 GPU 的 Tensor Cores）**。

### 2. 优雅奉茶侍者（Decode 解码任务）
在前厅里，一位侍者托着精致的银质茶盘。每隔整整 1 秒钟，他迈着小碎步优雅地走到客人面前，倒上一小勺热茶。
这个岗位需要**节奏极其平稳、绝对不能被打扰（显存带宽型，极度看重每一步的低延迟）**。

### 3. 一体化混跑的灾难：大砍刀震飞了客人的茶杯！
过去，饭店老板偏偏要求：大厨和侍者必须**共用同一张案板**！
大厨正挥着大砍刀疯狂剁土豆，案板剧烈晃动；
侍者端着茶杯刚走过来，滚烫的茶水直接被震飞了一地；
客人等得口干舌燥，痛苦地大喊：“服务员，我的下一口茶怎么等了 5 分钟还没倒上来？！”
这就是一体化大模型服务里的**长尾延迟雪崩（$P_{99}$ TPOT 暴跌）**——每当一个长文本用户发来请求，正在流畅看字的所有其他用户都会瞬间卡顿！

### 4. PD 分离革命：后厨切菜，前厅倒茶，专线传送！
新掌柜大刀阔斧推行改革：
- 把狂暴切菜的大厨，全部安置在**后院的独立重切车间（专属预填集群 Prefill Nodes，堆满算力大卡）**；
- 把奉茶侍者，全部安置在**安静优雅的迎宾茶厅（专属解码集群 Decode Nodes，专精高显存带宽）**；
- 两个车间之间，架设了一条极速飞驰的直通传送带（**超高带宽 RDMA 网络**）！
大厨在后厨 3 秒切完蔬菜，立刻把半成品（算好的 KV Cache）通过传送带唰地一下滑入前厅；
侍者从容接过食材，节奏丝滑平稳，再也没有任何一把大砍刀能够震晃客人的茶杯！

```
┌────────────────────────────────────────────────────────────────────────┐
│                        预填-解码物理分离 (PD DISAGGREGATION)           │
├────────────────────────────────────────────────────────────────────────┤
│ 用户请求 ──► 智能负载均衡网关                                          │
│                   │                                                    │
│                   ▼                                                    │
│   ┌────────────────────────────────┐                                   │
│   │ 预填集群 (Prefill Nodes):      │                                   │
│   │   - 算力怪兽，堆满计算核心     │                                   │
│   │   - 瞬间吃下数千字长 Prompt    │                                   │
│   └────────────────────────────────┘                                   │
│                   │                                                    │
│                   ▼ 超高速 RDMA 网络专线 (只花十几毫秒把 KV Cache 传送) │
│   ┌────────────────────────────────┐                                   │
│   │ 解码集群 (Decode Nodes):       │                                   │
│   │   - 显存带宽之王               │                                   │
│   │   - 丝滑稳定吐字，零卡顿零干扰 │                                   │
│   └────────────────────────────────┘                                   │
│                   │                                                    │
│                   ▼                                                    │
│           流式输出高品质答案 (极低时延)                                 │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 它在大模型里到底怎么工作？

除了宏观集群上的 PD 分离，在微观单台服务器内部，还存在一个根本性的工程难关：**模型实在太大了，单张卡根本塞不下！**
这就要依靠大名鼎鼎的 **Megatron 张量并行（Tensor Parallelism）**：
- 想象把一张巨大的矩阵算式像切积木一样，纵向切一刀（列切分），横向切一刀（行切分）；
- 8 张 GPU 彼此紧密相连，各自只算手头这一小块积木；
- 最神奇的是，在中间层运算时，8 张卡完全各算各的，零网络开销；只有在算完这一层时，大家才在超高速总线（NVLink）上轻轻击一下掌（All-Reduce 求和）。

---

## 如果拿掉它，世界会怎样？

如果没有张量并行与 PD 解耦架构：
- 像拥有 4050 亿参数的 Llama-3-405B 或者 6710 亿参数的 DeepSeek-V3 这种世界顶尖巨兽，人类根本无法将其部署上线；
- 在高并发、数百万人同时在线的真实生产环境里，系统要么会因为频繁打架而崩溃，要么只能承受极其低劣的卡顿体验。
PD 分离与分布式并行，代表着人类将大语言模型从实验室原型真正打造成世界级工业基础设施的巅峰智慧。

---

## 老师划重点

!!! tip "核心心法"
    **预填是奔放的算力风暴，解码是静水深流的节奏合奏。将异构的两相物理在空间上彻底解耦，并用张量切分化整为零，是大规模服务万亿级超级大模型的终极工业范式。**
"""

for path, content in chapters.items():
    full_path = os.path.join("/Users/guofei/workspace/the-math-behind-llm/concepts/docs", path)
    with open(full_path, "w", encoding="utf-8") as f:
        f.write(content)
    print(f"Generated: {full_path}")
