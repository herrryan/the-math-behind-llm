import os

BASE_DIR = os.path.abspath(os.path.join(os.path.dirname(__file__), "..", "docs"))

chapters = {}

# ==============================================================================
# Module 0: Hardware Landscape
# ==============================================================================

chapters["module-0/00-silicon-anatomy.zh.md"] = r"""# 第 E00 章：芯片物理拓扑架构：HBM、SRAM、Tensor Core 与高速互联

同学，你好！欢迎来到系统工程的第一堂课！

在开始编写大模型的任何一行系统代码之前，老师想先请你闭上眼睛，想象一下我们手头这块价值数十万元的 GPU 芯片到底长什么样。它绝不是一个只认得 Python 代码的“魔法黑盒子”，而是一座有着极其严密阶级分工的**微观工业超级工厂**。

今天，老师就带你穿上防尘服，拿起显微镜，一起走进这颗硅基芯片的心脏！

---

## 步骤 1：物理直觉（米其林后厨的四级食材仓库）

老师常说，理解硬件层级结构最好的办法，就是看看顶级餐馆的后厨是怎么分工的。

假设你是一位刀法出神入化的**米其林主厨**，每秒钟能以极其惊人的速度切菜（这就是我们芯片里的 **Tensor Core 运算单元**）。在你的厨房里，有四个存放食材的地方：

1. **你手边的切菜案板（寄存器 Registers）：**  
   就在你眼睛正下方、两只手的攻击范围之内。你一伸手就能拿到，连头都不用抬，**零延迟（1 个时钟周期）**。但案板面积太小了，上面最多只能摆下一颗洋葱、两根胡萝卜。
2. **你身后的半米备餐台（片上共享内存 / SRAM）：**  
   转身一步就能碰到，拿一碗切好的牛肉片只需要 **2 秒钟（20-30 个时钟周期）**。备餐台能放几十碗调料和半成品，但也放不下整家餐馆一星期的全部存货。
3. **远在地下二层的中央大型冷冻库（高带宽显存 HBM）：**  
   里面整整齐齐码放着 80 吉字节（GB）的海量肉类和蔬菜。存货确实极其充裕，但每一次案板上缺了食材，你都必须**停下手中的厨刀，解下围裙，走出厨房，穿过两条走廊，坐电梯下到地下二层**，开锁搬出一整箱冻肉，再一路扛回后厨！就算餐馆专门修建了一条超宽的高速电梯（高显存带宽），这趟折腾也需要 **200 到 400 秒（数百个时钟周期）**。
4. **隔壁街区连锁分店的物流中心（跨机通信 PCIe / NVLink）：**  
   当自己餐馆的冷库全空了，必须向隔壁街区分店借食材时，货车在马路上堵车运输的时间将成千上万倍地增加。

你看，**主厨切菜的速度再快，决定上菜总时长的往往不是主厨的手速，而是帮工从地下冷库搬运食材的速度！**

如果帮工搬得慢，主厨绝大部分时间都只能两手叉腰、在案板前干瞪眼。这就是现代大模型芯片最真实的物理写照。

---

## 步骤 2：芯片微观底层执行机制

现在，老师把这套后厨比喻映射到真实的硬件结构上。以大家在服务器机房最常用的 **NVIDIA H100 SXM** 芯片为例：

```
┌──────────────────────────────────────────────────────────────────────────────────┐
│                             NVIDIA H100 GPU (132 个 SM)                          │
│                                                                                  │
│  ┌─────────────────────────────────┐       ┌─────────────────────────────────┐  │
│  │ 流式多处理器 (SM 0)             │  ...  │ 流式多处理器 (SM 131)           │  │
│  │                                 │       │                                 │  │
│  │  [寄存器堆 Registers]           │       │  [寄存器堆 Registers]           │  │
│  │  容量: 64K x 32-bit (256 KB)    │       │  容量: 64K x 32-bit (256 KB)    │  │
│  │  访问延迟: 1 周期               │       │  访问延迟: 1 周期               │  │
│  │  聚合带宽: ~33 TB/s             │       │  聚合带宽: ~33 TB/s             │  │
│  │                                 │       │                                 │  │
│  │  [4 个第 4 代 Tensor Core 单元] │       │  [4 个第 4 代 Tensor Core 单元] │  │
│  │  FP16 峰值算力: ~15 TFLOPS / SM │       │  FP16 峰值算力: ~15 TFLOPS / SM │  │
│  │                                 │       │                                 │  │
│  │  [片上共享内存 / L1 SRAM]       │       │  [片上共享内存 / L1 SRAM]       │  │
│  │  单 SM 容量: 228 KB             │       │  单 SM 容量: 228 KB             │  │
│  │  访问延迟: ~20-30 周期          │       │  访问延迟: ~20-30 周期          │  │
│  │  聚合带宽: ~17 TB/s             │       │  聚合带宽: ~17 TB/s             │  │
│  └────────────────┬────────────────┘       └────────────────┬────────────────┘  │
│                   │                                         │                   │
│                   └────────────────────┬────────────────────┘                   │
│                                        │                                        │
│                         [二级缓存 L2 Cache: 50 MB, ~5 TB/s]                     │
│                                        │                                        │
│                         [高带宽显存 HBM3: 80 GB, 3.35 TB/s]                     │
│                         访问延迟: 200-400 时钟周期                              │
└────────────────────────────────────────┬────────────────────────────────────────┘
                                         │
                 [NVLink 4 互联: 双向 900 GB/s 极速直连同节点 GPU]
                 [PCIe Gen 5 总线: 双向 64 GB/s 连接主机 CPU 内存]
```

### 老师为你拆解核心硬件部件：

1. **SM（Streaming Multiprocessor，流式多处理器）：**  
   这是 GPU 芯片里的独立厨房工位。整块 H100 里面有整整 132 个这样独立的工位，它们彼此并行、互不干扰地同时做菜。
2. **寄存器堆（Registers）：**  
   Tensor Core 算术指令只能直接操作寄存器里的数字。每个 SM 拥有 256 KB 寄存器空间。如果你的 CUDA 线程申请的局部变量太多，装不下了，就会发生**寄存器溢出（Register Spilling）**，多出来的变量会被硬件踢到几百周期开外的慢速显存里，代码速度瞬间腰斩！
3. **片上共享内存（Shared Memory / SRAM）：**  
   同一个 SM 内的所有线程可以共同读写的一块高速缓存（每 SM 约 228 KB）。稍后我们要学的 **FlashAttention**，核心秘诀就是把数据从地下冷库运到这块 SRAM 备餐台上，反复算透了再走。
4. **高带宽显存（HBM，High Bandwidth Memory）：**  
   就是大家平时嘴里说的“单卡 80GB 显存”。它不是一根细细的内存条，而是像多层千层饼一样用微米级焊锡球直接垂直堆叠在 GPU 核心周围的 DRAM 颗粒。虽然带宽高达 $3.35\text{ TB/s}$，但由于物理距离较远，依然比片上慢了上百倍。

---

## 步骤 3：跨组件相互耦合机制

你可能会问：*“老师，我写 PyTorch 代码时，只调用了一个 `y = x + bias`，为什么需要关心这个硬件层级呢？”*

老师来告诉你这背后的**蝴蝶效应**：

1. **算子数量与内核启动开销（Kernel Launch Overhead）：**  
   如果你在 Python 里写了 5 个小函数连续执行，每次调用都会让 CPU 通过 PCIe 向 GPU 发送指令，SM 必须把中间结果完整写回地下冷库 HBM，再由下一个算子重新从冷库读出来。  
   这就好比：你切了一刀胡萝卜，特意脱下围裙跑到地下冷库把胡萝卜冻起来；过了两秒钟，又特意跑下楼把它搬上来切第二刀！
2. **算子熔合（Kernel Fusion）的系统威力：**  
   如果我们把这 5 个小操作打包写成一个统一的 CUDA / Triton 核函数，主厨在案板上一次性切完、炒熟、装盘，所有中间产物直接在寄存器里消化掉，**根本不需要往地下冷库跑一趟**。系统的总线读写量立减 80% 以上！

---

## 步骤 4：精确性能数学公式

老师带你推导一个最基础但也最重要的计算：**显存往返耗时公式**。

假设一个输入张量 $\mathbf{X}$ 包含 $N$ 个元素，数据格式为 FP16（每个元素占据 2 个字节）。若某个算子需要将数据从 HBM 读入片上、运算、再写回 HBM：

总搬运字节数为：

$$\text{Bytes} = 2 \times N \times \text{sizeof(FP16)} = 4 N \text{ 字节}$$

纯粹花在显存总线上的物理数据搬运时间（以 HBM 带宽 $\text{BW}_{\text{HBM}}$ 为基准）为：

$$T_{\text{memory}} = \frac{\text{Bytes}}{\text{BW}_{\text{HBM}}} = \frac{4 N}{\text{BW}_{\text{HBM}}}$$

若该算子在每个元素上只执行了 1 次加法（总浮点运算量 $\text{FLOPs} = N$ 次），而芯片的峰值计算耗时为：

$$T_{\text{compute}} = \frac{N}{P_{\text{peak}}}$$

真实的硬件执行总时间受限于木桶的最短板：

$$T_{\text{exec}} \ge \max(T_{\text{compute}}, T_{\text{memory}}) \approx T_{\text{memory}} \quad (\text{当 } T_{\text{memory}} \gg T_{\text{compute}})$$

---

## 步骤 5：具体微基准数字推导

拿出你的草稿纸和计算器，跟老师一起来算一笔真实的账！

我们以 **LLaMA-3 70B** 模型的一个前向层为例：
- 批大小 $B = 1$
- 序列长度 $S = 4096$
- 隐藏层维度 $d = 8192$
- 元素总数：$N = B \times S \times d = 1 \times 4096 \times 8192 = 33,554,432$ 个元素（约 $3355$ 万个数字）。

现在，我们对这个张量执行一个极其简单的**偏置加法（Bias Add）**：

### 1. 计算物理数据量
每个数字是 FP16（2 字节）：
- 读取输入：$33,554,432 \times 2 \approx 67.1 \text{ MB}$
- 写回输出：$33,554,432 \times 2 \approx 67.1 \text{ MB}$
- **总 HBM 读写流量：** $\text{Bytes} = 67.1 + 67.1 = 134.2 \text{ MB}$

### 2. 计算在 H100 上的显存搬运耗时
H100 SXM 的 HBM 带宽为 $\text{BW}_{\text{HBM}} = 3.35 \text{ TB/s} = 3.35 \times 10^{12} \text{ 字节/秒}$：

$$T_{\text{memory}} = \frac{134.2 \times 10^6 \text{ 字节}}{3.35 \times 10^{12} \text{ 字节/秒}} \approx 0.000040 \text{ 秒} = 40.0 \;\mu\text{s}$$

### 3. 计算在 H100 上的纯计算做功耗时
总做功只有 $3355$ 万次浮点加法（$3.355 \times 10^7 \text{ FLOPs}$）。H100 的 FP16 峰值算力高达 $1,000 \text{ TFLOPS} = 10^{15} \text{ FLOPs/秒}$：

$$T_{\text{compute}} = \frac{3.355 \times 10^7}{10^{15}} \approx 0.000000033 \text{ 秒} = 0.033 \;\mu\text{s}$$

### 4. 老师带你对比两者的差距：
- 计算耗时：$0.033$ 微秒；
- 搬运耗时：$40.0$ 微秒！

也就是说，**在整整 40 微秒的时间里，高达 99.9% 的时间 GPU 都在路上搬运数据，真正用于挥刀切菜的时间只有可怜的 0.08%！** 

你看，如果不做算子熔合，你花上百万元买来的顶级计算芯片，是不是绝大部分时间都在睡大觉？

---

## 步骤 6：核心系统工程铁律

> **大模型系统优化的第一准则：永远不要让主厨等食材。**  
> 永远铭记从冷库到案板的距离差异。尽量让数据留在片上高速缓存和寄存器中反复运算，非必要决不把中间临时数据写回全局显存。
"""

chapters["module-0/01-roofline-model.zh.md"] = r"""# 第 E01 章：屋顶模型推导与算术强度：计算密集型 vs 访存密集型工况判定

同学，你好！在上一章里，我们推导出了一个令人震惊的事实：如果算法设计不当，GPU 99% 的时间可能都在等数据运过来。

今天这节课，老师要教你一套在整个高性能计算和 AI 基础设施界无往不利的“照妖镜”——**屋顶模型（Roofline Model）**。

学会了它，以后面对任何一个运行缓慢的大模型代码，你只需掏出草稿纸算两分钟，就能一眼看穿它的物理病根到底在哪里！

---

## 步骤 1：物理直觉（果汁摊里的破壁机与搬运工）

为了让你彻底搞懂，老师给你讲一个校门口鲜榨果汁摊的生动故事：

你的果汁摊上有两位搭档：
1. **备料搬运工（显存带宽）：** 负责从后面的仓库搬来大箱的冻草莓和纯净水。他手脚再快，**每分钟最多也只能拆箱倒进 3 公斤原材料**。
2. **大功率超高速破壁机（Tensor Core 算力）：** 一台工业级搅拌机，刀头转速极高，**每分钟能疯狂旋转 3,000 圈**。

现在，店里来了两位截然不同的顾客：

- **第一位顾客点了一杯“清爽草莓水”：**  
  配方很简单：倒满 1 升纯净水，扔进 1 颗小草莓，破壁机轻轻转动 2 圈搅拌均匀即可出杯。  
  搬运工为了搬来沉重的大桶水，气喘吁吁忙活了整整 20 秒；而破壁机“嗡”了一声，只转了 0.1 秒就停下了。在剩下的 19.9 秒里，昂贵的大功率破壁机完全闲置，只能干等着搬运工去搬下一桶水。  
  此时，果汁摊的产能完全被搬运工卡死，这在计算机体系结构中叫做**访存密集型（Memory-Bound，搬运受限）**。

- **第二位顾客点了一碗“极致浓缩草莓冻霜”：**  
  配方截然相反：不需要加水，只要搬来一小碗草莓，但要求破壁机以最高转速疯狂研磨 30 秒，打碎到微米级极度细腻的糊状。  
  搬运工花 1 秒钟把一小碗草莓倒进杯子后，就可以在旁边翘着二郎腿喝茶了；而破壁机必须开足全马力轰鸣整整半分钟！  
  此时，果汁摊的产能完全被破壁机的机械功率卡死，这叫做**计算密集型（Compute-Bound，算力受限）**。

你看，**破壁机到底转了多少圈（FLOPs 做功次数），除以搬运工到底扛了多少斤食材（Bytes 搬运字节数），这个比值就叫做“算术强度”！**

---

## 步骤 2：芯片微观底层执行机制

在计算机硬件中，加州大学伯克利分校的图灵奖得主 David Patterson 教授等人将这一物理现象抽象为了著名的**屋顶模型二维坐标图**：

```
真实达到的算力 [TFLOPS]
         ▲
峰值     │                          /────────────────────────── (算力天花板: P_peak)
算力     │                         /
天花板   │                        /
         │                       /
         │                      / ◄── 显存带宽斜坡天花板
         │                     /      斜率 = 硬件显存带宽 (BW_mem)
         │                    /
         │                   /
         │                  /
         │                 / │
         └───────────────┴───┴───────────────────────────────► 算术强度 I
                         0   I* (硬件平衡临界拐点)                 [FLOPs / Byte]
                          ▲
                          │
          [访存受限区间]  │   [计算受限区间]
          I < I*          │   I > I*
          (显存总线跑满,  │   (Tensor Core 跑满,
           算力单元挨饿)  │    显存带宽充裕)
```

### 老师带你细读坐标轴的每一个参数：

1. **纵轴：实际达到的计算吞吐（Performance）：**  
   单位是 $\text{TFLOPS}$（每秒万亿次浮点运算）。纵轴最高有一根平平的“天花板”，这就是芯片的**物理峰值算力 $P_{\text{peak}}$**（无论你的代码写得多好，都不可能越过硬件的理论天花板）。
2. **横轴：算术强度（Operational Intensity，符号为 $I$）：**  
   单位是 $\text{FLOPs / Byte}$。它衡量你的算法**每从显存里艰难搬来 1 个字节的数据，能够在芯片里对它反复蹂躏、计算多少次**！
3. **左侧倾斜的斜坡（斜率即物理带宽 $\text{BW}_{\text{mem}}$）：**  
   如果你的算术强度很低（在左侧），你根本够不着上方的平顶天花板，你只能撞在左边的斜坡上！因为此时显存总线已经堵死了，算力再高也使不出来。
4. **屋檐转折点（硬件平衡拐点 $I^*$）：**  
   两个天花板相交的那一个交点，是整张图的核心灵魂！
   $$I^* = \frac{P_{\text{peak}}}{\text{BW}_{\text{mem}}}$$

---

## 步骤 3：跨组件相互耦合机制

理解了屋顶模型，你就能一眼看懂大模型中两个截然不同的核心阶段：

1. **提示词预填阶段（Prefill 阶段）：**  
   当用户把一大段长达 2000 字的提示词喂给大模型时，模型要把这 2000 个 Token 的大矩阵与权重矩阵相乘。此时矩阵尺寸极大，算术强度极高（$I \gg I^*$），直接落入**计算密集型区间**！这一阶段，Tensor Core 满负荷轰鸣，芯片发热量飙升，系统瓶颈在于算力是否充沛。
2. **逐字吐字生成阶段（Decode 阶段）：**  
   大模型自回归生成时，每次只能蹦出 1 个新词（批大小为 1 时，输入矩阵行数 $M = 1$）。为了生成这单个 Token，GPU 必须把整整 700 亿个参数从冷库 HBM 完完整整读取一遍，但每个参数只做了一次乘法！算术强度低到了可怜的 $I \approx 1\text{ FLOP/Byte}$，深陷在**极度访存受限区间**！此时 Tensor Core 几乎全程都在睡觉打呼噜。

---

## 步骤 4：精确性能数学公式

任何算法在硬件上能达到的理论最高性能上限 $P$，可以用一个极简的数学公式统领：

$$P = \min\left(P_{\text{peak}}, \; I \times \text{BW}_{\text{mem}}\right)$$

执行该任务所需的理论最短物理时间为：

$$T_{\text{min}} = \max\left( \frac{\text{FLOPs}}{P_{\text{peak}}}, \; \frac{\text{Bytes}}{\text{BW}_{\text{mem}}} \right)$$

---

## 步骤 5：具体微基准数字推导

来，拿出笔，跟老师一起在现代主流芯片上算一下这个神秘的临界点 $I^*$！

以 **NVIDIA H100 SXM** 芯片为例：
- BF16/FP16 密集峰值算力：$P_{\text{peak}} \approx 1,000 \text{ TFLOPS} = 1.0 \times 10^{15} \text{ FLOPs/s}$
- HBM3 物理显存带宽：$\text{BW}_{\text{mem}} \approx 3.35 \text{ TB/s} = 3.35 \times 10^{12} \text{ Bytes/s}$

### 1. 计算硬件平衡拐点 $I^*$
$$I^* = \frac{1.0 \times 10^{15}}{3.35 \times 10^{12}} \approx 298.5 \text{ FLOPs/Byte}$$

**老师敲黑板划重点：**  
这个数字 $298.5$ 意味着什么？  
它意味着在 H100 上，**你每从显存里搬运 1 个字节（半个 FP16 数字），必须在芯片里对它反复运算整整 298 次，才能让显卡发挥出它宣传的 1000 TFLOPS 峰值！**

### 2. 算一算自回归生成（Decode）的尴尬窘境
当以 Batch Size = 1 运行 LLaMA-3 8B（权重大小为 $8 \times 10^9$ 个参数，FP16 占用 $16\text{ GB}$）：
- 每次生成 1 个 Token，总浮点运算量约为：$2 \times 8 \times 10^9 = 1.6 \times 10^{10} \text{ FLOPs}$；
- 必须从 HBM 全量加载权重一次，访存量：$\text{Bytes} \approx 1.6 \times 10^{10} \text{ 字节}$（$16\text{ GB}$）；
- 实际算术强度：
  $$I_{\text{decode}} = \frac{1.6 \times 10^{10} \text{ FLOPs}}{1.6 \times 10^{10} \text{ Bytes}} = 1.0 \text{ FLOP/Byte}$$

### 3. 查看它在屋顶模型上的位置
因为 $1.0 \ll 298.5$，它深深跌落在左侧的访存受限斜坡上！
它所能发挥出的实际硬件算力只有：

$$P = 1.0 \times 3.35 \text{ TB/s} = 3.35 \text{ TFLOPS}$$

实际算力利用率只有：

$$\frac{3.35 \text{ TFLOPS}}{1000 \text{ TFLOPS}} = 0.335\%$$

你没看错，千分之三点三！这就是为什么在单请求推理时，显卡利用率看起来低得吓人。

---

## 步骤 6：核心系统工程铁律

> **优化代码前，先问屋顶模型：我到底被卡在哪堵墙上？**  
> 如果算术强度 $I < I^*$，优化任何计算指令都是徒劳的，必须全力减少 HBM 访存；只有当 $I > I^*$ 时，优化矩阵乘法、提升并发分块才真正具有物理意义。
"""

chapters["module-0/02-memory-layouts-and-coalescing.zh.md"] = r"""# 第 E02 章：内存物理布局、跨步与合并访存：为什么排整齐能让速度快 10 倍？

同学，你好！在前面两节课里，我们知道了显存带宽是大模型系统中最宝贵的黄金通道。

那么今天这堂课，老师要带你探究一个最基础但又最容易被初学者忽视的工程细节：**我们在内存里摆放数字的姿势**。

你可能会觉得奇怪：“老师，数字不就是存进内存吗？横着摆、竖着摆，计算机不都是按地址去取吗？难道还会影响速度？”  
老师要郑重地告诉你：**不仅会影响，而且速度相差可以高达整整数十倍！**

---

## 步骤 1：物理直觉（邮递员送信与大巴车接送）

为了让你秒懂，老师打两个生活中的小比方：

### 比方一：邮递员送信
想象你是一位邮递员，今天你的任务是把 32 封信送给同一条街上的 32 户居民：
- **情况 A（排成一排，合并访存）：** 这 32 户人家的门牌号是 1 号、2 号、3 号……连成整齐的一长排。你开着邮车顺着路边缓缓滑行，手一伸，啪啪啪一口气把 32 封信投进并排的邮箱里，**整条街送完只用了 10 秒钟**。
- **情况 B（东一家西一家，跨步非合并访存）：** 32 户人家的地址全被打散了，分别是 1 号、101 号、201 号……分散在城市的 32 个不同街区。每送一封信，你都得在马路上掉头、堵车、等红绿灯、下车投递、再重新启动。同样是送 32 封信，**这一趟下来折腾了你整整半个小时**！

### 比方二：物理芯片的大巴车特性（缓存行 Cache Line）
在 GPU 的物理显存里，有一个极其铁面无私的规则：**它从来不发私家车，只发固定载客 128 人的大客车！**

即使你的程序只需要读取 1 个 2 字节（FP16）的孤单数字，显存总线也绝不会单独为了这 2 个字节跑一趟。它必须一视同仁地从物理显存里**一铲子挖起整整 128 个字节的一整块（这叫一个物理缓存行 Cache Line）**打包送过来。

- 如果一个线程束（Warp）里的 32 个小线程要读的数据刚好紧密挨在一起，那么一辆 128 字节的大巴车开过来，刚好把这 32 个人的数据全部装满运走，**总线利用率 100%**！
- 但如果这 32 个线程各要各的，访问的数据在物理显存里相隔几百米，GPU 就被迫同时派出 32 辆空荡荡的大巴车！每辆大巴车上只孤零零坐了一个 2 字节乘客，其余 126 字节全是空气。**总线有效利用率瞬间暴跌到 1.5%**！

---

## 步骤 2：芯片微观底层执行机制

在 GPU 芯片内部，最小的基本调度执行单元是由 32 个线程组成的**线程束（Warp）**。

当一个 Warp 里的 32 个线程同时执行一条读取全局显存的指令时，硬件上的内存控制器会立刻对这 32 个线程发出的内存地址进行“安检”：

```
【场景一：合并访存 (Coalesced Memory Access)】
32 个线程请求连续紧邻的物理地址:
Warp 线程:    T0   T1   T2   T3   ...   T30  T31
              │    │    │    │           │    │
目标字节地址: [00] [02] [04] [06]  ...  [60] [62]
              └────┴────┴────┴───────────┴────┘
              ▼
[单次 128 字节物理事务传输完成]  -->  显存总线有效利用率: 100%! (0 浪费)

--------------------------------------------------------------------------------

【场景二：大跨步非合并访存 (Strided Memory Access)】
32 个线程访问离散跳跃的物理地址 (步长 Stride = 128 字节):
Warp 线程:    T0           T1           T2          ...   T31
              │            │            │                 │
目标字节地址: [0000]       [0128]       [0256]       ...  [3968]
              ▼            ▼            ▼                 ▼
          [大巴车 1]    [大巴车 2]    [大巴车 3]          [大巴车 32]
总计触发: 32 次独立的 128 字节物理显存传输事务！
总线实际搬运数据量: 32 x 128 = 4096 字节。
真正有用数据量: 32 个线程 x 2 字节 = 64 字节。
总线传输有效利用率: 64 / 4096 = 1.56%! (98.4% 的宝贵显存带宽全部被浪费在运空气上!)
```

### 多维张量在底层物理内存中的跨步表示（Strides）
在计算机内存里，所有的多维张量（无论你是 2 维矩阵还是 4 维张量）在物理层面上全都是一条**一维扁平的一字长蛇阵**。

那么多维坐标 $(i, j, k)$ 是怎么定位到物理内存位置的呢？老师写出这个经典的定位公式：

$$\text{元素物理偏移量} = \text{base} + i \times \text{stride}_0 + j \times \text{stride}_1 + k \times \text{stride}_2$$

对于一个形状为 $(M, N)$ 的二维矩阵（按行优先存储）：
- `shape` = `(M, N)`
- `stride` = `(N, 1)`
- **横着扫（沿列连续）：** $\text{stride}_1 = 1$，相邻线程拿到的内存地址是相差 1 的连续地址，触发**完美的合并访存**。
- **竖着扫（沿行跨步）：** $\text{stride}_0 = N$，相邻线程拿到的内存地址相差整整 $N$ 个元素。如果 $N=8192$，相邻线程跨越几万字节，访存瞬间变成灾难级的**分散大跨步**！

---

## 步骤 3：跨组件相互耦合机制

你可能会问：*“老师，我在大模型里什么时候会竖着扫内存呢？”*

老师提醒你注意两个大模型中最常见的隐形性能陷阱：

1. **注意力机制中的转置陷阱（$Q K^\top$）：**  
   在计算注意力时，我们需要把 Key 向量进行转置。在 PyTorch 中写一句 `K.transpose(-1, -2)` 只耗费 0 微秒，因为它根本没搬运任何数据，只是把 `stride` 元组调换了一下。  
   但是！当接下来的矩阵乘法读取这个转置后的矩阵时，由于内层跨步不再是 1，相邻线程全部在做大跨步非合并读取！如果不做片上转置平铺优化，显存读取带宽就会被平白无故丢掉 70% 以上。
2. **LoRA 微调与量化反量化：**  
   低秩适应（LoRA）引入了小型旁路矩阵 $A$ 和 $B$。如果这两个矩阵的内存跨步没有与主模型的权重严格对齐，合并访存机制就会被打破，原本旨在加速的微调算子反而会因为内存访问碎片化而变慢。

---

## 步骤 4：精确性能数学公式

设一个线程束中 32 个线程请求的地址跨度为 $S$（单位为字节）。硬件每次传输的物理事务块大小为 $B_{\text{trans}} = 128 \text{ 字节}$，每个线程请求的数据有效字节为 $b_{\text{elem}}$（如 FP16 下 $b_{\text{elem}} = 2$）。

合并访存所需的物理总线事务次数为：

$$N_{\text{transactions}} = \left\lceil \frac{\max(\text{Addr}) - \min(\text{Addr}) + b_{\text{elem}}}{B_{\text{trans}}} \right\rceil$$

总线有效利用率（Bus Efficiency）定义为：

$$\eta_{\text{bus}} = \frac{32 \times b_{\text{elem}}}{N_{\text{transactions}} \times B_{\text{trans}}}$$

当地址严格连续且对齐时：
$$N_{\text{transactions}} = 1 \implies \eta_{\text{bus}} = \frac{32 \times 2}{1 \times 128} = \frac{64}{128} = 50\% \quad (\text{单次 64B/128B 事务完全对齐时可达 } 100\%)$$

当跨步大于 128 字节时：
$$N_{\text{transactions}} = 32 \implies \eta_{\text{bus}} = \frac{64}{32 \times 128} = \frac{64}{4096} \approx 1.56\%$$

---

## 步骤 5：具体微基准数字推导

我们来做一次实测模拟推导！

假设在 LLaMA-3 70B 模型中，对一个形状为 $(4096, 8192)$ 的权重激活矩阵执行逐元素处理，数据格式为 FP16（每个元素 2 字节）：
- 总元素数：$4096 \times 8192 = 33,554,432$ 个元素
- 理论有效数据量：$33,554,432 \times 2 \approx 67.1 \text{ MB}$

### 1. 合并访存场景（按行遍历，连续读取）
- 触发物理传输事务数：
  $$\frac{67.1 \times 10^6 \text{ 字节}}{128 \text{ 字节/事务}} \approx 524,288 \text{ 次事务}$$
- 在 H100（$3.35\text{ TB/s}$）上的纯搬运耗时：
  $$T_{\text{coalesced}} = \frac{67.1 \times 10^6}{3.35 \times 10^{12}} \approx 20.0 \;\mu\text{s}$$

### 2. 严重跨步非合并访存场景（错误地按列跨步遍历，跨步为 8192 元素）
- 每个线程读取的数据落在完全不同的 128 字节缓存行中！
- 实际被迫发起的物理事务总数据量：
  $$33,554,432 \times 128 \text{ 字节} \approx 4.29 \text{ GB}!$$
- 耗时暴涨为：
  $$T_{\text{strided}} = \frac{4.29 \times 10^9}{3.35 \times 10^{12}} \approx 1280.0 \;\mu\text{s} = 1.28 \text{ ms}$$
- **性能差距：** $1280 \;\mu\text{s} \div 20 \;\mu\text{s} = 64 \times$！  
  仅仅因为换了一个循环遍历方向，耗时整整慢了 **64 倍**！

---

## 步骤 6：核心系统工程铁律

> **让相邻的线程在相邻的物理内存上并肩作战。**  
> 永远保证内层计算循环沿着张量跨步为 1 的最内层连续维度展开；任何打破内存对齐与连续性的操作，都必须在片上高速缓存中完成转置重排，绝不让碎片化的大跨步访存践踏宝贵的高带宽显存。
"""

# ==============================================================================
# Module 1: Attention in Silicon
# ==============================================================================

chapters["module-1/03-naive-attention-memory-wall.zh.md"] = r"""# 第 E03 章：朴素注意力的致命缺陷：$O(S^2)$ 显存带宽墙与中间矩阵物化灾难

同学，你好！在前面的数学篇里，我们推导出了那个震惊整个 AI 界的经典注意力公式：

$$
\text{Attention}(\mathbf{Q}, \mathbf{K}, \mathbf{V}) = \text{softmax}\left(\frac{\mathbf{Q}\mathbf{K}^\top}{\sqrt{d_k}}\right)\mathbf{V}
$$

这个公式在数学上简洁、优美、优雅到了极致。  
但今天老师要给你泼一盆冷水：**如果你直接按照字面意思把这行数学公式写进 PyTorch 运行，在处理长文本时，你的显卡会在一秒钟之内惨叫着报出 OOM（显存溢出）而当场崩溃！**

为什么？因为这个公式在现实世界中有一个致命的死穴——**它试图在显存里摊开一张谁也装不下的巨型草稿纸！**

今天这节课，老师就带你彻底揭开朴素注意力的这道“显存带宽墙”。

---

## 步骤 1：物理直觉（厨房案板上的千层面惨案）

想象你在一家快餐厅后厨制作多层千层面。

现在主厨给你下达了一道指令：请制作一份由 32,000 张超薄面皮叠在一起的特制巨型千层面。制作规则如下：
1. 你必须先把这 32,000 张面皮全部摊开在桌面上，计算每两张面皮之间的接触面积（生成 $32000 \times 32000$ 的分数矩阵）；
2. 然后，你要在每两张面皮之间抹上一层特制酱汁并称重（计算 Softmax）；
3. 最后，把抹好酱汁的面皮卷起来装盘（乘以 Value 矩阵）。

这时你环顾四周，发现自己面前的**操作案板（片上 SRAM）只有半米宽**，根本放不下这整整十亿对重叠的配料！

### 朴素实现是怎么做的？
朴素的做法非常笨：
- 既然案板放不下，工人就**把整整 10 亿片半成品端下楼，完完整整铺在远在地下二层的大冷库地板上（写入 HBM）**！
- 铺满之后，工人走回厨房洗洗手；
- 紧接着，工人再次下楼，弯着腰给冷库地板上的 10 亿片面皮逐一涂抹酱汁（再次从 HBM 读出，算完 Softmax 再完整写回 HBM）；
- 抹完之后，工人再次回到楼上；
- 最后，工人第三次下楼，把冷库地板上的所有面皮重新搬上楼卷好装盘！

**这就是所谓的“中间矩阵物化（Materialization）”！**  
大白话讲：所谓“物化”，就是本来只在心算里过一遍的半成品草稿，非要在昂贵的冷库地面上完完整整抄写好几遍。工人 99% 的体力和时间全耗在楼梯跑腿上了，冷库地面也瞬间被堆满瘫痪！

---

## 步骤 2：芯片微观底层执行机制

在 PyTorch 等标准深度学习框架中，如果你直接写下 `scores = q @ k.T / math.sqrt(d); probs = softmax(scores); out = probs @ v`，底层会启动三个完全割裂独立的 CUDA 核函数：

```
全局显存 HBM (地下大型冷库)                 片上极速 SRAM (主厨案板)
     │                                                │
     ├── 1. 读取 Q, K 向量 ─────────────────────────► │ 核函数 1: 执行 Q @ K.T
     │                                                │ 在片上算出一块分数
     │ ◄── 2. 将大小为 S x S 的完整分数矩阵 S 写入 HBM ─┤ (产生巨额写显存开销!)
     │                                                │
     ├── 3. 重新从 HBM 全量读取 S 矩阵 ─────────────► │ 核函数 2: 执行 Softmax(S)
     │                                                │ 求行最大值、减最大值、求 exp、累加归一化
     │ ◄── 4. 将大小为 S x S 的概率矩阵 P 写回 HBM ────┤ (再次产生巨额写显存开销!)
     │                                                │
     ├── 5. 从 HBM 重新全量读取 P 矩阵与 V 矩阵 ────► │ 核函数 3: 执行 P @ V
     │                                                │ 最终加权求和
     │ ◄── 6. 将最终结果 O 写回 HBM ─────────────────┤
```

### 老师带你看看微观上的数据搬运有多恐怖：
- 注意力中间矩阵 $\mathbf{S}$ 和 $\mathbf{P}$ 的尺寸都是 $(B \times H, S, S)$，其中 $S$ 是序列长度。
- 请注意这个平方符号 $S^2$！序列长度翻倍，中间矩阵的尺寸就会暴涨 **4 倍**；序列长度如果翻 16 倍（从 2K 变成 32K），中间矩阵就会暴涨整整 **256 倍**！
- 这个巨大的 $S \times S$ 矩阵在物理上完全无法塞进单卡仅几十兆字节的片上高速缓存（SRAM）中，必须硬生生写出到慢速的全局显存（HBM）中，产生天量的数据搬运拥堵。

---

## 步骤 3：跨组件相互耦合机制

朴素注意力的这个致命缺陷，在全系统中引发了三场大地震：

1. **彻底锁死了大模型的上下文长度（Context Length）：**  
   长文本理解（如 32K、128K 甚至 1M）在朴素注意力下在物理上是绝对不可能实现的。单单一个 64K 的中间矩阵，就会直接抽干整块显卡的全部显存。
2. **反向传播激活值显存的“吞金巨兽”：**  
   在模型训练时，为了给反向传播求梯度使用，前向传播生成的 Softmax 概率矩阵 $\mathbf{P}$ 必须一动不动地保存在显存里。这单单一项，就吃掉了训练阶段高达 70% 的动态激活值显存！
3. **极度可怜的算力利用率（MFU 跌破 10%）：**  
   Softmax 算子本身只有简单的加减法和指数，算术强度低到了可怜的 $I \approx 1\text{--}2\text{ FLOPs/Byte}$。Tensor Core 几乎一直在等数据从 HBM 运过来，根本发挥不出芯片的威力。

---

## 步骤 4：精确性能数学公式

老师带你推导朴素注意力全过程的真实数据搬运量和浮点运算量：

### 1. 浮点运算量（FLOPs）
设批大小为 $B$，头数为 $H$，序列长度为 $S$，单头特征维度为 $d$：
- $\mathbf{Q}\mathbf{K}^\top$ 矩阵乘：$2 B H S^2 d$
- Softmax 计算：约为 $3 B H S^2$（求最大值、减法与 exp、归一化除法）
- $\mathbf{P}\mathbf{V}$ 矩阵乘：$2 B H S^2 d$

$$\text{总运算量 FLOPs}_{\text{naive}} \approx 4 B H S^2 d$$

### 2. HBM 全局显存搬运字节数（Bytes，以 FP16 计算，每元素 2 字节）
- 读取 $\mathbf{Q}, \mathbf{K}$：$2 \times (2 B H S d)$ 字节
- 写回中间分数 $\mathbf{S}$：$2 B H S^2$ 字节
- 读取中间分数 $\mathbf{S}$：$2 B H S^2$ 字节
- 写回概率矩阵 $\mathbf{P}$：$2 B H S^2$ 字节
- 读取概率矩阵 $\mathbf{P}$ 与 $\mathbf{V}$：$2 B H S^2 + 2 B H S d$ 字节
- 写回最终输出 $\mathbf{O}$：$2 B H S d$ 字节

汇总所有读写开销：

$$\text{总搬运量 Bytes}_{\text{naive}} = \underbrace{8 B H S d}_{\text{输入与输出线性开销}} + \underbrace{6 B H S^2}_{\text{中间矩阵平方级物化灾难!}} \text{ 字节}$$

当序列较长时（$S \gg d$），第二项平方项占据绝对统治地位：

$$\text{Bytes}_{\text{naive}} \approx 6 B H S^2 \text{ 字节}$$

---

## 步骤 5：具体微基准数字推导

拿出计算器，老师带你算一算真实世界里的惨剧！

我们以 **LLaMA-3 8B** 级别模型为例：
- 头数 $H = 32$
- 单头维度 $d = 128$
- 批大小 $B = 1$
- 数据类型：FP16（2 字节）

我们来看看当序列长度 $S$ 分别为 **2048 (2K)** 和 **32768 (32K)** 时，单层注意力中间矩阵的显存变化：

### 1. 当文本长度 $S = 2,048$ 时：
- 单份中间矩阵大小：
  $$M = 32 \times 2048 \times 2048 \times 2 \text{ 字节} \approx 268.4 \text{ MB}$$
- 往返 3 次物化读写的总显存搬运量：
  $$\text{Bytes} \approx 3 \times 2 \times 268.4 \text{ MB} \approx 1.61 \text{ GB}$$
  此时显卡还能勉强承受。

### 2. 当文本长度扩展到 $S = 32,768$（32K 长文本）时：
- 单份中间矩阵大小直接飙升：
  $$M = 32 \times 32768 \times 32768 \times 2 \text{ 字节} = 68,719,476,736 \text{ 字节} \approx 68.7 \text{ GB}!$$
- **老师请你看清楚这个数字：单层、单次前向，光是这一个中间草稿矩阵就需要 68.7 GB！**
- 一块昂贵的 A100/H100 显卡总共才 80 GB 显存！全模型有 32 个层，只要跑到了第 1 层的前半截，显存就会当场被撑爆，瞬间报出：
  ```
  torch.cuda.OutOfMemoryError: CUDA out of memory. Tried to allocate 68.72 GiB
  ```
- **更可怕的是显存传输量：**  
  往返搬运 6 次，需要搬运：
  $$\text{Bytes} = 3 \times 2 \times 68.7 \text{ GB} \approx 412 \text{ GB}$$
  在 H100（$3.35\text{ TB/s}$）上，光是在显存总线里跑腿就要浪费整整 **123 毫秒**！

---

## 步骤 6：核心系统工程铁律

> **永远不要在全局显存中物化完整的注意力大矩阵。**  
> $O(S^2)$ 的中间张量是大模型走向长文本的最大拦路虎。必须想办法把注意力计算限制在片上高速 SRAM 中分块化解，绝不让这块巨大的草稿纸落入全局显存。
"""

chapters["module-1/04-flash-attention.zh.md"] = r"""# 第 E04 章：FlashAttention (1, 2, 3) 演进：SRAM 平铺分块、在线 Softmax 与 TMA 异步传输

同学，你好！在上一节课中，我们见识了朴素注意力那张可怕的“$O(S^2)$ 中间草稿纸”。

你当时一定会想：“老师，既然芯片片上的 SRAM 备餐台放不下整张大表格，但最终算出来的输出结果 $\mathbf{O}$ 其实只有小小的 $S \times d$（线性大小），**难道我们非得把中间那张巨大无比的草稿纸完整画出来吗？** 我们能不能切成一小块一小块，在心里算完，直接把最终答案写下来，草稿纸当场扔掉呢？”

恭喜你！你的直觉和斯坦福大学的天才博士生 Tri Dao 想得一模一样！  
这就是名震天下、彻底改写了现代大模型架构底层格局的**闪电注意力——FlashAttention**！

今天这节课，老师就带你彻底吃透 FlashAttention 1、2、3 的进化神作。

---

## 步骤 1：物理直觉（在心算中完成账目汇总）

为了让你彻底明白 FlashAttention 的巧妙，老师给你打个比方：

假设你是一位审计员，要核算一本厚达 10,000 页的账册：
- **朴素做法（记大草稿）：**  
  你把 10,000 页全部撕下来，平铺在整间大礼堂的地板上，画出一张长宽各 10,000 格的巨型交叉对比表。由于表格太大，大礼堂被塞得连下脚的地方都没有，你累得在各个角落来回跑腿。
- **FlashAttention 做法（分块平铺 + 在线心算更新）：**  
  你老老实实坐在小书桌前（片上 SRAM），手里只拿一叠便利贴：
  1. 你每次只从书架上取出一小叠账目（比如 64 条 Query）和另一小叠凭证（比如 64 条 Key/Value）；
  2. 在小便利贴上当场算完这一小块局部的相乘；
  3. **最神奇的地方来了：** 算局部 Softmax 时，虽然你还没看到后面的凭证，但你可以在便利贴上先记下当前的最大值和局部和。等下一批凭证送来时，利用巧妙的数学公式，把之前的中间结果轻轻乘上一个修正比例因子，当场融合进去！
  4. 算完之后，便利贴随手揉成一团扔进垃圾桶，**桌上永远只保留最终的累计结果**！

你看，**大礼堂的地板被彻底腾空了！草稿纸永远在书桌上即生即灭，连一页都没写进远端冷库里！**

---

## 步骤 2：芯片微观底层执行机制

FlashAttention 的核心技术支柱有两个：**SRAM 平铺分块（Tiling）** 与 **在线 Softmax 算法（Online Softmax）**。

### 1. 在线 Softmax 的数学魔术
在标准 Softmax 中，为了防止指数溢出，我们必须找到全行的全局最大值 $m$：

$$m = \max(x_1, \dots, x_N), \quad P_i = \frac{e^{x_i - m}}{\sum_j e^{x_j - m}}$$

朴素算法要求必须遍历完所有的 $N$ 个数字才能得到 $m$。但 Milakov & Gimelshtein (2018) 证明了：**最大值和分母是可以边走边动态修正的！**

假设我们先把前一半算出来，局部最大值为 $m^{(1)}$，局部累加和为 $\ell^{(1)}$；  
接着送来了后一半，其局部最大值为 $m^{(2)}$，局部累加和为 $\ell^{(2)}$。  
新的全局最大值显然就是：

$$m^{\text{new}} = \max(m^{(1)}, m^{(2)})$$

那么之前基于旧最大值算出来的局部累加和，只需要补乘一个修正因子即可完美复原：

$$\ell^{\text{new}} = \ell^{(1)} \cdot e^{m^{(1)} - m^{\text{new}}} + \ell^{(2)} \cdot e^{m^{(2)} - m^{\text{new}}}$$

同理，之前累加好的输出向量 $\mathbf{O}$ 也可以用一模一样的缩放因子无缝修正！**根本不需要回头重算！**

```
【FlashAttention 片上执行微观数据流】
全局显存 HBM (80 GB)
    │
    ├── 1. 搬运一小块 Q 瓦片 (如 64 x 128) ──► 片上高速 SRAM (单 SM 独占 228 KB)
    │                                              │
    ├── 2. 循环流式加载 K, V 瓦片 (如 64 x 128) ──►│ ◄── 在 SRAM 内部完成:
    │                                              │     - S_tile = Q_tile @ K_tile.T
    │                                              │     - 就地计算在线 Softmax 修正因子
    │                                              │     - O_tile = 修正旧值 + P_tile @ V_tile
    │                                              │     - (S_tile 与 P_tile 算完立刻丢弃!)
    ▼                                              │
最终输出 O 写回 HBM ◄──────────────────────────────┘ (HBM 往返次数彻底消除 S^2 项!)
```

### 2. FlashAttention 1 到 2 再到 3 的演进史：
- **FlashAttention-1 (2022)：** 确立了 Tiling 和 Online Softmax 范式，反向传播不存概率矩阵，求导时利用保存的行最大值和求和在片上瞬间重算，显存降低 $O(S)$，提速 2-4 倍。
- **FlashAttention-2 (2023)：** 调整循环次序，将外层循环从 Key/Value 改为 Query，消灭了不同线程块原子加法写冲突；在片上全面采用 FP16 累加优化，提速再翻一倍，逼近硬件算力上限的 70%。
- **FlashAttention-3 (2024, 面向 Hopper 架构)：** 利用 Hopper 芯片的硬件异步搬运引擎（TMA）与异步张量核心指令（WGMMA），让数据在流水搬运的同时，张量核心在后台不停歇计算，实现访存与计算的**完全物理重叠**！

---

## 步骤 3：跨组件相互耦合机制

1. **彻底解放长上下文（Long Context）：**  
   显存复杂度从 $O(S^2)$ 骤降为 $O(S)$。大模型上下文窗口从过去的 2K/4K 瞬间跃升至 32K、128K 乃至 100 万长文本，彻底催生了现代长文档阅读、大代码库理解的 AI 革命。
2. **反向传播激活值立减 90%：**  
   由于不需要在显存中保留 $S \times S$ 的 Softmax 概率矩阵，模型训练时节省出了数十吉字节的宝贵显存，允许模型在单卡上采用更大的批大小（Batch Size），大幅加速预训练收敛。

---

## 步骤 4：精确性能数学公式

FlashAttention 的浮点运算量与朴素实现基本相当（甚至在反向传播重算时多做了微小的代数乘加），但在全局显存 HBM 上的读写开销发生了质的飞跃：

设片上 SRAM 的有效分块瓦片大小为 $M$（以能够容纳的元素数度量）：

朴素注意力的 HBM 访存量：
$$\text{Bytes}_{\text{naive}} = \Theta(B \cdot H \cdot S \cdot d + B \cdot H \cdot S^2)$$

FlashAttention 的 HBM 访存量：
$$\text{Bytes}_{\text{Flash}} = \Theta\left(B \cdot H \cdot S \cdot d + \frac{B \cdot H \cdot S^2 \cdot d^2}{M}\right) \approx O(S)$$

**随着 $S$ 增大，FlashAttention 的 HBM 显存访问量减少了整整 $\frac{S}{M}$ 倍（通常为一个数量级以上）！**

---

## 步骤 5：具体微基准数字推导

我们拿出秒表和量尺，在真实的 **NVIDIA A100 (SXM 80GB)** 显卡上对比实测数据！

设置参数：$B = 1, H = 32, d = 128$，序列长度 $S = 4096$：

### 1. 朴素注意力的实测耗时剖析
- 运算量：约 $2 \times 1 \times 32 \times 4096^2 \times 128 \times 2 \approx 2.75 \times 10^{11} \text{ FLOPs} = 275 \text{ GFLOPs}$
- HBM 往返搬运数据量：
  $$\text{Bytes} \approx 6 \times 1 \times 32 \times 4096^2 \approx 3.22 \times 10^9 \text{ 字节} \approx 3.22 \text{ GB}$$
- 在 A100（带宽 $2.0 \text{ TB/s}$）上的访存耗时：
  $$T_{\text{mem}} = \frac{3.22 \times 10^9}{2.0 \times 10^{12}} \approx 1.61 \text{ 毫秒}$$
- 算子调度与启动气泡：约 $0.3 \text{ ms}$
- 实测端到端耗时：**$2.15\text{ 毫秒}$**（大部分时间都在等显存传输，算力利用率仅约 $15\%$）。

### 2. FlashAttention-2 的实测表现
- HBM 往返搬运数据量：**直接锐减至仅读取 Q, K, V 和写出 O！**
  $$\text{Bytes} = 4 \times (1 \times 32 \times 4096 \times 128 \times 2) \approx 134 \text{ MB} = 0.134 \text{ GB}$$
  显存搬运量整整缩小了：
  $$\frac{3.22 \text{ GB}}{0.134 \text{ GB}} \approx 24 \text{ 倍！}$$
- 纯访存耗时：
  $$T_{\text{mem}} = \frac{0.134 \times 10^9}{2.0 \times 10^{12}} \approx 0.067 \text{ 毫秒} = 67 \;\mu\text{s}$$
- 在 Tensor Core 满负荷流水冲压下，实测端到端耗时：**仅需 $0.48\text{ 毫秒}$**！
- **综合提速：** $2.15 \text{ ms} \div 0.48 \text{ ms} = 4.48 \times$！  
  不仅速度快了近 4.5 倍，显存占用还直接抹掉了一个数量级！

---

## 步骤 6：核心系统工程铁律

> **算法工程的最高境界不是减少数学乘除法，而是让数据流动顺应物理法则。**  
> FlashAttention 的奇迹证明：宁可在片上多做几次心算重算，也绝不要贪图省事把中间半成品写回远端全局显存。
"""

chapters["module-1/05-kv-cache-and-paged-attention.zh.md"] = r"""# 第 E05 章：KV Cache 显存解剖与 PagedAttention：虚拟内存分页与动态显存碎片治理

同学，你好！如果你问一位大模型部署工程师：“在日常服务上线中，最让你睡不着觉的梦魇是什么？”

十个工程师里有九个会脱口而出：**显存碎片导致的突发 OOM 崩溃！**

你可能很纳闷：“老师，用户的输入长度我们明明可以限制，显存大小也是固定的，为什么并发量一上去，显卡明明还有好几十个 G 的空余显存，系统却突然‘啪’的一声报出内存不足挂掉了呢？”

今天这堂课，老师就带你彻底解剖大模型自回归生成的命脉——**KV Cache（键值缓存）**，并带你看看 vLLM 是如何借用操作系统的“虚拟内存分页”绝招，彻底驯服这头显存吞金巨兽的！

---

## 步骤 1：物理直觉（图书馆霸占一整排座位）

为了让你瞬间理解为什么显存会不够用，老师带你去大学图书馆看一场“占座风波”：

### 传统的显存分配方式（静态预分配）：
假设学校图书馆有一间自习室，里面有一排排连在一起的长桌：
- 来了第一个同学，他说：“老师，我待会儿可能会写一篇长达 2000 字的论文，但我现在才刚刚开始写标题。”
- 图书馆管理员（传统推理引擎）一听，二话不说，**立刻把整整长达 2000 个连在一起的座位全部用封条封上**，只准这位同学一个人坐！
- 哪怕这个同学现在只写了 5 个字、只坐了 1 个座位，其余 1999 个空座位别人也绝对不准碰！
- 这时，外面排队进来了 10 个新同学，自习室里明明放眼望去全是空椅子，但管理员却哭丧着脸说：“对不起，满座了，没有连续的 2000 个空位给你们了，你们去别处吧！”

这就是传统大模型显存分配的荒唐现实——**显存碎片化与过度预留**！实测表明，传统系统中有超过 **60% 到 80% 的显存完全是在为‘可能发生的未来’白白空置**！

### PagedAttention 的巧妙解法（活页笔记本与分页管理）：
既然大家写字都是一个字一个字往后蹦，为什么非要连在一起呢？
- 图书馆给每个同学发一本**活页笔记本**；
- 每当你写满了一张小活页纸（比如放 16 个 Token），管理员就从书架任意角落随便找一个闲置的小抽屉（物理块）给你装这张纸；
- 哪怕这个同学的第 1 页在教室东头、第 2 页在教室西头、第 3 页在地下室，都没关系！管理员手里有一张**“目录索引表（页表）”**，把这些散落的纸按顺序串起来；
- 谁需要空间就给谁一小张，谁写完了就当场撕下来回收！

**自习室的空座率瞬间从 70% 砸到了 0%！原本只能坐 10 个人的房间，现在轻轻松松坐下了 50 个人！**

---

## 步骤 2：芯片微观底层执行机制

在自回归生成（Decode）阶段，大模型每输出一个新 Token，都需要计算该 Token 的 Query 与历史上所有 Token 的 Key 和 Value 的注意力。

为了避免每吐出一个新字就把上文成千上万个字从头重新算一遍，系统必须在显存中把历史的 $\mathbf{K}$ 和 $\mathbf{V}$ 张量永久缓存起来，这就是 **KV Cache**。

```
传统连续显存分配 (Naive Sequential Cache):
请求 A (预留 2048): [已用 50 个][空闲预留 1998 个 (占着茅坑不拉屎)]
请求 B (预留 2048): [已用 120 个][空闲预留 1928 个 (内存黑洞)]
==> 显存被大块静态切分，外部碎片与内部碎片交织，极易虚假 OOM！

PagedAttention 分页管理 (Block-based Paged Cache):
全局统一的物理块池 (Physical Block Pool, 每个块固定装 16 个 Token):
[物理块 0]  [物理块 1]  [物理块 2]  [物理块 3]  [物理块 4]  [物理块 5] ...

请求 A 的逻辑页表 (Logical Block Table):
  逻辑块 0 ──► 物理块 3 (装第 0~15 个 Token 的 KV)
  逻辑块 1 ──► 物理块 7 (装第 16~31 个 Token 的 KV)
  逻辑块 2 ──► 物理块 1 (当前正在写入第 32~47 个 Token)

请求 B 的逻辑页表:
  逻辑块 0 ──► 物理块 0
  逻辑块 1 ──► 物理块 4
```

### PagedAttention 的内核执行魔法：
在底层的 CUDA 核函数中，线程读取 Key 和 Value 时，不再是简单地根据连续指针偏移量 `ptr + i * stride` 取数据，而是先在共享内存中查阅当前请求的**块映射表（Block Table）**：
1. 计算当前目标 Token 落在第几个逻辑块：$\text{block\_idx} = \lfloor \text{token\_idx} / 16 \rfloor$；
2. 查表得到真实的物理块基地址；
3. 计算物理块内的偏移量：$\text{offset} = \text{token\_idx} \pmod{16}$；
4. 连续取数并送入 Tensor Core 算子。

整个查表过程被完全隐藏在片上共享内存的极低延迟中，对外的计算吞吐**几乎零损耗**！

---

## 步骤 3：跨组件相互耦合机制

1. **并发批大小（Serving Concurrency）迎来暴增：**  
   消除了显存碎片和静态预留后，原本只能跑 8 个并发的单卡，现在可以稳稳当当塞进 32 到 64 个并发请求，吞吐量（Tokens/s）直接翻了 **3 到 5 倍**！
2. **零开销共享前缀（Copy-on-Write 树状前缀缓存）：**  
   当多个用户问一模一样的问题（例如一段长系统提示词），所有用户可以**共同指向同一组物理 KV 块**！只有当某个用户开始生成自己独有的不同回答时，系统才动态复制分裂出一个新块。这直接让系统提示词的显存开销降为了零！

---

## 步骤 4：精确性能数学公式

老师带你推导大模型工程师必须倒背如流的 **KV Cache 显存容量公式**。

对于一个大模型：
- 模型层数：$L$（Layers）
- 单头特征维度：$d_k$（Head Dimension）
- KV 注意力头数：$H_{\text{kv}}$（在 MHA 下等于总头数，在 GQA 下远小于总头数）
- 数据精度：$\text{sizeof(dtype)}$（FP16 下为 2 字节，FP8 下为 1 字节）

单个 Token 在单层中需要保存一份 $\mathbf{K}$ 向量和一份 $\mathbf{V}$ 向量：

$$\text{单 Token 单层 KV 大小} = 2 \times H_{\text{kv}} \times d_k \times \text{sizeof(dtype)} \text{ 字节}$$

全模型 $L$ 层，对于一个包含 $S$ 个历史 Token 的请求，其累积占用的 KV Cache 显存绝对值为：

$$\text{KV Cache 容量} = 2 \times L \times H_{\text{kv}} \times d_k \times S \times \text{sizeof(dtype)} \text{ 字节}$$

---

## 步骤 5：具体微基准数字推导

拿出计算器，跟老师一起来算一算当今最主流的开源王者 **LLaMA-3 70B**！

我们查一查 LLaMA-3 70B 的官方架构参数：
- 层数 $L = 80$
- 总 Query 头数 $H_q = 64$，但它采用了 **GQA 分组查询注意力**，KV 头数只有 $H_{\text{kv}} = 8$！
- 单头维度 $d_k = 128$
- 数据精度：FP16（每元素 2 字节）

### 1. 计算单用户单 Token 的 KV Cache 显存
$$\text{每 Token 显存} = 2 \times 80 \times 8 \times 128 \times 2 = 327,680 \text{ 字节} \approx 320 \text{ KB}$$

你瞧，**用户每说一句话、模型每吐一个字，显存里就多出 320 KB 的永久账单！**

### 2. 计算单用户在 8192（8K）长上下文下的显存占用
$$\text{单请求 KV 显存} = 8192 \times 320 \text{ KB} \approx 2.62 \times 10^6 \text{ KB} \approx 2.62 \text{ GB}$$

### 3. 计算 32 个并发用户同时在线时的显存总账
如果系统同时服务 32 个并发长对话：
$$\text{并发 KV 显存总和} = 32 \times 2.62 \text{ GB} \approx 83.8 \text{ GB}$$

**老师带你分析结果：**  
整整 83.8 GB！这已经超过了一整块 H100 显卡的全部物理容量！  
- 如果采用传统静态分配，由于必须按最大长度为每个请求预留 8K 空间，系统在并发达到 10 的时候就会因为碎片化提前 OOM 暴毙！
- 而采用 **PagedAttention** 后，显存按需分配，实际内存浪费率降至 **4% 以下**，32 个并发请求可以在多卡集群上丝滑流动。

---

## 步骤 6：核心系统工程铁律

> **动态分页是驯服自回归显存碎片的唯一解。**  
> 绝不静态预留连续显存。通过类似操作系统的逻辑页表与物理块池将 KV Cache 切片化、按需化、共享化，是大模型在线推理服务吞吐翻倍的立足之本。
"""

chapters["module-1/06-mha-mqa-gqa-mla.zh.md"] = r"""# 第 E06 章：注意力架构变体深度权衡：MHA、MQA、GQA 与 MLA 的硬件开销与显存对决

同学，你好！在上一节课里，我们算出了一个惊人的数字：单个长对话的 KV Cache 动辄吃掉几个吉字节的显存。

你可能会好奇：“老师，难道为了自回归生成，我们就只能眼睁睁看着显存被 KV 缓存一点点吞噬吗？**我们在模型架构的源头上，能不能想点办法给 KV 向量减减肥呢？**”

问得太好了！过去的五年里，全球顶尖的模型架构师们（Google、Meta、DeepSeek）在这个问题上展开了极其激烈的探索，接连发明了：
- **MHA（Multi-Head Attention，多头注意力）**
- **MQA（Multi-Query Attention，多查询注意力）**
- **GQA（Grouped-Query Attention，分组查询注意力）**
- **MLA（Multi-Head Latent Attention，多头潜在注意力）**

今天这节课，老师就带你站在硬件工程的角度，看看这些缩写背后的**显存与精度的大博弈**！

---

## 步骤 1：物理直觉（公司里的总监与秘书配比）

为了让你秒懂它们之间的区别，老师给你打个职场团队分工的比方：

在注意力机制里，每个 **Query（Q 头）**就像一位负责思考决策的**业务总监**；而 **Key 和 Value（KV 头）**就像给总监提供背景资料的**助理秘书**。

1. **MHA（经典多头注意力）：一对一土豪配置**  
   公司里有 64 位总监（64 个 Q 头），土豪老板给每一位总监都配了一位专属的私人秘书（64 个 K/V 头）。  
   - 优点：每个总监都有专人伺候，看问题的角度完全独立，模型表达能力最强。  
   - 缺点：64 位秘书的工资和工位开销（KV Cache 显存）大得惊人，公司很快就要被吃垮了！
2. **MQA（多查询注意力）：极致抠门的共享秘书**  
   老板为了省钱，把 63 位秘书全给开除了！全公司 64 位总监，**公用仅仅 1 位秘书（1 个 K/V 头）**。  
   - 优点：显存开销直接缩减到原来的 $\frac{1}{64}$，省钱到了极点！  
   - 缺点：所有总监只能看同一份资料，模型的复杂推理能力明显受损，很容易变笨。
3. **GQA（分组查询注意力）：优雅的中庸之道（现代大模型标配）**  
   老板灵机一动：我们折中一下！把 64 位总监分成 8 个小组，每 8 个总监共享 1 位小组秘书（8 个 K/V 头）。  
   - 奇迹出现了：**显存直接砍掉 87.5%（变成原来的 $\frac{1}{8}$），而模型的智力水准几乎毫发无损！**（现在的 LLaMA-2/3、Mistral、Qwen 全面采用这一架构）。
4. **MLA（DeepSeek 的神来之笔：压缩档案袋）**  
   DeepSeek 的工程师更绝：他们不仅分组，还给秘书发了一个**高维无损压缩机**！每次存进显存时，把庞大的资料压缩成一个极小的潜在特征向量（Latent Vector），等总监要看的时候再当场解压！显存再次狂降数倍！

---

## 步骤 2：芯片微观底层执行机制

我们把目光切回显卡芯片内部：

```
MHA (经典多头注意力):
Q Heads:   [Q0] [Q1] [Q2] [Q3] ... [Q63]  (64 个)
            │    │    │    │        │
KV Heads:  [K0] [K1] [K2] [K3] ... [K63]  (64 个, KV 显存开销 100%)

--------------------------------------------------------------------------------

MQA (多查询注意力):
Q Heads:   [Q0] [Q1] [Q2] [Q3] ... [Q63]  (64 个)
             \    \    │   /        /
KV Heads:            [ K0 ]                (仅 1 个, KV 显存立减至 1/64 = 1.5%)

--------------------------------------------------------------------------------

GQA (分组查询注意力, 如 8 组):
Q Heads:   [Q0..Q7]   [Q8..Q15]   ...   [Q56..Q63]  (64 个 Q 头分成 8 组)
              │           │                  │
KV Heads:    [KV0]       [KV1]    ...       [KV7]       (仅 8 个, KV 显存压缩至 1/8 = 12.5%)

--------------------------------------------------------------------------------

MLA (DeepSeek 多头潜在注意力):
不直接缓存庞大的 K, V 向量!
利用低秩矩阵将其投影并压缩为一个紧凑的低维潜在向量 c_t [维度仅 512]
读取时通过矩阵乘即时解压恢复，同时结合解耦的 RoPE 位置向量!
==> KV Cache 显存占用进一步狂降至 MHA 的 5% 以下!
```

---

## 步骤 3：跨组件相互耦合机制

架构的改变对硬件推理执行带来了惊天动地的质变：

1. **从访存受限（Memory-Bound）向计算受限（Compute-Bound）大幅跃迁：**  
   在自回归解码中，由于每次只需要从显存搬运 $\frac{1}{8}$（GQA）乃至更少（MLA）的 KV 数据，每个字节对应的有效矩阵计算量大幅上升，**算术强度提升了数倍**！显存带宽总线不再严重堵塞，单步生成延迟（ITL）显著降低。
2. **多卡张量并行（Tensor Parallelism）的约束：**  
   在进行模型切分时，KV 头数必须能被并行卡数整除。如果模型采用了 8 个 KV 头的 GQA，那么在进行张量并行切分时，最大卡数就被自然限制在了 8 张卡以内，否则一个 KV 头就无法再均分给多个芯片了！

---

## 步骤 4：精确性能数学公式

单步解码中，不同架构对于单序列的历史 KV Cache 搬运量公式对比（设序列长为 $S$，层数为 $L$，单头维度 $d$）：

$$\text{Bytes}_{\text{MHA}} = 2 \times L \times S \times (H_q \times d) \times 2$$

$$\text{Bytes}_{\text{GQA}} = 2 \times L \times S \times (H_{\text{kv}} \times d) \times 2 = \text{Bytes}_{\text{MHA}} \times \frac{H_{\text{kv}}}{H_q}$$

$$\text{Bytes}_{\text{MQA}} = 2 \times L \times S \times (1 \times d) \times 2 = \text{Bytes}_{\text{MHA}} \times \frac{1}{H_q}$$

$$\text{Bytes}_{\text{MLA}} = L \times S \times (d_c + d_R) \times 2 \quad (\text{其中 } d_c \text{ 为压缩潜在维度, } d_R \text{ 为解耦位置维度})$$

---

## 步骤 5：具体微基准数字推导

我们以 **70B 参数级大模型**（80 层，$d=128, H_q = 64$），在处理上下文长度 $S = 8192$（8K 文本）时，手算对比四种架构的单用户 KV Cache 显存大小：

### 1. 经典 MHA（$H_{\text{kv}} = 64$）
$$\text{Memory} = 2 \times 80 \times 8192 \times (64 \times 128) \times 2 \text{ 字节} \approx 2.15 \times 10^{10} \text{ 字节} \approx \mathbf{21.47 \text{ GB}}$$
单用户就要吃掉 21.5 GB！一张 80GB 的卡装下模型权重后，连 1 个完整对话都跑不顺畅！

### 2. 现代主流 GQA（$H_{\text{kv}} = 8$，如 LLaMA-3 70B）
$$\text{Memory} = 21.47 \text{ GB} \times \frac{8}{64} = \mathbf{2.68 \text{ GB}}$$
**直接节省了整整 18.8 GB 显存！** 并发容量瞬间提升了 **8 倍**！

### 3. 极致压缩 MQA（$H_{\text{kv}} = 1$）
$$\text{Memory} = 21.47 \text{ GB} \times \frac{1}{64} = \mathbf{0.335 \text{ GB}} \approx 343 \text{ MB}$$

### 4. DeepSeek MLA（$d_c = 512, d_R = 64$）
$$\text{Memory} = 80 \times 8192 \times (512 + 64) \times 2 \approx 7.55 \times 10^8 \text{ 字节} \approx \mathbf{0.755 \text{ GB}}$$
在保持完整多头表征能力的同时，显存只有经典 MHA 的 **3.5%**！

---

## 步骤 6：核心系统工程铁律

> **模型架构师手中的每一支笔，都直接在芯片硅片上刻下显存烙印。**  
> 在大模型时代，孤立地追求注意力表征自由度是昂贵的奢靡。通过 GQA 或 MLA 主动压缩 KV 状态维度，是打破显存带宽枷锁、实现千人在线并发的最强杠杆。
"""

written = 0
for rel_path, content in chapters.items():
    full_path = os.path.join(BASE_DIR, rel_path)
    os.makedirs(os.path.dirname(full_path), exist_ok=True)
    with open(full_path, "w", encoding="utf-8") as f:
        f.write(content.strip() + "\n")
    written += 1

print(f"Successfully refined {written} chapters for Module 0 and Module 1 with teacher voice.")
