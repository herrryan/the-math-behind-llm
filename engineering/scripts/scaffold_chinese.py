import os

BASE_DIR = os.path.abspath(os.path.join(os.path.dirname(__file__), "..", "docs"))

chinese_files = {}

# Module 1
chinese_files["module-1/03-naive-attention-memory-wall.zh.md"] = r"""# 第 E03 章：朴素注意力的致命缺陷：$O(S^2)$ 显存带宽墙与中间矩阵物化灾难

## 步骤 1：物理直觉

想象你在一个狭小的厨房里制作千层面，台面上放不下大托盘。按照传统菜谱要求：
1. 你必须先把整叠 4000 张面皮平铺在远在地下二层的冷库地面上（生成完整的 $S \times S$ 注意力矩阵 $\mathbf{S}$ 并写入 HBM）；
2. 走回厨房洗手，然后再下楼一趟，把每一张面皮都涂上一层特制酱汁并算好分量（计算 Softmax $\mathbf{P} = \text{softmax}(\mathbf{S})$，再次读写整张大矩阵）；
3. 最后再下楼一趟，把酱汁面皮卷起来端上楼（计算 $\mathbf{O} = \mathbf{P}\mathbf{V}$ 并写回输出）。

在这个过程中，你的大部分时间和体力不是在做菜，而是在走廊和楼梯上反复跑腿搬运这个巨大的半成品托盘。如果面皮数量从 4000 张增加到 32000 张，托盘的面积将扩大 64 倍！地下冷库会瞬间被塞满（显存溢出 OOM），整个厨房的运转彻底陷入瘫痪。

---

## 步骤 2：芯片微观底层执行机制

在标准注意力机制中，数学公式为：

$$
\mathbf{O} = \text{softmax}\left(\frac{\mathbf{Q}\mathbf{K}^\top}{\sqrt{d_k}}\right)\mathbf{V}
$$

在没有算子熔合的朴素实现中，PyTorch 或深度学习框架会将该计算分解为多个独立的 CUDA 算子调用：

```
全局显存 (HBM)                           片上共享内存 (SRAM / 寄存器)
   │                                                 │
   ├── 1. 读取 Q, K ───────────────────────────────► │ 矩阵乘 GEMM-1: Q @ K.T
   │                                                 │ 生成注意力分数中间矩阵
   │ ◄── 2. 将大小为 S x S 的 S 矩阵写入 HBM ────────┤
   │                                                 │
   ├── 3. 重新从 HBM 读取整张 S 矩阵 ──────────────► │ 执行 Softmax(S):
   │                                                 │ 求最大值、减最大值、求指数、累加求和
   │ ◄── 4. 将大小为 S x S 的 P 矩阵写回 HBM ────────┤
   │                                                 │
   ├── 5. 从 HBM 重新读取整张 P 矩阵与 V 矩阵 ─────► │ 矩阵乘 GEMM-2: P @ V
   │                                                 │
   │ ◄── 6. 将最终注意力输出 O 写回 HBM ─────────────┤
```

### 核心微观瓶颈：
- 中间矩阵 $\mathbf{S}$ 和 $\mathbf{P}$ 的尺寸均为 $(B \times H, S, S)$。对于序列长度 $S = 8192$、头数 $H = 32$、批大小 $B = 1$ 的模型，仅单层注意力的单份中间矩阵在 FP16 下就需要消耗：
  $$1 \times 32 \times 8192 \times 8192 \times 2 \text{ 字节} = 4.29 \text{ GB}$$
- 一次前向传播中，仅为了这一个中间矩阵，GPU 就必须向 HBM 写入 $4.29\text{ GB}$，再读出 $4.29\text{ GB}$，再写回 $4.29\text{ GB}$，再读出 $4.29\text{ GB}$，总计产生超 $17\text{ GB}$ 的纯往返数据搬运！

---

## 步骤 3：跨组件相互耦合机制

1. **对最大上下文长度（Context Length）的绝对封锁：**  
   显存占用随序列长度呈 $O(S^2)$ 爆炸式增长。当序列长度从 4K 扩展到 64K 时，中间矩阵的显存暴涨 $256$ 倍。如果没有消除中间矩阵物化的算法，哪怕是 $80\text{ GB}$ 的旗舰显卡，也会在处理单条请求时瞬间 OOM。
2. **对反向传播激活值显存的支配：**  
   在模型训练中，前向传播生成的 Softmax 概率矩阵 $\mathbf{P} \in \mathbb{R}^{B \times H \times S \times S}$ 必须被原样保存在 HBM 中，以备反向传播计算梯度时使用，这直接占据了训练激活值显存的 $70\%$ 以上。
3. **极低的模型浮点利用率（MFU）：**  
   Softmax 算子自身的算术强度仅为 $I \approx 2\text{ FLOPs/Byte}$，在 H100 等芯片上只能发挥不足 $1\%$ 的 Tensor Core 算力。

---

## 步骤 4：精确性能数学公式

朴素注意力机制在 HBM 上的总显存往返读写流量为：

$$
M_{\text{naive}} = \underbrace{4 B H S d}_{\mathbf{Q}, \mathbf{K}, \mathbf{V}, \mathbf{O} \text{ 读写}} + \underbrace{4 B H S^2}_{\mathbf{S}, \mathbf{P} \text{ 中间矩阵写与读}} \quad (\text{以元素计})
$$

在 FP16 精度下（每个元素 2 字节），总流量为：

$$
\text{Bytes}_{\text{naive}} = 8 B H S d + 8 B H S^2 \text{ 字节}
$$

而注意力的总浮点运算量为：

$$
\text{FLOPs} = 2 \times (2 B H S^2 d) = 4 B H S^2 d \text{ FLOPs}
$$

当序列长度 $S \gg d$ 时，算术强度退化为：

$$
I_{\text{naive}} = \frac{4 B H S^2 d}{8 B H S d + 8 B H S^2} \approx \frac{4 B H S^2 d}{8 B H S^2} = \frac{d}{2} \text{ FLOPs/Byte}
$$

如果单头维度 $d = 128$，则无论序列多么长，朴素注意力的算术强度都被锁死在：

$$
I \le 64 \text{ FLOPs/Byte}
$$

在硬件平衡拐点 $I^* \approx 298.5$ 的 H100 SXM 上，朴素注意力算子**天生处于深度访存受限状态**，其理论 MFU 上限无法突破：

$$
\text{MFU}_{\text{max}} = \frac{64}{298.5} \approx 21.4\%
$$

---

## 步骤 5：具体微基准数字推导

以 LLaMA-3 8B 模型（$H = 32, d = 128$），在序列长度 $S = 8192$、批大小 $B = 1$ 条件下计算单层注意力的性能：

1. **总运算量：**
   $$F = 4 \times 1 \times 32 \times 8192^2 \times 128 \approx 1.10 \times 10^{12} \text{ FLOPs} = 1.10 \text{ TFLOPs}$$
2. **纯计算耗时（在 H100 1000 TFLOPS 下）：**
   $$T_{\text{compute}} = \frac{1.10 \times 10^{12}}{1.0 \times 10^{15}} = 0.0011 \text{ 秒} = 1.1 \text{ 毫秒}$$
3. **中间矩阵 HBM 搬运量：**
   $$\text{Bytes} = 8 \times 1 \times 32 \times 8192^2 = 1.718 \times 10^{10} \text{ 字节} \approx 17.18 \text{ GB}$$
4. **访存传输耗时（在 H100 3.35 TB/s 下）：**
   $$T_{\text{memory}} = \frac{17.18 \times 10^9}{3.35 \times 10^{12}} = 0.0051 \text{ 秒} = 5.1 \text{ 毫秒}$$
5. **实际执行总耗时：**
   $$T_{\text{exec}} \ge 5.1 \text{ 毫秒}$$

显存搬运耗时几乎是纯计算耗时的 **$4.6$ 倍**！高达 $80\%$ 的硬件时间被浪费在将中间注意力分数写出到 HBM 并重新读回的机械搬运上。

---

## 步骤 6：核心系统工程铁律

> 永远不要将长序列注意力的中间分数矩阵物化写入全局显存（HBM）。在现代大模型系统中，谁在 HBM 中分配了 $S \times S$ 的中间显存，谁就在物理层面上注定了系统的 OOM 崩溃与极低吞吐。
"""

chinese_files["module-1/04-flash-attention.zh.md"] = r"""# 第 E04 章：FlashAttention (1, 2, 3) 演进：SRAM 平铺分块、在线 Softmax 与 TMA 异步传输

## 步骤 1：物理直觉

在第 E03 章中，我们看到每次做千层面都要跑到远端地下冷库搬运巨大托盘。现在，一位聪明的工程大师改写了工作流：
- 他拿来一个刚好能放在身旁备餐台（片上 SRAM）上的**小保鲜盒（Tile 分块）**；
- 他一次只从冷库取出一小叠面皮（一个 Block），在备餐台上当场完成涂抹酱汁与局部加权累加；
- 他发明了一种数学技巧（**在线 Softmax 动态更新**）：当下一盒新面皮端上来时，如果发现新面皮的温度更高，他不需要倒掉重做，而是用一个缩放系数瞬间校正碗里已经做好的半成品，把新的数据融合进来；
- 整个制作过程一气呵成，巨大的中间矩阵彻底不需要存在，所有计算都在小保鲜盒与备餐台上搞定！

这就是 **FlashAttention** 的本质：将注意力机制的显存读写复杂度，直接从 $O(S^2)$ 砍到与序列长度呈严格线性的 $O(S)$！

---

## 步骤 2：芯片微观底层执行机制

FlashAttention 的核心突破由三大物理机制协同支撑：

### 1. 瓦片分块（Tiling）与 SRAM 驻留
将 $\mathbf{Q}, \mathbf{K}, \mathbf{V}$ 矩阵在序列维度切分成较小的块（如 $B_r \times d$ 和 $B_c \times d$，典型值为 $64 \times 128$ 或 $128 \times 128$），完全适配单个 SM 的 $228\text{ KB}$ SRAM 大小。

```
输入矩阵 Q, K, V (位于高带宽显存 HBM)
        │
        ├── 每次仅加载一个 Tile 分块至片上共享内存 (SRAM)
        ▼
┌────────────────────────────────────────────────────────┐
│                   单个 SM 的 SRAM 内部                 │
│  [分块 Q_i: 64x128]  @  [分块 K_j^T: 128x64]           │
│  ==> 产生微型中间矩阵 S_ij (仅 64x128 大小!)            │
│  ==> 局部在线 Softmax: 动态跟踪 max_val 与 sum_exp      │
│  ==> 局部乘加累加至局部输出 O_i                         │
└────────────────────────────────────────────────────────┘
        │
        └── 遍历完全部 K, V 块后，仅将最终注意力结果 O_i 写回 HBM!
```

### 2. 在线 Softmax（Online Softmax）数学机制
标准 Softmax 需要遍历整行求全局最大值 $m = \max(x_i)$ 和全局归一化分母 $l = \sum e^{x_i - m}$。在线 Softmax（Milakov & Gimelshein, 2018）允许在分块流式迭代中，每遇到新的局部最大值 $\tilde{m}$，利用缩放因子动态修正旧累加和：

$$
m_{\text{new}} = \max(m_{\text{old}}, \; \tilde{m})
$$

$$
l_{\text{new}} = l_{\text{old}} \cdot e^{m_{\text{old}} - m_{\text{new}}} + \tilde{l} \cdot e^{\tilde{m} - m_{\text{new}}}
$$

$$
\mathbf{O}_{\text{new}} = \mathbf{O}_{\text{old}} \cdot \left(\frac{l_{\text{old}} e^{m_{\text{old}} - m_{\text{new}}}}{l_{\text{new}}}\right) + \tilde{\mathbf{P}} \mathbf{V}_j \cdot \left(\frac{e^{\tilde{m} - m_{\text{new}}}}{l_{\text{new}}}\right)
$$

### 3. FlashAttention-1 到 FlashAttention-3 的代际跃迁
- **FlashAttention-1（Dao et al., 2022）：** 引入 SRAM 分块与在线 Softmax，消灭 HBM 中间矩阵，实现 $2\text{--}4\times$ 加速。外层循环遍历 K 和 V，内层循环遍历 Q。
- **FlashAttention-2（Dao, 2023）：** 反转内外层循环（外层遍历 Q，内层遍历 K/V），消灭不同 SM 之间写回输出 $\mathbf{O}$ 时的同步原子加锁；优化线程束内部划分，算力利用率从 $35\%$ 跃升至 $50\text{--}73\%$。
- **FlashAttention-3（Shah et al., 2024，专为 Hopper 架构打造）：** 
  - 充分利用硬件异步张量内存加速器（TMA），绕过寄存器直接将数据从 HBM 拷贝至 SRAM；
  - 采用 Warp-Specialization（线程束专业化分组），让一组 Warp 专职负责异步数据搬运，另一组 Warp 专职在 Tensor Core 上满负荷运算，实现数据加载与矩阵乘法的完全重叠；
  - 支持 FP8 低精度，算力利用率突破 $75\text{--}80\%$。

---

## 步骤 3：跨组件相互耦合机制

1. **反向传播中的以计算换显存（Activation Recomputation）：**  
   FlashAttention 在反向传播时不从 HBM 读取庞大的 Softmax 概率矩阵 $\mathbf{P}$，而是仅保留小巧的标量统计量 $(m, l)$，在反向传播时于 SRAM 中极速**重新计算**局部小块的 $\mathbf{S}$ 和 $\mathbf{P}$。显存占用由 $O(S^2)$ 骤降为 $O(S)$，为增大训练批大小释放了数十吉字节的空间。
2. **头维度 $d$ 与 SRAM 容量的博弈：**  
   若单头维度从 $d = 64$ 扩大到 $d = 128$ 再到 $d = 256$，单个 Tile 所占用的 SRAM 会成倍增加。当 $d = 256$ 时，单个 SM 无法再放下较大的分块，必须缩小 Tile 尺寸，导致共享内存读取冲突增加。
3. **因果掩码（Causal Masking）的计算短路：**  
   在因果自回归模型中，对于注意力矩阵右上角全为 0 的无效分块，FlashAttention 可以直接从调度层面整块跳过，直接节省近 $50\%$ 的 Tensor Core 运算量。

---

## 步骤 4：精确性能数学公式

FlashAttention 的全局显存（HBM）总访存量为：

$$
M_{\text{flash}} = 2 B H S d \text{ (读取 Q, 写入 O)} + \frac{2 B H S^2 d}{M_{\text{sram}}} \text{ (多次分块流式加载 K, V)}
$$

其中 $M_{\text{sram}}$ 为单个 SM 的片上可用共享内存容量（以元素计）。

对比朴素注意力与 FlashAttention 的显存读写量：

$$
\frac{M_{\text{flash}}}{M_{\text{naive}}} \approx \frac{O(S)}{O(S^2)} = \frac{4 d}{S} \quad (\text{当 } S \gg d \text{ 时})
$$

当序列长度 $S = 8192$、单头维度 $d = 128$ 时：

$$
\frac{M_{\text{flash}}}{M_{\text{naive}}} \approx \frac{4 \times 128}{8192} = \frac{512}{8192} = \frac{1}{16}
$$

**FlashAttention 将物理显存总线上的数据搬运量缩减到了原来的 $\frac{1}{16}$（降低了 $93.75\%$）！**

其算术强度由此跃升为：

$$
I_{\text{flash}} \approx \frac{4 B H S^2 d}{2 B H S d + \frac{2 B H S^2 d}{M_{\text{sram}}}} \propto M_{\text{sram}} \gg I_{\text{naive}}
$$

使得算子在长序列下成功脱离访存受限区间，昂贵的 Tensor Core 终于得以全速运转。

---

## 步骤 5：具体微基准数字推导

以 H100 SXM 运行 LLaMA-3 8B 序列长度 $S = 8192$、批大小 $B = 1$ 的单层注意力为例：

1. **总浮点运算量：** $F \approx 1.10 \times 10^{12} \text{ FLOPs}$。
2. **FlashAttention 实际 HBM 数据读写量：**
   - 读取 $\mathbf{Q}$: $1 \times 32 \times 8192 \times 128 \times 2 \approx 67.1 \text{ MB}$
   - 写回 $\mathbf{O}$: $67.1 \text{ MB}$
   - 分块流式读取 $\mathbf{K}, \mathbf{V}$：在 $M_{\text{sram}} \approx 100\text{ KB}$ 条件下，有效重用倍率使得读取量仅约 $0.98\text{ GB}$。
   - **HBM 总数据量：** $M \approx 1.11 \text{ GB}$（而朴素实现高达 $17.18\text{ GB}$！）。
3. **访存传输耗时：**
   $$T_{\text{memory}} = \frac{1.11 \times 10^9}{3.35 \times 10^{12}} \approx 0.00033 \text{ 秒} = 0.33 \text{ 毫秒}$$
4. **计算耗时（在 H100 实际利用率 60% 即 600 TFLOPS 下）：**
   $$T_{\text{compute}} = \frac{1.10 \times 10^{12}}{600 \times 10^{12}} \approx 1.83 \text{ 毫秒}$$
5. **实际性能对比：**
   - 朴素注意力物理耗时：$\ge 5.1 \text{ ms}$（显存带宽卡死）
   - FlashAttention 物理耗时：$\approx 1.9 \text{ ms}$（计算密集型，数据搬运时间已被计算完全隐藏）
   - **实测端到端加速比：约 $2.7\times$ 到 $4\times$！**

---

## 步骤 6：核心系统工程铁律

> 算法优化的最高境界不是减少数学乘除法次数，而是改变数据驻留的物理层级。FlashAttention 通过在线 Softmax 巧妙重构代数流程，将原本需要在 HBM 中流转的二次方中间矩阵彻底囚禁在片上 SRAM 中，完成了大模型系统从访存墙向算力充分释放的决定性飞跃。
"""

chinese_files["module-1/05-kv-cache-and-paged-attention.zh.md"] = r"""# 第 E05 章：KV Cache 显存解剖与 PagedAttention：虚拟内存分页与动态显存碎片治理

## 步骤 1：物理直觉

想象一家拥有 1000 间客房的超级度假酒店：
- **传统静态预分配（Static Allocation）：** 每当一位客人办理入住时，前台根本不知道他会住 1 天还是 30 天。为了保险起见，酒店直接为这位客人把一整层 30 间套房全部挂牌锁定，禁止其他任何人预订。结果：客人实际上只住了一晚就退房了，整座酒店有 80% 的客房长年空置，但新客人却在门口排起长龙，前台频繁报错“客满拒绝接待”！
- **分页虚拟内存管理（PagedAttention）：** 酒店引入了现代智能调度系统。客人办理入住时，前台只给他发一张今晚的房卡（一个页面 Block，包含 16 个房间）。明天如果客人续住，前台在系统中再动态分配任意空闲楼层的下一间客房，并通过一张“虚拟房间索引表”将它们逻辑拼接起来。

在自回归 LLM 推理中，历史 Token 的 Key 和 Value 向量必须被持久缓存（**KV Cache**）。如果没有操作系统般的**分页虚拟内存管理（PagedAttention）**，多达 $60\text{--}80\%$ 的昂贵显存将被静态预分配的空缓冲区和碎片彻底浪费！

---

## 步骤 2：芯片微观底层执行机制

在自回归解码（Decode）阶段，模型每生成 1 个新 Token，只需要将当前新 Token 的 $\mathbf{q}_t$ 与历史所有 Token 的 $\mathbf{k}_{\le t}$ 计算注意力分数，并对 $\mathbf{v}_{\le t}$ 聚合加权。为了避免每步都重新从头计算所有历史词的注意力投影，必须将历史的 Key 和 Value 向量保存在 GPU 显存中，这部分显存被称为 **KV Cache**。

```
传统连续缓存模式 (连续内存分配):
请求 1: [已用 Token: 100] [静态预分配的无用空闲缓冲区: 1948 个位置...] --> 显存浪费率 95%!
请求 2: [已用 Token: 400] [静态预分配的无用空闲缓冲区: 1648 个位置...] --> 无法被其他请求使用
显存物理空间充斥着外部与内部碎片，极大限制了并发并发量 (Batch Size)。

PagedAttention 分页管理模式 (vLLM 架构):
物理显存池 (由固定大小的 Block 构成，例如每个 Block 存放 16 个 Token 的 KV):
物理块 0: [R1: Tok 0-15]     物理块 1: [R2: Tok 0-15]    物理块 2: [R1: Tok 16-31]
物理块 3: [R3: Tok 0-15]     物理块 4: [R2: Tok 16-31]   物理块 5: [空闲块 Free]

块表 (Block Table):
请求 1 (R1) 逻辑页 -> 物理块映射: 逻辑块 0 -> 物理块 0, 逻辑块 1 -> 物理块 2
```

### 核心微观机制：
1. **逻辑地址与物理地址解耦：** 在 CUDA 算子内部，线程束通过读取维护在 SRAM 中的块表（Block Table），将逻辑上连续的序列索引映射为物理显存中离散的内存块指针。
2. **零显存浪费与按需分配：** 只有当一个 Block（例如 16 个 Token）完全写满时，显存管理器才会从空闲池中申请下一个物理 Block。内部碎片被严格限制在最后一个未满的 Block 内部（浪费率 $< 4\%$）。
3. **写时复制（Copy-on-Write）支撑分支生成：** 在并行采样（Parallel Sampling）或束搜索（Beam Search）中，多个候选序列的前缀完全一致。通过让多个逻辑块指向同一个物理块，共享相同历史前缀，显存消耗直接减少 $50\%$ 以上。

---

## 步骤 3：跨组件相互耦合机制

1. **KV Cache 对在线服务并发批大小的绝对压制：**  
   在推理服务中，GPU 显存总容量固定（如 $80\text{ GB}$）。除去固定的模型权重参数显存外，**剩下的所有显存都属于 KV Cache**。如果每个请求的 KV Cache 占用过大，显存能够容纳的并发请求数（Batch Size）就会被压缩。而批大小越小，解码阶段的算术强度就越低，系统的总体 Token 吞吐率就越惨淡。
2. **长上下文对显存带宽的冲击：**  
   随着生成序列从 1K 延伸到 32K，每一步解码必须从 HBM 读取的 KV Cache 体积扩大了 32 倍。单步解码耗时随上下文长度呈严格线性上升。
3. **结合 GQA 削减缓存体积：**  
   将全头注意力（MHA）改为分组查询注意力（GQA），可以直接将 KV Cache 的物理体积削减至原来的 $\frac{1}{4}$ 或 $\frac{1}{8}$，使系统并发能力直接实现 $4\times$ 到 $8\times$ 的飞跃。

---

## 步骤 4：精确性能数学公式

单条请求在序列长度为 $S$ 时，KV Cache 所占用的精确物理显存字节数为：

$$
\text{Memory}_{\text{kv}} = 2 \times 2 \times n_{\text{layers}} \times n_{\text{kv\_heads}} \times d_{\text{head}} \times S \quad (\text{字节，在 16 位精度下})
$$

公式各项因子拆解：
- 第一个 $2$：同时缓存 Key 和 Value 两个矩阵；
- 第二个 $2$：FP16 / BF16 每个数值占 2 字节；
- $n_{\text{layers}}$：Transformer 的总层数；
- $n_{\text{kv\_heads}}$：Key 和 Value 的头数（在 MHA 下等于 $n_{\text{heads}}$，在 GQA 下远小于 $n_{\text{heads}}$）；
- $d_{\text{head}}$：注意力单头的特征维度；
- $S$：当前上下文的已处理总序列长度。

对于一个并发批大小为 $B$、平均上下文长度为 $S$ 的推理引擎，全局 KV Cache 总占用为：

$$
\text{Total Memory}_{\text{kv}} = B \times \text{Memory}_{\text{kv}}(S)
$$

最大并发请求支持上限 $B_{\text{max}}$ 为：

$$
B_{\text{max}} = \left\lfloor \frac{\text{显存总容量} - \text{模型权重占用} - \text{运行时系统保留显存}}{\text{Memory}_{\text{kv}}(S_{\text{max}})} \right\rfloor
$$

在传统连续预分配机制下，分母被迫采用 $S_{\text{max}}$；而在 PagedAttention 机制下，分母仅为平均实际序列长度 $\bar{S}$，从而将有效并发容量提升：

$$
\text{并发提升倍率} = \frac{S_{\text{max}}}{\bar{S}} \approx 2\text{--}4\times
$$

---

## 步骤 5：具体微基准数字推导

以业界主流的 **LLaMA-3 70B**（采用 GQA 架构）在单卡或多卡推理环境下的 KV Cache 消耗手算：
- 模型层数：$n_{\text{layers}} = 80$
- 注意力 Query 头数：$n_{\text{heads}} = 64$
- 注意力 KV 头数：$n_{\text{kv\_heads}} = 8$（由于 GQA，仅为 Query 的 $1/8$）
- 单头维度：$d_{\text{head}} = 128$
- 数据精度：BF16（每元素 2 字节）

### 步骤 5.1：计算单 Token 的 KV Cache 显存增量
每增加 1 个 Token，全模型所有 80 层需要追加缓存的显存为：

$$
\Delta_{\text{token}} = 2 \times 2 \times 80 \times 8 \times 128 = 327,680 \text{ 字节} \approx 320 \text{ KB / Token}
$$

### 步骤 5.2：计算单条长序列请求（$S = 8192$）的显存占用
对于一条上下文长度达到 8K 的请求：

$$
\text{Memory}_{\text{req}} = 8192 \times 320 \text{ KB} \approx 2.62 \times 10^6 \text{ KB} \approx 2.62 \text{ GB}
$$

*注：如果该模型使用的是传统的 MHA（$n_{\text{kv\_heads}} = 64$），则单条请求的 KV Cache 将高达 $2.62 \times 8 = 20.97\text{ GB}$！一条长请求就能吃掉四分之一张 A100！*

### 步骤 5.3：并发与显存利用率对比
假设 8 张 H100 GPU 组网，总可用显存为 $8 \times 80 = 640\text{ GB}$。
- 模型自身权重占用（FP16）：$140\text{ GB}$
- 剩余可用显存空间：$640 - 140 - 20 (\text{系统缓存}) = 480\text{ GB}$

在平均上下文长度 $\bar{S} = 2048$ 条件下（每条请求占用约 $0.655\text{ GB}$）：
- **采用 PagedAttention 时：**
  $$B_{\text{concurrent}} \approx \frac{480 \text{ GB}}{0.655 \text{ GB}} \approx 732 \text{ 个并发请求}$$
- **采用传统静态预分配（按 $S_{\text{max}} = 8192$ 预分配）时：**
  $$B_{\text{concurrent}} \approx \frac{480 \text{ GB}}{2.62 \text{ GB}} \approx 183 \text{ 个并发请求}$$
- **结论：** PagedAttention 让同一套硬件服务器的在线服务承载并发吞吐**瞬间翻了整整 4 倍**，而推理服务的基础设施服务器成本直接降低了 $75\%$！

---

## 步骤 6：核心系统工程铁律

> 大模型推理的显存管理本质上就是操作系统的虚拟内存管理。谁在自回归服务中为每个请求静态预分配连续的显存缓冲区，谁就在将昂贵的显存带宽和容量白白弃置；将内存切分为固定粒度的小页面并通过页表动态映射，是现代高吞吐推理引擎的生命线。
"""

chinese_files["module-1/06-mha-mqa-gqa-mla.zh.md"] = r"""# 第 E06 章：注意力架构变体深度权衡：MHA、MQA、GQA 与 MLA 的硬件开销与显存对决

## 步骤 1：物理直觉

想象一家拥有 32 位翻译专家的联合翻译办公室：
- **多头注意力（MHA）：** 办公室里每位专家都要独立配备两本厚重的专业词典（自己的专属 Key 词典和 Value 词典，共 32 对词典）。书架被塞得水泄不通，走廊上全堆满了书（显存被撑爆）。
- **多查询注意力（MQA）：** 办公室规定：所有 32 位专家必须共用同一对词典。书架瞬间腾空了 31 本，但所有专家都要争抢同一本书，翻译质量出现了明显下滑。
- **分组查询注意力（GQA）：** 折中方案：将 32 位专家分成 8 个小组，每组 4 人共享一对词典。书架空间被压缩了 75%，但每组内部协作默契，翻译质量几乎零损失。
- **多头潜在注意力（MLA，DeepSeek）：** 激进的数学压缩：不再摆放整本大词典，而是把词典里的每一页都用高精度压缩算法浓缩成一小张微型缩印胶片（低秩潜在投影）。读取时只需抓取这张极薄的胶片，在手边用放大镜即时还原，显存占用暴跌 90% 以上！

---

## 步骤 2：芯片微观底层执行机制

注意力架构的演进，本质上是在**模型表达能力**与**自回归解码阶段 HBM 访存带宽消耗**之间寻找帕累托最优解：

```
MHA (多头注意力 - 传统标准):
Q 头: [Q0] [Q1] [Q2] [Q3] [Q4] [Q5] [Q6] [Q7] ... (共 H 个)
K 头: [K0] [K1] [K2] [K3] [K4] [K5] [K6] [K7] ... (共 H 个)
V 头: [V0] [V1] [V2] [V3] [V4] [V5] [V6] [V7] ... (共 H 个)
==> KV 缓存体积: 100% (最大显存消耗与访存带宽瓶颈)

MQA (多查询注意力 - 极度压缩):
Q 头: [Q0] [Q1] [Q2] [Q3] [Q4] [Q5] [Q6] [Q7] ... (共 H 个)
K 头: [唯一共享 K]
V 头: [唯一共享 V]
==> KV 缓存体积: 1/H (显存占用极小，但模型能力与收敛性受到较大损伤)

GQA (分组查询注意力 - 工业界现代黄金标准，如 LLaMA-3):
Q 头: [Q0 Q1 Q2 Q3]  [Q4 Q5 Q6 Q7]  ... (H 个 Query 分成 G 组)
K 头:    [K_组0]        [K_组1]     ... (共 G 个，通常 G = 8)
V 头:    [V_组0]        [V_组1]     ... (共 G 个)
==> KV 缓存体积: G/H (通常为 1/8，在质量几乎零损失下大幅释放显存带宽)

MLA (多头潜在注意力 - DeepSeek-V2/V3 前沿突破):
通过低秩投影将 KV 联合压缩为一个低维隐向量 c_KV (维度 d_c 远小于 H * d_head)
在自回归生成解码时，仅需将低维的 c_KV 写入并在 HBM 中缓存！
在计算注意力分数时，通过吸收矩阵乘法与 Q 融合，完全避免物化还原全量高维 KV！
```

---

## 步骤 3：跨组件相互耦合机制

1. **算术强度与硬件屋顶平衡：**  
   在自回归解码单步生成中，显存带宽是唯一的核心瓶颈。从 MHA 切换到 GQA（$G = 8, H = 64$），每步自回归需从 HBM 加载的 KV 字节数减少了 $87.5\%$。这意味着单步解码的 HBM 传输时间缩短了近 8 倍，使服务引擎可以在相同延迟约束下支持大得多的并发批大小。
2. **张量并行（TP）切分的硬件约束：**  
   在分布式张量并行中，注意力头数必须能够被 GPU 卡数整除。对于 GQA 架构，KV 头数通常为 $8$。这意味着该模型的张量并行度（TP Degree）在不引入复杂跨卡通信的情况下，**最大只能设置为 8**！如果需要在 16 卡或 32 卡上切分单层，必须辅以序列并行或流水线并行。
3. **MLA 架构的矩阵吸收优化：**  
   DeepSeek MLA 通过将 Key 矩阵的解压缩投影矩阵 $\mathbf{W}^{UK}$ 直接吸收进 Query 的投影权重 $\mathbf{W}^Q$ 中，使得计算注意力的内积直接在低维压缩空间进行，从而彻底消除了在片上 SRAM 中解压缩高维 KV 的运算开销。

---

## 步骤 4：精确性能数学公式

各架构在单 Token 上的 KV Cache 物理显存增量对比公式：

$$
\text{Bytes}_{\text{MHA}} = 4 \times n_{\text{layers}} \times H \times d_{\text{head}}
$$

$$
\text{Bytes}_{\text{GQA}} = 4 \times n_{\text{layers}} \times G \times d_{\text{head}} = \text{Bytes}_{\text{MHA}} \times \left(\frac{G}{H}\right)
$$

$$
\text{Bytes}_{\text{MQA}} = 4 \times n_{\text{layers}} \times 1 \times d_{\text{head}} = \text{Bytes}_{\text{MHA}} \times \left(\frac{1}{H}\right)
$$

$$
\text{Bytes}_{\text{MLA}} = 2 \times n_{\text{layers}} \times (d_c + d_R)
$$

其中 $d_c$ 为低秩压缩潜空间维度（如 512），$d_R$ 为解耦的带旋转位置编码（RoPE）特征维度（如 64）。

以单步自回归解码时的有效算术强度对比：

$$
I_{\text{decode}} = \frac{2 B \cdot P_{\text{model}} + 4 B \cdot n_{\text{layers}} \cdot H \cdot S \cdot d_{\text{head}}}{2 P_{\text{model}} + \text{KV\_Cache\_Bytes}(S)}
$$

随着 KV Cache 字节数被 GQA 削减 8 倍、或被 MLA 削减 15 倍以上，解码阶段的访存带宽压力大幅释放，系统的整体算术强度实现显著提升。

---

## 步骤 5：具体微基准数字推导

对比 70B 级别大模型在上下文长度 $S = 8192$、批大小 $B = 32$ 时，不同注意力架构的 KV Cache 显存消耗（模型层数 $n_{\text{layers}} = 80$，$H = 64, d_{\text{head}} = 128$）：

### 1. 传统 MHA 架构：
$$\text{单 Token 显存} = 4 \times 80 \times 64 \times 128 = 2,621,440 \text{ 字节} = 2.50 \text{ MB}$$
$$\text{全并发显存} = 32 \times 8192 \times 2.50 \text{ MB} = 655,360 \text{ MB} \approx 640 \text{ GB}!$$
*结论：需要整整 8 张 80GB 的 A100 GPU 仅仅用来装 KV 缓存，系统已无多余空间留给模型权重和并发扩展！*

### 2. GQA 架构（取 $G = 8$，如 LLaMA-3 70B）：
$$\text{单 Token 显存} = 4 \times 80 \times 8 \times 128 = 327,680 \text{ 字节} = 0.3125 \text{ MB} \quad (\text{仅为 MHA 的 } 12.5\%)$$
$$\text{全并发显存} = 32 \times 8192 \times 0.3125 \text{ MB} \approx 80 \text{ GB}$$
*结论：显存占用直降 560 GB，完全可以在 8 卡系统上轻松留出大批显存用于高并发吞吐！*

### 3. MLA 架构（DeepSeek，取 $d_c = 512, d_R = 64$）：
$$\text{单 Token 显存} = 2 \times 80 \times (512 + 64) = 92,160 \text{ 字节} \approx 0.0879 \text{ MB}$$
$$\text{全并发显存} = 32 \times 8192 \times 0.0879 \text{ MB} \approx 22.5 \text{ GB}$$
*结论：显存消耗相比 MHA 暴降至原本的 $\frac{1}{28.5}$（压缩了整整 $96.5\%$）！单台机器即可支撑恐怖的高并发长上下文推理。*

---

## 步骤 6：核心系统工程铁律

> 模型架构不是神圣不可侵犯的象牙塔，硬件约束才是架构演化的根本推手。从 MHA 到 GQA 与 MLA 的演进，本质上是算法科学家向物理显存带宽与容量现实的深刻妥协与精妙重构：通过微小的数学变换将自回归解码的访存数据量砍掉 80% 至 95%，彻底重塑了工业界大模型在线服务的吞吐与经济学底色。
"""

# Module 3
chinese_files["module-3/11-number-formats-fp8-bf16.zh.md"] = r"""# 第 E11 章：硅基芯片中的数值格式：FP32、FP16、BF16 与 FP8（E4M3/E5M2）

## 步骤 1：物理直觉

想象一把长度只有 8 厘米或 16 厘米的小刻度尺：
- **符号位（Sign）：** 决定方向（是往东走还是往西走）。
- **指数阶码位（Exponent）：** 相当于望远镜的“变倍旋钮”（它决定你能测量星系尺度的超大数，还是原子尺度的极小数——即数值的动态范围）。
- **尾数精度位（Mantissa）：** 相当于尺子表面雕刻的“细密刻度”（刻度越多，测量结果越精准细腻）。

尺子的总长度固定为 8 位或 16 位。如果你把更多长度分给“变倍旋钮”，望远镜就能看到更远的深空（不发生数值溢出 Overflow），但尺子刻度就变粗了（舍入误差增大）；反之，如果分给细密刻度，测量就极其精确，但稍微大一点的物体尺子就会爆表！

在 GPU 硅基芯片中，乘法器的晶体管硬件面积与位数呈**二次方关系**（$O(N^2)$）。将数值从 16 位降到 8 位，意味着相同的芯片硅片面积上可以塞进 **4 倍数量的乘法器**，同时将数据搬运吞吐直接**翻倍**！

---

## 步骤 2：芯片微观底层执行机制

现代深度学习加速芯片（NVIDIA Hopper/Blackwell）支持四种主流浮点数物理格式：

```
IEEE FP32 (单精度): [1 位符号] [8 位指数] [23 位尾数]
动态范围: 10^-38 ~ 10^38, 高精度，但占据 4 字节，计算消耗极大。

IEEE FP16 (半精度): [1 位符号] [5 位指数] [10 位尾数]
动态范围: 10^-5 ~ 65504, 精度适中，但极易在训练大模型梯度时发生溢出 (Underflow/Overflow)。

Google BF16 (Bfloat16): [1 位符号] [8 位指数] [7 位尾数]
动态范围完全等同于 FP32 (8 位指数)，几乎彻底杜绝溢出！保留 7 位精度，成为大模型训练的通用工业标准。

NVIDIA FP8 (Hopper 引入的 8 位浮点格式):
- FP8 E4M3: [1 位符号] [4 位指数] [3 位尾数]
  数值上限 ~448，精度较高，专用于前向激活值与模型权重 GEMM。
- FP8 E5M2: [1 位符号] [5 位指数] [2 位尾数]
  数值上限 ~57344，动态范围更宽，专用于反向传播中梯度易震荡的阶段。
```

### 芯片执行优势：
1. **Tensor Core 算力暴涨：** 在 H100 上，FP16/BF16 密集算力为 $1,000\text{ TFLOPS}$；而切换至 FP8 精度时，Tensor Core 硬件单元直接翻倍提供 **$2,000\text{ TFLOPS}$** 的恐怖算力！
2. **显存总线带宽利用率翻倍：** 原本传输 1 个 BF16 数字的 2 字节总线空间，现在可以一次性搬运 2 个 FP8 数字，有效显存带宽翻倍。

---

## 步骤 3：跨组件相互耦合机制

1. **缩放因子（Scaling Factor）与延迟缩放：**  
   由于 FP8 的动态范围非常窄（E4M3 超过 448 就会截断饱和），必须为每个张量或每个 Tile 分块维护一个 FP32 标量缩放因子 $S$。硬件在执行 GEMM 前先乘以 $S$ 映射进 FP8，算完后再除以 $S$ 还原。
2. **Master Weights（高精度主权重）保留：**  
   在 FP8 训练中，模型参数更新的步长极小（学习率如 $10^{-4}$）。如果直接在低精度上加梯度，微小的梯度更新会被直接舍入归零（Underflow 消失）。因此，优化器状态必须在 HBM 中保留一份完整的 **FP32 主权重**。

---

## 步骤 4：精确性能数学公式

对于含有 $P$ 个参数的模型在不同精度下的显存占用与吞吐公式：

$$\text{权重显存 (FP16/BF16)} = 2 \times P \text{ 字节}$$

$$\text{权重显存 (FP8)} = 1 \times P \text{ 字节} = \frac{1}{2} \text{ 权重显存 (BF16)}$$

在纯访存受限的自回归解码单步生成中，单 Token 的最小理论物理耗时为：

$$T_{\text{decode}} = \frac{\text{权重显存}}{\text{BW}_{\text{mem}}}$$

$$T_{\text{decode}}(FP8) = \frac{1 \times P}{\text{BW}_{\text{mem}}} = \frac{1}{2} T_{\text{decode}}(BF16)$$

**将数值精度从 BF16 压缩至 FP8，直接将自回归逐字生成延迟砍去整整 $50\%$！**

---

## 步骤 5：具体微基准数字推导

以 LLaMA-3 70B 模型在 H100 SXM（显存带宽 $3.35\text{ TB/s}$，峰值算力 BF16: 1000 TFLOPS, FP8: 2000 TFLOPS）下单卡解码单步手算：
- 参数量：$P = 70 \times 10^9$

### 1. BF16 精度工况：
- 显存权重载入量：$140\text{ GB}$
- 单步最小读取时间：
  $$T_{\text{BF16}} = \frac{140 \times 10^9}{3.35 \times 10^{12}} \approx 41.8 \text{ 毫秒}$$
- 最大理论单用户生成速度：$\frac{1}{0.0418} \approx 23.9 \text{ Tokens/s}$

### 2. FP8 精度工况：
- 显存权重载入量：$70\text{ GB}$
- 单步最小读取时间：
  $$T_{\text{FP8}} = \frac{70 \times 10^9}{3.35 \times 10^{12}} \approx 20.9 \text{ 毫秒}$$
- 最大理论单用户生成速度：$\frac{1}{0.0209} \approx 47.8 \text{ Tokens/s}$
- **性能飞跃：单用户交互生成速度提升整整 $100\%$！** 且原本需要 2 张 80GB 卡才能装下的 70B 模型，现在**单张卡即可完全装入**！

---

## 步骤 6：核心系统工程铁律

> 每一比特显存都是纯粹的物理成本。大模型在工业界的规模化普及，本质上是一场向低比特进军的精度战役；在 Hopper 及以后的架构上，FP8 已经成为兼顾 $2\times$ 算力翻倍与 $50\%$ 访存减半的全新分水岭。
"""

chinese_files["module-3/12-quantization-strategies-w8a8-w4a16.zh.md"] = r"""# 第 E12 章：量化策略实战：W8A8、W4A16 与 INT4 在推理阶段的物理表现与硬件选型

## 步骤 1：物理直觉

想象把一部巨幅高清电影压缩转录到老式光盘上：
- **仅权重量化（W4A16 / AWQ / GPTQ）：** 相当于把光盘本身的盘片文件狠狠压缩了 4 倍（仓储体积变小了，运送光盘变得极快）；但在播放时，播放机必须在现场一边高速解压缩，一边用全高清画质给屏幕放映。
- **权重与激活同时量化（W8A8 / SmoothQuant）：** 不仅光盘是压缩版，连放映机的主板芯片也换成了专门为 8 位信号设计的轻量化矩阵处理器，直接以超高帧率点对点硬解码，没有任何即时解压缩包袱！

如果在自回归逐字生成阶段（运送光盘是唯一瓶颈），W4A16 是绝佳神器；但如果是在提示词长文本预填阶段（播放机的计算能力被顶满），现场解压缩反而会给处理器增加额外计算负担，必须使用原生的 W8A8 甚至 FP8 才能获得真正的物理加速！

---

## 步骤 2：芯片微观底层执行机制

量化方式对底层硬件执行流有着根本性影响：

```
W4A16 (Weight-Only INT4, 激活保留 FP16):
- 全局显存 (HBM): 权重存储为 4-bit 打包整数 (每字节装 2 个权重)
- 传输过程: HBM -> 寄存器 传输量减少 75%! (极大缓解解码阶段带宽瓶颈)
- 计算前夕: 必须在片上寄存器中执行即时反量化 (Dequantize):
  w_fp16 = (w_int4 - zero_point) * scale
- 执行计算: 调用 FP16 Tensor Core 执行标准矩阵乘。
==> 适用于 Decode 阶段 (受显存带宽限制)。在 Prefill 阶段可能会变慢!

W8A8 (Weight & Activation INT8 / FP8):
- 全局显存 (HBM): 权重与激活值均以 8-bit 存储
- 计算前夕: 无需复杂的反量化解压
- 执行计算: 直接喂入 INT8 或 FP8 专用的物理 Tensor Core 硬件脉动阵列!
==> Prefill 阶段与 Decode 阶段均能获得极致性能，算力与带宽全面翻倍!
```

### 异常值（Outliers）处理困境：
当模型规模超过 6.7B 时，激活值中会出现少量极端通道（数值高达正常值的 100 倍以上）。SmoothQuant 巧妙地在数学上构造了一个对角平滑矩阵 $\mathbf{S}$：

$$
\mathbf{Y} = (\mathbf{X} \text{diag}(\mathbf{S})^{-1}) \cdot (\text{diag}(\mathbf{S}) \mathbf{W})
$$

将难以量化的激活值方差，离线转移给权重矩阵吸收，使得全网能够平稳采用 W8A8 进行低比特硬件加速。

---

## 步骤 3：跨组件相互耦合机制

1. **Prefill 与 Decode 阶段的量化收益反转：**  
   W4A16 在解码阶段表现惊艳，但在长文本预填阶段，反量化计算会成为额外的纯计算负荷；而 W8A8 则能通吃两大阶段。
2. **反量化算子与 GEMM 深度熔合：**  
   若调用独立的解量化 CUDA 算子将 INT4 转为 FP16，会多出一整次 HBM 往返。现代 AWQ/GPTQ 算子必须将反量化指令直接编写在 GEMM 的寄存器加载微流水线内部。

---

## 步骤 4：精确性能数学公式

各量化方案在单步解码（$B=1$）时的显存总线搬运量与理论加速比：

$$\text{Bytes}_{\text{FP16}} = 2 \times P \text{ 字节}$$

$$\text{Bytes}_{\text{W8A8}} = 1 \times P \text{ 字节} \quad (\text{访存减少 } 50\%)$$

$$\text{Bytes}_{\text{W4A16}} = 0.5 \times P + \text{Scales\_Zeros} \approx 0.55 \times P \text{ 字节} \quad (\text{访存减少 } \approx 72.5\%)$$

理论单步解码加速比上限：

$$\text{Speedup}_{\text{decode}} \approx \frac{\text{Bytes}_{\text{FP16}}}{\text{Bytes}_{\text{quant}}} \approx \begin{cases} 2.0\times & \text{for W8A8} \\ 3.6\times & \text{for W4A16} \end{cases}$$

---

## 步骤 5：具体微基准数字推导

以单台双卡 A100（每卡 $80\text{ GB}$，总显存 $160\text{ GB}$，每卡带宽 $2.0\text{ TB/s}$）部署 **LLaMA-3 70B** 为例：
- FP16 权重大小：$140\text{ GB}$。两张卡装下模型后仅剩 $20\text{ GB}$ 显存，根本无法支持任何长上下文并发服务！

### 采用 AWQ INT4（W4A16）量化后：
1. **模型权重显存占用：** 骤降至约 **$38.5\text{ GB}$**！
2. **显存空闲空间：** 瞬间腾出高达 $160 - 38.5 = 121.5\text{ GB}$ 的巨量空间，全部可以作为 PagedAttention 的 KV Cache 显存池！
3. **单步解码带宽时间：**
   $$T = \frac{38.5 \times 10^9}{4.0 \times 10^{12} \text{ (双卡聚合)}} \approx 9.6 \text{ 毫秒}$$
   相较于 FP16（$35.0\text{ ms}$），**单 Token 交互生成延迟缩短了整整 $3.6\times$ 倍**！

---

## 步骤 6：核心系统工程铁律

> 离开具体的运行阶段（Prefill 还是 Decode）谈量化选型是毫无意义的。自回归解码受显存带宽支配，权重量化（W4A16）能以最低代价压减延迟；而在大并发或长预填受算力支配的场景下，支持底层 Tensor Core 满速硬件加速的 W8A8 / FP8 才是真正的工业级王者。
"""

chinese_files["module-3/13-kv-cache-quantization.zh.md"] = r"""# 第 E13 章：KV Cache 显存量化压缩：将历史上下文缓存压缩至 FP8 与 INT4 的硬件收益与保真度

## 步骤 1：物理直觉

想象你必须为每一位在图书馆自习的读者留下一张写满摘要的便利贴：
- **原始方案（BF16）：** 每张便利贴都用大号硬卡纸打印，写了 1000 页书，便利贴就堆成了一座大山，自习室的桌子全部被纸张压垮了。
- **低比特压缩方案（FP8 / INT4）：** 换用极薄的便签纸，并用微型代号速记。整整 1000 页的摘要现在只需要一个小巴掌大的便签盒就能全部装下！

在长文本问答（如 32K 乃至 128K 上下文）中，**KV Cache 消耗的显存很快就会大幅反超模型权重自身**！将 KV Cache 压缩为 FP8 或 INT4，就像给自习室腾出了 75% 的空间，允许服务引擎同时接待 4 倍以上的并发读者！

---

## 步骤 2：芯片微观底层执行机制

KV Cache 量化的核心是在写出与读入环节插入瞬时量化算子：

```
写入流程 (当前 Token 生成新的 Key / Value 向量):
[新 K/V 向量: FP16] ──► [SRAM 中计算局部最大值并量化为 FP8 / INT4] ──► [写入 PagedAttention 显存池]
                                                                        (显存写入流量减少 50% - 75%!)

读取流程 (随后的注意力计算):
[从显存池加载压缩后的 K/V] ──► [在 SRAM 中反量化或直接送入低精度 Tensor Core] ──► [计算注意力分数]
```

### 两种量化粒度权衡：
1. **Per-Tensor（张量级量化）：** 整个层共用一个缩放标量。实现极简，但面对长序列中的异常值容易产生精度崩溃。
2. **Per-Head / Per-Token（分头/逐词量化）：** 为每个注意力头或每个 Token 独立分配一个 FP32 缩放因子。能完美保留注意力分数的长尾分布，精度近乎无损。

---

## 步骤 3：跨组件相互耦合机制

1. **解除在线推理的并发上限：**  
   当 KV Cache 尺寸减半（FP8）或缩小四分之三（INT4）时，GPU 显存池能容纳的 Block 数量成倍增加，直接将推理服务的最大并发吞吐推高 $2\text{--}4\times$。
2. **FlashAttention 低精度内核适配：**  
   在 FlashAttention-3 中，如果输入与 KV Cache 均为 FP8，硬件可以直接在 Hopper 的 FP8 Tensor Core 上以 $2\times$ 算力执行点积，彻底消除反量化带来的中间开销。

---

## 步骤 4：精确性能数学公式

单 Token 的 KV Cache 显存占用公式：

$$\text{Memory}_{\text{kv\_quant}} = 2 \times b \times n_{\text{layers}} \times n_{\text{kv\_heads}} \times d_{\text{head}} \text{ 字节}$$

其中数值位宽 $b$：
- BF16: $b = 2$ 字节
- FP8: $b = 1$ 字节（节省 $50\%$）
- INT4: $b = 0.5$ 字节（节省 $75\%$）

长上下文下单步解码的显存总线搬运量：

$$\text{Bytes}_{\text{decode}} = \text{Weights} + \text{Memory}_{\text{kv\_quant}}(S)$$

当上下文长度 $S$ 极大时，$\text{Memory}_{\text{kv}}$ 占支配地位，解码耗时直接与位宽 $b$ 成正比缩短！

---

## 步骤 5：具体微基准数字推导

以 LLaMA-3 70B 模型在处理上下文长度 $S = 65536$（64K 超长文本）、批大小 $B = 8$ 时手算：
- 层数 $n_{\text{layers}} = 80$，$n_{\text{kv\_heads}} = 8$，$d_{\text{head}} = 128$

### 1. BF16 精度下的 KV Cache 消耗：
$$\text{单 Token} = 2 \times 2 \times 80 \times 8 \times 128 = 320 \text{ KB}$$
$$\text{总显存} = 8 \times 65536 \times 320 \text{ KB} \approx 167.8 \times 10^6 \text{ KB} \approx 167.8 \text{ GB}!$$
*即使拥有两张 80GB 的 GPU，光是装这 8 个人的长上下文缓存就会当场 OOM 崩溃！*

### 2. 采用 INT4 KV Cache 量化后：
$$\text{单 Token} = 2 \times 0.5 \times 80 \times 8 \times 128 = 80 \text{ KB}$$
$$\text{总显存} = 8 \times 65536 \times 80 \text{ KB} \approx 41.9 \text{ GB}$$
*显存直接省出整整 $125.9\text{ GB}$！不仅彻底消除了 OOM，还允许将并发批大小进一步扩大！*

---

## 步骤 6：核心系统工程铁律

> 在长上下文大模型时代，显存容量的真正主宰是 KV Cache 而不是模型参数。对模型权重做低比特量化只能释放固定的显存空间，而对 KV Cache 实施低比特量化，才能打破长文本并发扩展的物理天花板。
"""

# Module 4
chinese_files["module-4/14-distributed-communication-primitives.zh.md"] = r"""# 第 E14 章：分布式集合通信原语：All-Reduce 环状与树状拓扑、Reduce-Scatter 与 All-Gather 通信开销

## 步骤 1：物理直觉

想象 8 位数学家围坐在一张圆桌旁：
- 每个人手中都持有一页自己计算出的矩阵数字（一份局部梯度向量）；
- 最终目标：让每一位数学家手中的数字，都变成所有人手头数字的累加总和（**All-Reduce**）。

如果所有人都在大厅中央向同一个白板喊话（中心化汇总），中间的网络交换机瞬间就会被震耳欲聋的拥堵噪音挤瘫。  
聪明的做法是**环状传递（Ring Algorithm）**：
- 每个人只把手头数据的 $\frac{1}{8}$ 切片转交给右手边的邻居，同时从左手边邻居接过数据并加到自己的纸上；
- 绕着圆桌转一圈后，每个人手中都拥有了某一块数字的全局汇总（**Reduce-Scatter**）；
- 紧接着，大家再顺着圆桌把汇总好的切片传递一圈（**All-Gather**）。

没有任何人需要同时和多个人讲话，所有人都在全速向邻居传递数据，通信总线利用率达到了惊人的 **$100\%$**！

---

## 步骤 2：芯片微观底层执行机制

现代分布式 AI 集群中，跨 GPU 通信由 NVIDIA NCCL 库主导。核心集合通信原语的执行逻辑为：

```
All-Reduce 环状算法两阶段 (Ring-AllReduce):
设 GPU 节点数为 P，通信数据总量为 N 字节。

第 1 阶段: Reduce-Scatter (累加发散)
- 将数据均匀切分成 P 个分块。
- 每个 GPU 在每一次迭代中，向右侧节点发送 1 个分块，并从左侧节点接收 1 个分块进行就地累加。
- 经过 (P - 1) 步后，第 k 个 GPU 拥有了第 k 个分块的全量加和结果。
- 传输数据量: ((P - 1) / P) * N 字节。

第 2 阶段: All-Gather (全量收集)
- 每个 GPU 将自己算好的那个全局完整分块，再次沿着环路向右侧广播传播。
- 经过 (P - 1) 步后，所有 GPU 都收集到了全部 P 个分块。
- 传输数据量: ((P - 1) / P) * N 字节。

总传输数据量 = 2 * ((P - 1) / P) * N 字节 ≈ 2N 字节 (当 P 很大时)!
```

### 拓扑感知通信（Topology-Aware Communicators）：
- **节点内部（Intra-Node）：** 8 张 GPU 之间通过 NVLink 4 专有总线相连，单向带宽高达 $450\text{ GB/s}$。
- **跨节点集群（Inter-Node）：** 通过 RoCEv2 或 InfiniBand（如 400 Gbps HDR/NDR 网络）相连，单网卡带宽仅为 $50\text{ GB/s}$（比 NVLink 慢了近 10 倍！）。
- **层次化通信（Hierarchical All-Reduce）：** NCCL 会先在每台机器的 8 张卡内部用 NVLink 做局部 Reduce-Scatter，再通过跨机网络做全局 All-Reduce，最后在节点内用 NVLink 做 All-Gather，避免脆弱的跨机网络被淹没。

---

## 步骤 3：跨组件相互耦合机制

1. **与张量并行（TP）的绑定：**  
   在 Megatron-LM 张量并行中，每一层 Transformer 必须在节点内进行 2 次实时的 All-Reduce 通信。由于该通信处于前向传播的关键路径上，**它必须运行在高速 NVLink 之上，绝不能跨机跨节点部署**。
2. **通信与计算的重叠（Communication Overlap）：**  
   通过 CUDA 流（Streams），在大规模数据并行（DDP）中，当反向传播计算上一层的权重梯度时，NCCL 可以在后台异步启动当前层的梯度 All-Reduce 通信，使网络耗时几乎完全隐蔽在计算时间之后。

---

## 步骤 4：精确性能数学公式

通信总耗时由网络**固有时延（Latency $\alpha$）**与**传输带宽耗时（Bandwidth $\beta$）**共同决定（Alpha-Beta 性能模型）：

$$T_{\text{comm}} = 2(P - 1)\alpha + 2\left(\frac{P - 1}{P}\right)\frac{N}{\text{BW}_{\text{bus}}}$$

其中：
- $P$ 为参与通信的 GPU 节点总数；
- $N$ 为通信张量的字节大小；
- $\alpha$ 为每次启动握手的网络固有时延（NVLink 约为 $1\text{--}2 \;\mu\text{s}$，跨机以太网/IB 约为 $5\text{--}15 \;\mu\text{s}$）；
- $\text{BW}_{\text{bus}}$ 为总线有效物理单向带宽。

当数据量 $N$ 足够大时（大模型场景），延迟项可以忽略，公式化简为：

$$T_{\text{AllReduce}} \approx 2 \times \frac{N}{\text{BW}_{\text{bus}}}$$

对于 Reduce-Scatter 与 All-Gather 单独原语：

$$T_{\text{ReduceScatter}} = T_{\text{AllGather}} \approx \frac{P - 1}{P} \frac{N}{\text{BW}_{\text{bus}}} \approx \frac{N}{\text{BW}_{\text{bus}}}$$

---

## 步骤 5：具体微基准数字推导

以 8 张 H100 GPU（NVLink 双向带宽 $900\text{ GB/s}$，单向有效带宽取 $\text{BW} = 450\text{ GB/s}$）组成单节点系统，测试一次全量模型梯度 All-Reduce（70B 模型在 BF16 下，梯度张量大小 $N = 140\text{ GB}$）：

1. **传输数据总量：**
   $$N_{\text{trans}} = 2 \times \frac{8 - 1}{8} \times 140 \text{ GB} = 2 \times \frac{7}{8} \times 140 = 245 \text{ GB}$$
2. **NVLink 物理传输耗时：**
   $$T = \frac{245 \times 10^9}{450 \times 10^{9}} \approx 0.544 \text{ 秒} = 544 \text{ 毫秒}$$
3. **若在普通 PCIe Gen 5（单向有效带宽 $32\text{ GB/s}$）上执行：**
   $$T_{\text{pcie}} = \frac{245 \times 10^9}{32 \times 10^9} \approx 7.65 \text{ 秒}$$
- **结论：** NVLink 将节点内通信延迟缩短了整整 **$14\times$ 倍**。对于每秒钟都在进行迭代的大模型训练系统，这直接决定了集群训练是能以 90% 线性加速，还是彻底沦为通信等待的僵尸集群。

---

## 步骤 6：核心系统工程铁律

> 分布式训练与推理的瓶颈从不在算力，而在网络。理解集合通信在物理拓扑上的分步切片传输，是在大规模多机多卡集群上正确设计张量并行、流水线并行与 ZeRO 显存分片的先决条件。
"""

chinese_files["module-4/15-tensor-parallelism-megatron.zh.md"] = r"""# 第 E15 章：张量并行（Megatron-LM）底层实现：列切与行切拆分策略，每层仅两次 All-Reduce 的通信约束

## 步骤 1：物理直觉

想象一块巨大的数学积木，它太沉了，单张显卡根本扛不动。两位工人（两张 GPU）决定合力抬起它：
- **愚蠢的切法：** 每次切下一小片，两人就停下来开会核对一次账目（频繁跨卡通信）。算完一行字开了八次会，工人大部分时间全在等对方汇报。
- **Megatron 优雅的正交切法（列切 + 行切）：**
  - **第一道工序（列切分 Column Parallel）：** 工人 A 和工人 B 各自分头加工左半部分和右半部分。在这道工序里，**双方完全不需要讲话**，各自低头全速干活；
  - **第二道工序（行切分 Row Parallel）：** 两人把刚才加工好的零件拿过来，工人 A 负责收拢前半截，工人 B 负责收拢后半截；
  - **仅在最后交差时刻：** 两人把最终算出的残差数字在桌子中央碰一下（一次 All-Reduce 通信！），立刻就能严丝合缝地拼接出与单张巨型显卡完全一模一样的全局结果！

---

## 步骤 2：芯片微观底层执行机制

Shoeybi 等人（Megatron-LM, 2019）提出了将 Transformer 内部 GEMM 进行正交切分的经典范式：

```
MLP 层的张量并行实现 (TP = 2):
输入 X [B, S, d] 同时广播至 GPU 0 与 GPU 1

第一步: 列并行线性层 (Column Parallel Linear: W1 拆分为 [W1_1, W1_2])
- GPU 0 计算: Y1 = GeLU(X @ W1_1)   --> 产生局部输出 Y1 [B, S, d_ffn / 2]
- GPU 1 计算: Y2 = GeLU(X @ W1_2)   --> 产生局部输出 Y2 [B, S, d_ffn / 2]
==> 此处完全不需要任何跨卡通信! 各自独立计算!

第二步: 行并行线性层 (Row Parallel Linear: W2 拆分为 [W2_1; W2_2])
- GPU 0 计算: Out1 = Y1 @ W2_1      --> 产生部分和 Out1 [B, S, d]
- GPU 1 计算: Out2 = Y2 @ W2_2      --> 产生部分和 Out2 [B, S, d]
==> 此处各自得到局部的部分和 (Partial Sum)!

第三步: 集合通信 All-Reduce
- Out = All-Reduce-Sum(Out1 + Out2) = X @ W1 @ W2
==> 整个 MLP 模块的前向传播，仅需 1 次 All-Reduce 通信!
```

对于多头注意力层（Self-Attention）：
- $Q, K, V$ 投影矩阵按**列切分**（每个 GPU 分配 $\frac{H}{\text{TP}}$ 个注意力头，各自独立算局部注意力）；
- 输出投影矩阵 $W_O$ 按**行切分**；
- 最终仅在 $W_O$ 输出时执行 **1 次 All-Reduce**！

---

## 步骤 3：跨组件相互耦合机制

1. **严格限制在单节点 NVLink 内部：**  
   Transformer 每一层需要 2 次 All-Reduce（注意力层 1 次，MLP 层 1 次）。对于一个 80 层的模型，单步前向传播必须连续触发 $160$ 次通信！由于通信处于关键执行路径上，**张量并行度 TP 通常严格限制在 $\text{TP} \le 8$**（即仅在单机 8 卡的 NVLink 环内运行），跨机部署会导致灾难性的网络延迟暴跌。
2. **注意力头数的物理整除约束：**  
   若选择 $\text{TP} = 8$，模型的注意力 Query 头数 $H$ 与 GQA 的 KV 头数 $G$ 必须能够被 8 整除。如果一个架构只有 6 个 KV 头，直接设置 $\text{TP} = 8$ 将引发硬件断言报错！

---

## 步骤 4：精确性能数学公式

对于隐藏层维度为 $d$、序列长度为 $S$、批大小为 $B$、张量并行度为 $\text{TP}$ 的单层 Transformer：

### 显存占用降低：
$$\text{单卡权重显存} = \frac{\text{层总权重显存}}{\text{TP}}$$

### 每层通信数据量（前向传播）：
每一层包含 2 次 All-Reduce，每次传输大小为 $B \times S \times d$ 的激活值张量：

$$\text{单层前向通信量} = 2 \times \left( 2 \cdot \frac{\text{TP} - 1}{\text{TP}} \cdot (2 B S d) \right) \text{ 字节 (FP16)}$$

当 $\text{TP} = 8$ 时：

$$\text{通信量} = 4 \times \frac{7}{8} \times (2 B S d) = 7 B S d \text{ 字节 / 层}$$

反向传播中同样产生 2 次 All-Reduce，因此整网训练每步通信量为 $14 B S d$ 字节/层。

---

## 步骤 5：具体微基准数字推导

以 LLaMA-3 70B（$d = 8192, n_{\text{layers}} = 80$）在单机 8 卡 H100（$\text{TP} = 8$），批大小 $B = 4$，序列长度 $S = 4096$ 下前向传播通信开销：

1. **单次 All-Reduce 激活值张量大小：**
   $$M = 4 \times 4096 \times 8192 \times 2 \text{ 字节} = 268.4 \text{ MB}$$
2. **单层 2 次 All-Reduce 实际走过的通信总线流量：**
   $$\text{单层流量} = 2 \times \left(2 \times \frac{7}{8} \times 268.4 \text{ MB}\right) \approx 939.4 \text{ MB}$$
3. **全模型 80 层累计总前向通信量：**
   $$\text{全模型通信总量} = 80 \times 939.4 \text{ MB} \approx 75.15 \text{ GB}$$
4. **在 NVLink（有效单向带宽 $450\text{ GB/s}$）下的物理通信总耗时：**
   $$T_{\text{comm}} = \frac{75.15 \times 10^9}{450 \times 10^9} \approx 0.167 \text{ 秒} = 167 \text{ 毫秒}$$
- **结论：** 若在没有 NVLink 的普通 PCIe 服务器上执行该操作，通信耗时将暴增至 **2.3 秒** 以上，系统完全丧失实时推理与训练价值。

---

## 步骤 6：核心系统工程铁律

> 优秀的分布式并行设计，本质是对通信拓扑的极致精简。Megatron-LM 的精髓在于通过“列切 + 行切”的正交数学共轭，将庞大的中间激活值通信完全闭环在片内，将每层的全局通信强行压缩至仅仅两次 All-Reduce，锁定了单机超高速互联的物理红利。
"""

chinese_files["module-4/16-pipeline-parallelism-1f1b.zh.md"] = r"""# 第 E16 章：流水线并行（PP）与 1F1B 调度算法：流水线气泡因子、微批次切分与激活值缓存上限

## 步骤 1：物理直觉

想象一条横跨四座车间的汽车装配流水线（4 个流水线阶段 PP = 4）：
- **车间 1：** 负责冲压底盘（模型前 20 层）；
- **车间 2：** 负责焊接车身（模型 21-40 层）；
- **车间 3：** 负责喷涂烤漆（模型 41-60 层）；
- **车间 4：** 负责总装质检（模型最后 20 层）。

如果一天只生产一辆大巴车：车间 1 干活时，其他三个车间的所有工人全都在围观摸鱼；车间 2 干活时，车间 1 已经收工，车间 3、4 还在干等。这个等待的空白期被称为**流水线气泡（Pipeline Bubble）**。  
为了消灭气泡：
- 我们必须把一天的任务切分成几十辆小型微型车（**微批次 Micro-batch**）；
- 车间 1 刚把第 1 辆微型车推给车间 2，立刻转身开始冲压第 2 辆微型车的底盘；
- 更加聪明的 **1F1B（One-Forward-One-Backward）调度法** 规定：每个车间只要干完一次前向组装（1 Forward），立刻接着干一次上一辆车的反向质检（1 Backward），使得所有工人的双手永远在满负荷做功！

---

## 步骤 2：芯片微观底层执行机制

当模型参数量庞大到单机 8 卡也装不下时，必须采用跨机器的**流水线并行（Pipeline Parallelism）**，将模型按层切分到不同的节点上：

```
朴素流水线调度 (Naive / GPipe):
卡 0: [F0][F1][F2][F3] ........................ [B3][B2][B1][B0]
卡 1: ... [F0][F1][F2][F3] ............ [B3][B2][B1][B0] ...
卡 2: ....... [F0][F1][F2][F3] .... [B3][B2][B1][B0] .......
卡 3: ........... [F0][F1][F2][F3][B3][B2][B1][B0] ...........
==> 存在大段全卡空闲的 "气泡" 浪费! 且必须在显存中同时缓存所有微批次 F 的激活值!

1F1B 稳态调度 (One-Forward-One-Backward):
卡 0: [F0][F1][F2][F3][B0][F4][B1][F5][B2][F6][B3] ...
卡 1: ... [F0][F1][F2][B0][F3][B1][F4][B2][F5][B3] ...
卡 2: ....... [F0][F1][B0][F2][B1][F3][B2][F4][B3] ...
卡 3: ........... [F0][B0][F1][B1][F2][B2][F3][B3] ...
==> 进入稳态后，每执行 1 次前向传播 (F)，紧接着执行 1 次反向传播 (B)!
==> 激活值显存占用被严格锁死在至多 PP 个微批次之内!
```

### 点对点（P2P）异步通信：
与张量并行的大规模广播不同，流水线并行只需要相邻两张卡之间进行**点对点（P2P Send/Recv）**数据投递。通信数据量小（仅为切片边界的单个激活值张量），因此可以平稳运行在跨机跨柜的以太网或 InfiniBand 链路上。

---

## 步骤 3：跨组件相互耦合机制

1. **与张量并行（TP）与数据并行（DP）形成 3D 并行：**  
   通常，系统设计遵循层级网络原则：**机内 NVLink 跑 TP=8，跨机跨柜跑 PP（流水线）与 DP（数据并行）**。
2. **显存上限与微批次数的权衡：**  
   微批次数 $M$ 越大，流水线气泡占比越小；但过大的 $M$ 会导致中间激活值长时间无法被反向传播消耗，挤占宝贵的显存。1F1B 调度将最大未结清激活值数量严格限制在流水线阶段数 $P$ 之下。

---

## 步骤 4：精确性能数学公式

流水线并行中的**气泡因子（Bubble Rate，记作 $F_{\text{bubble}}$）**为：

$$F_{\text{bubble}} = \frac{P - 1}{M}$$

其中 $P$ 为流水线阶段数（PP Degree），$M$ 为每次迭代划分的微批次总数（Micro-batches）。

系统的理论浮点计算效率上限为：

$$\eta_{\text{pp}} = 1 - F_{\text{bubble}} = 1 - \frac{P - 1}{M}$$

要使流水线气泡占比小于 $10\%$（即 $\eta_{\text{pp}} \ge 90\%$），微批次数必须满足：

$$M \ge 10 \times (P - 1)$$

在 1F1B 调度下，单个 GPU 节点需要持久缓存的最大前向激活值副本数上限被严格约束为：

$$\text{Max Cached Activations} = P$$

（而在朴素 GPipe 调度下该数字高达 $M$）。

---

## 步骤 5：具体微基准数字推导

假设一个 400B 巨型模型切分在 16 个流水线阶段上（$P = 16$）：
- 总批大小划分的微批次数：$M = 64$

1. **流水线气泡占比：**
   $$F_{\text{bubble}} = \frac{16 - 1}{64} = \frac{15}{64} \approx 23.4\%$$
   这意味着集群有近 **四分之一** 的昂贵算力被纯粹浪费在等待上游传球的气泡中！
2. **优化调度：增加微批次至 $M = 160$：**
   $$F_{\text{bubble}} = \frac{15}{160} \approx 9.37\%$$
   集群算力有效利用率成功提升至 $90.6\%$。
3. **显存对比：**
   在 $M = 160$ 时，若使用传统 GPipe，单卡需缓存 160 份微批次激活值，显存直接爆炸崩溃；而使用 1F1B，单卡显存中最多只存在 $P = 16$ 份激活值，**显存压力锐减了整整 10 倍**！

---

## 步骤 6：核心系统工程铁律

> 流水线并行的本质是切分微小任务以掩盖多阶段依赖的空白。永远保证微批次数 $M \gg P$，并坚定采用 1F1B 调度交替消化前向与反向传播，是将集群气泡开销控制在 10% 以下的工业级黄金法则。
"""

chinese_files["module-4/17-zero-fsdp-data-parallelism.zh.md"] = r"""# 第 E17 章：数据并行与零冗余优化（FSDP / ZeRO-1, 2, 3）：显存 16P 负担切分，以 50% 额外通信换取无界显存扩展

## 步骤 1：物理直觉

想象一个由 8 位搬家工人组成的团队，要搬运整整 16 箱极其沉重的大石头（16P 显存负担：模型参数、梯度、优化器动量）：
- **传统数据并行（DDP）：** 极其浪费——每个工人的背包里都要塞进全部 16 箱石头的完整副本！仅仅因为背包容积有限，工人们连走一步都费劲，根本装不下大石头（显存 OOM 崩溃）。
- **零冗余优化（ZeRO-3 / FSDP）：** 工人们达成协议：
  - “我们谁都不背完整的石头！这 16 箱石头被敲碎分成 8 份，每人背包里只背属于自己的那 $\frac{1}{8}$ 切片！”
  - 当工人 1 需要用第 2 箱石头干活时，他朝队伍喊一声：“把第 2 箱借我用一下！”（**All-Gather 即时拉取**）；
  - 工人 1 飞速算完，**立刻把借来的石头扔掉**，背包恢复轻盈；
  - 算完梯度后，大家再顺手把碎片汇拢交还给负责保管它的人（**Reduce-Scatter**）。

虽然大家在搬运过程中多费了一点嘴皮子借来还去（多了 50% 的网络通信），但每个人的背包重量**骤降了 87.5%**，使得原本需要几十台超级服务器才能跑的模型，普通小集群也能轻松扛起！

---

## 步骤 2：芯片微观底层执行机制

在混合精度（FP16/BF16）Adam 训练中，模型参数所带来的显存开销远远不止参数本身。对于一个含 $P$ 个参数的模型，静态显存被称为 **$16P$ 负担**：
- 模型权重参数（FP16）：$2P$ 字节；
- 梯度（FP16）：$2P$ 字节；
- 优化器状态（Adam 必须保存 FP32 主权重、一阶动量 $m$、二阶动量 $v$）：$(4 + 4 + 4)P = 12P$ 字节；
- **单副本静态显存累计：$2P + 2P + 12P = 16P$ 字节！**

```
ZeRO (Zero Redundancy Optimizer) 三级演进切分:
设数据并行节点数为 N_d。

ZeRO-1 (切分优化器状态 Optimizer States):
- 权重 P 和梯度 G 依然在每张卡保留完整副本。
- 12P 的优化器状态被均匀切分为 12P / N_d 分散在各卡上。
- 显存节省: ~4x, 额外通信量: 0!

ZeRO-2 (切分优化器状态 + 梯度 Gradients):
- 优化器状态 12P 与梯度 2P 同时切分。
- 每张卡只保留属于自己的局部梯度切片。
- 显存进一步大降, 额外通信量: 0! (直接用 Reduce-Scatter 替代 All-Reduce)。

ZeRO-3 / FSDP (完全切分: 优化器 + 梯度 + 模型参数 Parameters):
- 每张卡仅仅保存 16P / N_d 的全部数据!
- 前向传播: 算哪一层，临时 All-Gather 哪一层的权重，算完立即释放显存!
- 反向传播: 再次 All-Gather 权重，求出梯度后立即通过 Reduce-Scatter 聚合归还!
- 额外通信代价: 仅增加 50% 的跨卡通信量!
```

---

## 步骤 3：跨组件相互耦合机制

1. **显存释放与通信开销的终极置换：**  
   ZeRO-3 / FSDP 允许单卡显存完全突破模型参数尺寸的限制，只要卡数够多，甚至单张 $16\text{ GB}$ 的显卡都能跑 70B 模型。但代价是在前向和反向传播中频繁注入 All-Gather 通信。
2. **前向预取重叠（Prefetching Overlap）：**  
   在高效的 FSDP 实现中，当当前层正在计算 GEMM 时，CUDA 流会提前在后台异步触发下一层的权重 All-Gather 搬运，利用纯计算时间彻底掩盖数据拉取延迟。

---

## 步骤 4：精确性能数学公式

对于含 $P$ 个参数的模型，在 $N_d$ 个数据并行 GPU 上训练时的单卡静态显存公式：

$$\text{Memory}_{\text{DDP}} = 16 P \text{ 字节}$$

$$\text{Memory}_{\text{ZeRO-1}} = 4 P + \frac{12 P}{N_d} \text{ 字节}$$

$$\text{Memory}_{\text{ZeRO-2}} = 2 P + \frac{14 P}{N_d} \text{ 字节}$$

$$\text{Memory}_{\text{ZeRO-3 / FSDP}} = \frac{16 P}{N_d} \text{ 字节}$$

通信代价对比：
- 传统 DDP：仅反向传播需要 1 次梯度 All-Reduce（通信量 $2P$）。
- ZeRO-3 / FSDP：
  - 前向传播 All-Gather 权重：$1P$；
  - 反向传播 All-Gather 权重：$1P$；
  - 反向传播 Reduce-Scatter 梯度：$1P$；
  - **总通信量：$3P$！**

$$\text{通信量增加比例} = \frac{3P - 2P}{2P} = 50\%$$

**FSDP 用精确恒定的 $50\%$ 额外通信开销，换取了显存随卡数 $N_d$ 的无限线性分母压缩！**

---

## 步骤 5：具体微基准数字推导

以使用 64 张 A100（每卡 $80\text{ GB}$）全量预训练 **LLaMA-3 70B** 为例（$P = 70 \times 10^9$）：

1. **单卡 16P 静态显存总需求：**
   $$\text{Memory}_{\text{static}} = 16 \times 70 \times 10^9 \text{ 字节} = 1,120 \text{ GB}$$
   单卡根本不可能放得下（需要 14 张 80GB 卡才能拼出一份静态参数）！
2. **在 64 卡下采用 ZeRO-3 / FSDP 完全切分：**
   $$\text{单卡静态显存} = \frac{1,120 \text{ GB}}{64} \approx 17.5 \text{ GB}$$
   每张 80GB 的卡仅仅用掉了 **$17.5\text{ GB}$**，剩余的 **$62.5\text{ GB}$** 显存全部可以用来塞满超大训练批大小与激活值！
3. **性能结论：** 集群完全免去了复杂的张量并行与流水线配置，仅靠 FSDP 便能实现极致纯粹的高扩展训练。

---

## 步骤 6：核心系统工程铁律

> 任何不能被当前时钟周期立即使用的常驻显存都是对物理硬件的浪费。ZeRO 与 FSDP 的哲学是“按需索取、用毕即弃”，用确定性的 50% 额外集合通信为代价，彻底打破了深度学习被单卡物理显存容量封锁的历史。
"""

chinese_files["module-4/18-ring-attention-context-parallelism.zh.md"] = r"""# 第 E18 章：上下文与序列并行（Ring Attention）：环形拓扑通信与注意力计算完美重叠，突破单卡百万长文本瓶颈

## 步骤 1：物理直觉

想象一场横跨 8 座城市的超级国际接力阅读马拉松，要一口气研读一本长达 100 万页的巨著：
- 任何一座城市的图书馆都装不下整本书；
- 于是，每座城市各自保管其中的 12.5 万页（**上下文序列切分**）；
- **环状注意力（Ring Attention）：**
  - 城市 0 的研究员们手头拿着自己的问题清单（Query 分块），首先研读自己桌上的前 12.5 万页（Key/Value 分块）；
  - 研读完毕后，城市 0 把这 12.5 万页复印件通过高铁发给下座城市，同时从上游城市接收对方的 12.5 万页；
  - 高铁在铁轨上疾驰运送书页的同时，研究员们**一秒钟也不停歇**，立刻埋头研读刚刚送到的新书页！
  - 只要高铁运送书页的耗时，短于研究员研读一箱书的时间，**通信所产生的等待延迟就彻底变成了 0**！

---

## 步骤 2：芯片微观底层执行机制

标准 FlashAttention 能够解决单卡内部 $O(S)$ 的显存问题，但当序列长度达到 $S = 100\text{ 万}$ 时，即使是切分后的 KV Cache 本身也会突破单卡 $80\text{ GB}$ 的物理上限。  
**Ring Attention（Liu et al., 2023）** 将序列维度切分给 $N$ 个 GPU，并组织为环形拓扑：

```
GPU 环形调度 (Ring Topology, N = 4):
GPU 0: 拥有 Q0, 初始拥有 K0, V0
GPU 1: 拥有 Q1, 初始拥有 K1, V1
GPU 2: 拥有 Q2, 初始拥有 K2, V2
GPU 3: 拥有 Q3, 初始拥有 K3, V3

在 Step k (共 N 步迭代):
1. 在非阻塞通信流 (Stream 1) 中: 异步向右侧 GPU 发送当前持有的 K_curr, V_curr, 并从左侧接收 K_next, V_next (P2P Send/Recv)
2. 在核心计算流 (Stream 0) 中: 同步在 Tensor Core 上执行 FlashAttention 局部块计算:
   FlashAttn_Step(Q_local, K_curr, V_curr, 在线 Softmax 状态更新)
3. 同步两个 CUDA 流: 此时下一块 K, V 已经无缝送达 SRAM!
```

只要局部 FlashAttention 的计算耗时 $T_{\text{compute}}$ 大于点对点 P2P 通信耗时 $T_{\text{comm}}$，跨卡通信就被**完全隐蔽在计算阴影之中**！

---

## 步骤 3：跨组件相互耦合机制

1. **因果掩码（Causal Mask）的非对称负载均衡：**  
   在自回归因果注意力中，矩阵是一个下三角。如果简单切分，排在后面的 GPU 计算量远大于排在前面的 GPU。工业级实现（如 Zig-Zag Ring Attention）通过交错交织分配 Token 序列块，使得所有 GPU 的计算负载完全拉平。
2. **与张量并行（TP）的正交组合：**  
   长文本处理中，序列并行（Context Parallelism, CP）通常与张量并行正交结合：机内 8 卡跑 TP，跨机多节点通过 InfiniBand 环路跑 CP，轻松支撑起数百万 Token 的端到端训练与推理。

---

## 步骤 4：精确性能数学公式

设序列总长度为 $S$，上下文并行度为 $P_{\text{cp}}$，单头维度为 $d$。每个 GPU 负责的局部序列长度为：

$$S_{\text{local}} = \frac{S}{P_{\text{cp}}}$$

在单次环迭代中，每个 GPU 执行局部分块注意力的浮点运算量为：

$$\text{FLOPs}_{\text{step}} = 4 \times H \times S_{\text{local}}^2 \times d$$

单步点对点传输的 Key 和 Value 数据字节数为：

$$\text{Bytes}_{\text{step}} = 2 \times (2 \times H \times S_{\text{local}} \times d) = 4 H S_{\text{local}} d \text{ 字节 (FP16)}$$

通信被完全隐藏的充要物理条件是计算耗时覆盖通信耗时：

$$T_{\text{compute}} \ge T_{\text{comm}} \implies \frac{4 H S_{\text{local}}^2 d}{P_{\text{tensor\_core}}} \ge \frac{4 H S_{\text{local}} d}{\text{BW}_{\text{interconnect}}}$$

两端约去公共因子，得到极其优雅的**无感长文本物理临界公式**：

$$S_{\text{local}} \ge \frac{P_{\text{tensor\_core}}}{\text{BW}_{\text{interconnect}}}$$

只要每个 GPU 上切分到的局部序列长度 $S_{\text{local}}$ 大于当前互联网络的硬件平衡比，**通信开销就在物理上完全隐形**！

---

## 步骤 5：具体微基准数字推导

以单卡配备 400 Gbps（单向有效带宽 $\text{BW} = 45\text{ GB/s}$）InfiniBand 网卡的 H100 节点（实际有效算力取 $600\text{ TFLOPS}$）为例：

1. **计算零延迟通信所需的单卡局部序列下界：**
   $$S_{\text{local}} \ge \frac{600 \times 10^{12} \text{ FLOPs/s}}{45 \times 10^9 \text{ Bytes/s}} \approx 13,333 \text{ 个 Token}$$
2. **工程含义：**
   只要每张卡分到的 Token 数不少于约 **14K**：
   - 跨 8 张 GPU 组网，即可实现 **11 万 Token** 的无通信开销极限长文本；
   - 跨 64 张 GPU 组网，即可实现 **100 万 Token**（1M 上下文）的超长文本全速无感计算！
   - 通信开销被 100% 隐藏，集群算力利用率（MFU）与处理短序列时完全相同！

---

## 步骤 6：核心系统工程铁律

> 真正的系统扩展性不是消灭通信，而是让通信与计算完美重叠。Ring Attention 利用双缓冲流水线将环形 P2P 传输完全掩盖在 FlashAttention 分块计算的阴影之中，让百万级长上下文的计算复杂度在多卡集群中重返优雅的线性扩展轨道。
"""

# Module 5
chinese_files["module-5/19-prefill-vs-decode-regimes.zh.md"] = r"""# 第 E19 章：两大推理工况深度解耦：Prefill（计算密集型）与 Decode（访存密集型）的物理本质

## 步骤 1：物理直觉

想象一家超级餐厅面对两类截然不同的做菜任务：
- **第一类：宴席前总备菜（Prefill 预填）：** 厨房收到一张长长的宴席菜单（4000 字提示词）。主厨搬来一整车食材，大锅大灶同时猛火翻炒，所有的锅炉、大勺和刀具火力全开满负荷做功，香气冲天（Tensor Core 算力被 100% 榨干）。虽然很累，但出菜极具规模效应。
- **第二类：给包厢客人逐粒喂瓜子（Decode 逐字生成）：** 客人每次只嚼一颗瓜子，嚼完了才慢悠悠点下一颗。为了伺候这位客人，服务员每次都必须大老远跑去地下总冷库，把整座冷库里沉重无比的大箱子扛出来，小心翼翼撕开包装，只取出一颗瓜子递给客人，然后再把整箱推回冷库。主厨和猛火大灶在旁边闲得发慌打哈欠，服务员的腿却快要跑断了！

在大语言模型推理中，**Prefill（处理输入提示词）**与 **Decode（自回归逐字生成）**在物理底层根本就是两种完全不同性质的计算机工作流！

---

## 步骤 2：芯片微观底层执行机制

对比两者的芯片微观执行特征：

```
工况 A: 提示词预填阶段 (Prefill Phase)
- 输入形状: 批量输入 S 个已存在的 Token 序列 (如 S = 2048, 4096)
- 核心算子: 大规模矩阵乘 (GEMM)
- 算术强度 I: 极高 (I = 100 ~ 500+ FLOPs/Byte)
- 物理状态: 计算密集型 (Compute-Bound), 昂贵的 Tensor Core 满负荷高效轰鸣!
- 关键用户指标: 首字延迟 TTFT (Time to First Token)

工况 B: 自回归生成阶段 (Decode Phase)
- 输入形状: 每次输入仅仅 1 个全新 Token (B 个并发序列各输入 1 个)
- 核心算子: 矩阵向量乘 (GEMV) 与长 KV Cache 聚合
- 算术强度 I: 极低 (当 B 较小时, I = 1 ~ 5 FLOPs/Byte)
- 物理状态: 极度访存受限 (Memory-Bound), Tensor Core 95% 以上的时间处于闲置等待!
- 关键用户指标: 逐字生成延迟 ITL (Inter-Token Latency) 与系统吞吐量 (Tokens/s/GPU)
```

### 传统混部调度的灾难性冲突：
若将 Prefill 与 Decode 简单混合在同一个 GPU 实例中执行：
- 一个突然涌入的长提示词 Prefill 请求（需耗时数百毫秒计算），会霸占整个 GPU 的 Tensor Core；
- 导致正在以 30ms 匀速吐字的几十个 Decode 任务瞬间发生严重的**生成停顿卡顿（ITL 抖动 Jitter）**，极端损害交互体验。

---

## 步骤 3：跨组件相互耦合机制

1. **解耦式推理架构（Disaggregated Serving）：**  
   工业界最前沿的推理系统（如 Splitwise、DistServe）开始在物理上将硬件集群拆分为专职的 **Prefill 节点池** 与 **Decode 节点池**。Prefill 节点配置高算力卡，跑完后通过高速网络直接将生成的 KV Cache 传递给专攻超高带宽的 Decode 节点池，彻底根除两者互相抢占资源的冲突。
2. **批大小与带宽利用率的救赎：**  
   在 Decode 节点，提高系统吞吐的唯一办法是尽可能提升并发批大小 $B$，使更多的 Token 共享同一次全量权重的 HBM 读取。

---

## 步骤 4：精确性能数学公式

单步物理执行延迟分解公式：

$$T_{\text{prefill}} \approx \frac{2 \cdot P_{\text{model}} \cdot S}{P_{\text{peak\_compute}}} + T_{\text{flash\_attn}}(S)$$

$$T_{\text{decode}} \approx \frac{2 \cdot P_{\text{model}}}{\text{BW}_{\text{mem}}} + \frac{\text{KV\_Cache\_Bytes}(B, S)}{\text{BW}_{\text{mem}}}$$

在 Decode 阶段，生成每个 Token 的系统边际耗时随着并发批大小 $B$ 扩大而大幅摊薄：

$$\text{单 Token 边际耗时} = \frac{T_{\text{decode}}}{B} \approx \frac{2 P_{\text{model}}}{B \cdot \text{BW}_{\text{mem}}} + \frac{\text{单请求 KV 字节}}{\text{BW}_{\text{mem}}}$$

当 $B$ 足够大时，静态模型参数权重的搬运时间被完全平摊，系统吞吐量逼近物理理论极限！

---

## 步骤 5：具体微基准数字推导

以 LLaMA-3 70B 模型在 H100 SXM（算力 $1000\text{ TFLOPS}$，带宽 $3.35\text{ TB/s}$）上对比两种工况：

### 1. Prefill 阶段（处理一条 4096 长度的 Prompt）：
- 计算量：$F = 2 \times 70 \times 10^9 \times 4096 \approx 5.73 \times 10^{14} \text{ FLOPs} = 573 \text{ TFLOPs}$
- 物理耗时（按 60% 算力利用率即 600 TFLOPS 计算）：
  $$T_{\text{prefill}} \approx \frac{573 \times 10^{12}}{600 \times 10^{12}} \approx 0.955 \text{ 秒} \approx 955 \text{ 毫秒}$$
- 这一千毫秒内，GPU 算力被压榨到极致。

### 2. Decode 阶段（为该单请求生成 1 个新 Token，批大小 $B=1$）：
- 计算量：仅为 $140\text{ GFLOPs}$
- 但必须把全量 $140\text{ GB}$ 权重从 HBM 读取一遍：
  $$T_{\text{decode}} \approx \frac{140 \times 10^9}{3.35 \times 10^{12}} \approx 41.8 \text{ 毫秒}$$
- 此时单步计算实际仅需 $0.14\text{ ms}$，其余 $41.66\text{ ms}$（占比 $99.6\%$）全是在纯等待显存搬家！

---

## 步骤 6：核心系统工程铁律

> 混淆 Prefill 与 Decode 是大模型推理系统设计的大忌。前者是 Tensor Core 算力的巅峰碰撞，追求最高算术强度与低 TTFT；后者是 HBM 显存带宽的无休止折磨，追求最大并发摊销与极速 ITL。只有从物理层面对二者实施解耦与差异化调度，才能构筑最高效的生产级推理引擎。
"""

chinese_files["module-5/20-continuous-batching-scheduling.zh.md"] = r"""# 第 E20 章：连续批处理与动态调度引擎：基于 Iteration 粒度的动态调度，消灭静态批处理填充气泡

## 步骤 1：物理直觉

想象一个每次能载 4 人的游乐园过山车项目：
- **传统静态批处理（Static Batching）：** 4 位游客同时坐上车。其中 3 位游客坐了 1 圈就尽兴想下车，但最后 1 位游客买了“畅玩 100 圈”的联票。按照死板规定，所有人必须坐在车上陪他硬转 100 圈！前面的座位明明空着，外面排长队的数百名游客却被保安拦在门外禁止上车，过山车绝大部分座位全程放空（算力严重浪费，平均排队延迟炸裂）。
- **现代连续批处理（Continuous / Iteration-level Batching）：** 过山车每在站台转完 1 圈（单步迭代 Iteration），只要有人想下车，立刻解开安全带让他走；站台闸机瞬间放进下一位排队游客补上空位！过山车的每一个座位在每一圈都被严丝合缝地坐满，没有任何一个座位在空转！

---

## 步骤 2：芯片微观底层执行机制

在 Orca（Yu et al., OSDI 2022）与 vLLM 提出之前，深度学习推理遵循传统的静态批处理逻辑：必须等待批次内**最长的一条请求**完全生成结束（遇到 `<eos>`），整批才能释放。

```
传统静态批处理 (Static Batching):
请求 1: [Token 0] [Token 1] [EOS]  <-- 后面全被无用的 Padding 填充占位!
请求 2: [Token 0] [Token 1] [Token 2] ... [Token 200] [EOS]
请求 3: [Token 0] [EOS]            <-- 后面 99% 的时钟周期在纯等请求 2!
==> 显存与计算存在巨量填充浪费 (Padding Waste), 吞吐量极其低下!

连续批处理 (Continuous Batching / In-flight Batching):
迭代 0: [R1: Step 0]  [R2: Step 0]  [R3: Step 0]
迭代 1: [R1: Step 1]  [R2: Step 1]  [R3: EOS!] ──► 请求 3 结束，释放显存并返回结果!
                                                   │
迭代 2: [R1: EOS!]    [R2: Step 2]  [R4: Step 0] ◄── 新进请求 4 瞬间无缝插队填补!
迭代 3: [R5: Step 0]  [R2: Step 3]  [R4: Step 1] ◄── 新进请求 5 继续无缝补位!
==> 每一个时钟周期的迭代中，批处理槽位永远 100% 满负荷运转!
```

### 调度器核心实现机制：
在推理引擎的每个执行步（Iteration Loop）开始前：
1. **检查退出：** 检查哪些序列在上一步输出了 `<eos>` 或达到了最大生成长度，将其 KV Cache 页面归还内存池，立刻向客户端流式推送结束包；
2. **动态装载：** 检查当前空闲的显存 Block 数量，从等待队列（Waiting Queue）中挑选优先级最高的新请求，分配新的初始页面，无缝插入当前正在执行的批次张量（Running Batch）中；
3. **动态拼接：** 通过 PagedAttention 将不同长度的活跃请求打包成一个紧凑的变长输入张量，直接送入 GPU 执行。

---

## 步骤 3：跨组件相互耦合机制

1. **与 PagedAttention 互为基石：**  
   连续批处理要求序列能随时加入或离开，这在物理上要求显存绝对不能是静态连续分配的。没有 PagedAttention 的动态页表支持，频繁的动态加入与退出会迅速让显存布满不可回收的物理碎片。
2. **算术强度的稳定支撑：**  
   通过消除空闲等待，连续批处理使得 GPU 在解码阶段的有效并发批大小 $B$ 长期维持在极高水位，为矩阵乘法提供了持续稳定的算术强度保障。

---

## 步骤 4：精确性能数学公式

设一个静态批次中包含 $B$ 条请求，各请求的实际生成长度分别为 $L_1, L_2, \dots, L_B$。

在传统静态批处理下，该批次总共消耗的计算与显存等效步数为：

$$\text{Work}_{\text{static}} = B \times \max(L_1, L_2, \dots, L_B)$$

浪费在填充（Padding）上的无效工时比例为：

$$\text{Padding Waste Rate} = 1 - \frac{\sum_{i=1}^B L_i}{B \times \max(L_1, \dots, L_B)}$$

在生产环境中，生成长度通常呈高度右偏的长尾分布（大部分请求几百字，极少部分数千字），无效浪费率往往高达 **$60\%\text{--}80\%$**！

而在连续批处理调度下，系统执行的总工时精确等于有效载荷：

$$\text{Work}_{\text{continuous}} = \sum_{i=1}^B L_i$$

系统总体吞吐量（Tokens per second）提升倍率为：

$$\text{Throughput Speedup} = \frac{B \times \max(L_i)}{\sum L_i} \approx 2\text{--}4\times$$

---

## 步骤 5：具体微基准数字推导

假设一个并发批次 $B = 4$ 的在线服务场景，4 条请求的实际生成 Token 长度分别为：
- 请求 1：$50$ Tokens
- 请求 2：$100$ Tokens
- 请求 3：$150$ Tokens
- 请求 4：$1000$ Tokens

### 1. 传统静态批处理模式：
- 批次被迫以最长者为准执行整整 $1000$ 步！
- 总计消耗计算步数：$4 \times 1000 = 4000$ Token 步。
- 真正有用的计算步数：$50 + 100 + 150 + 1000 = 1300$ 步。
- **纯浪费步数：$4000 - 1300 = 2700$ 步（浪费率高达 $67.5\%$）！**
- 请求 1 的用户明明只要 50 个词，却被迫苦等整个批次跑满 1000 步才能彻底拿到响应，延迟惨遭放大 20 倍！

### 2. 连续批处理模式：
- 请求 1 在第 50 步准时交卷退出，显存槽位立刻让给请求 5；
- 请求 2 在第 100 步退出，立刻让给请求 6；
- 槽位利用率始终保持 $100\%$。
- **系统端到端 Token 产出吞吐直接暴涨 $3.08\times$ 倍，请求 1 的平均响应延迟降低了整整 $95\%$！**

---

## 步骤 6：核心系统工程铁律

> 静态批处理是大模型推理吞吐的物理毒药。任何现代工业级 LLM 运行时，其调度器粒度必须精确细化到单次 Iteration 维度；让跑完的请求立即离席释放显存，让等待的请求即刻插队补位，是推理引擎获取 3 倍以上吞吐质跃的核心底座。
"""

chinese_files["module-5/21-speculative-decoding.zh.md"] = r"""# 第 E21 章：投机解码：利用过剩算力加速生成：草稿模型推测与大模型并行验证，打破单 Token 访存墙

## 步骤 1：物理直觉

想象一位博学但行动缓慢的终身教授（大模型 Target Model），身边配了一位思维敏捷、打字极快的小助理（小草稿模型 Draft Model）：
- **传统逐字生成：** 教授一个人慢吞吞地打字。每打一个单词，他都得费劲地推着轮椅去图书馆最深处的档案库查一次资料（加载一次 140GB 权重），打出 5 个词需要跑 5 趟图书馆（耗时 200 毫秒）。
- **投机解码合作：**
  - 小助理手脚麻利，在草稿纸上以闪电般的速度噼里啪啦一口气蒙了 5 个单词（**推测阶段 Draft Phase**）；
  - 接着，小助理把写好的整整 5 个单词并排端到教授面前；
  - 教授只需要推着轮椅**跑一趟图书馆**，同时看一眼这 5 个词（**并行验证阶段 Verification Phase**）；
  - 教授推了推眼镜：“第 1、2、3 个词用得很棒，同意通过！第 4 个词语法不妥，我来改成正确词，第 5 个词作废！”
  - 结果：**跑了一趟图书馆的时间，一口气产出了整整 4 个正确 Token！**

---

## 步骤 2：芯片微观底层执行机制

投机解码（Leviathan et al., 2022; Chen et al., 2023）从根本上利用了 GPU 硬件在自回归阶段的**算力闲置过剩**现象：

```
投机解码工作流 (Draft-Verify Workflow):
设草稿步长为 K (例如 K = 4)。

第 1 步: 草稿小模型生成 (自回归极速吐出 K 个候选词)
小模型 (如 1B 参数) 体积极小, 访存开销微不足道, 极速生成:
Token 序列: [w1, w2, w3, w4]

第 2 步: 目标大模型并行验证 (一次性前向传播!)
将当前前缀加上草稿词 [w1, w2, w3, w4] 打包为一个长度为 K+1 的输入张量!
大模型 (如 70B) 仅仅执行 1 次计算密集型的 GEMM 前向传播!
==> 重点: 读取 140 GB 权重的耗时依然是固定约 40ms! 但一口气算出了 5 个位置的 Logits!

第 3 步: 拒绝采样验证 (Rejection Sampling)
如果目标模型概率分布 P >= 草稿模型概率分布 Q: 100% 接受该词!
如果 P < Q: 以 P / Q 的概率依条件接受, 否则拒绝并重采样修正!
```

**数学绝妙之处：** 该采样方案在严格概率论意义上，被证明其生成的最终文本分布与**大模型单独逐字自回归生成的概率分布完全 100% 数学等价**！没有任何智商与精度损失！

---

## 步骤 3：跨组件相互耦合机制

1. **算术强度与硬件利用率的突变：**  
   在验证阶段，大模型的输入不再是单个 Token（$M=1$），而是 $K+1$ 个 Token（$M=5$）。这直接将 GEMM 的算术强度提高了 5 倍，把原本处于极度浪费的 Tensor Core 算力利用率有效提升，实现了“用过剩算力换取时间”。
2. **草稿模型的显存开销：**  
   虽然草稿模型极小，但必须在 GPU 显存中持久驻留，并维持一份独立的 KV Cache。因此通常选用同系列的轻量级蒸馏模型（如 LLaMA-3 70B 配套 LLaMA-3 8B 或专门训练的 1B 投机头）。

---

## 步骤 4：精确性能数学公式

设草稿模型每步推测的 Token 数量为 $K$，草稿词被大模型接受的平均接受率（Acceptance Rate）为 $\alpha \in [0, 1]$。

大模型每次验证迭代所期望产出的有效 Token 数量（期望产出长度）为：

$$\mathbb{E}[\text{Accepted Tokens}] = \frac{1 - \alpha^{K+1}}{1 - \alpha}$$

端到端加速比（Speedup Ratio）公式为：

$$\text{Speedup} = \frac{\mathbb{E}[\text{Accepted Tokens}] \times T_{\text{target\_step}}}{K \cdot T_{\text{draft\_step}} + T_{\text{target\_verify}}}$$

其中：
- $T_{\text{target\_step}}$：大模型单独跑 1 步的物理耗时；
- $T_{\text{draft\_step}}$：小模型跑 1 步的物理耗时；
- $T_{\text{target\_verify}}$：大模型一次性验证 $K$ 个词的物理耗时（由于访存受限，在 $K \le 8$ 时，$T_{\text{target\_verify}} \approx T_{\text{target\_step}}$！）。

当接受率 $\alpha \ge 0.7\text{--}0.8$ 时，投机解码通常能带来 **$2\times$ 到 $3\times$ 的端到端真实物理延迟缩短**！

---

## 步骤 5：具体微基准数字推导

以 LLaMA-3 70B（目标模型）配合一个 1B 草稿模型，部署于单卡 H100 SXM：
- 目标大模型 70B 单步解码耗时：$T_{\text{target}} \approx 42 \text{ ms}$
- 1B 草稿模型单步解码耗时：$T_{\text{draft}} \approx 1.5 \text{ ms}$
- 设定推测窗口：$K = 4$
- 假设在当前对话场景下平均接受率为 $\alpha = 0.75$

### 步骤 5.1：计算一次推测-验证循环的总耗时
- 草稿模型推测 4 步耗时：$4 \times 1.5 = 6 \text{ ms}$
- 大模型并行验证一次耗时：$\approx 43 \text{ ms}$
$$\text{单次循环总物理耗时} = 6 + 43 = 49 \text{ 毫秒}$$

### 步骤 5.2：计算该循环期望生成的有效 Token 数
$$\mathbb{E} = \sum_{i=0}^4 0.75^i = 1 + 0.75 + 0.75^2 + 0.75^3 + 0.75^4 \approx 2.73 \text{ 个 Token}$$

### 步骤 5.3：对比传统逐字解码耗时
- 传统大模型独立生成 $2.73$ 个 Token 所需耗时：
  $$2.73 \times 42 \text{ ms} \approx 114.7 \text{ 毫秒}$$
- **实际加速比：**
  $$\text{Speedup} = \frac{114.7 \text{ ms}}{49 \text{ ms}} \approx 2.34\times$$
- **结论：** 在不牺牲任何模型输出精度的前提下，每秒生成的 Token 数从 $23.8\text{ Tok/s}$ 飙升至 **$55.7\text{ Tok/s}$**！

---

## 步骤 6：核心系统工程铁律

> 既然硬件的显存带宽已经将自回归解码拖入了泥潭，那就用算力去对抗带宽。投机解码的哲学是用极低廉的微小推测成本，换取大模型验证时高并发 GEMM 的算术强度红利，是打破单流交互延迟物理墙的最高效利器。
"""

chinese_files["module-5/22-chunked-prefill-prefix-caching.zh.md"] = r"""# 第 E22 章：分块预填与前缀缓存技术：长提示词切片调度以平抑 ITL 抖动，利用 Radix Tree 实现前缀零开销复用

## 步骤 1：物理直觉

想象一家繁忙的快餐汉堡店：
- **前缀缓存（Prefix Caching）：** 很多顾客点单的第一句话都是一模一样的：“我要一份招牌牛肉堡套餐，加冰可乐”（重复的长系统提示词 System Prompt）。聪明的店长绝不会每次都从头杀牛榨油，而是把做好的 50 份半成品汉堡胚恒温保温在架子上（**前缀缓存**）。新顾客一开口，店员一秒钟取出半成品，只需最后往上面挤一滴特调沙拉酱即可出餐！
- **分块预填（Chunked Prefill）：** 突然，一位顾客进门要为全公司订购 500 个汉堡（一个长达 32K Token 的超长提示词）。如果厨房把所有员工全部调去立刻做这 500 个汉堡，外面所有排队买单杯咖啡的散客都会被晾在原地 20 分钟（严重破坏逐字交互延迟 ITL）。店长规定：每次只做 50 个汉堡（**切片分块 Chunking**），做完 50 个就回头帮散客打一杯咖啡，然后再做 50 个。这样，既做完了大单，散客的体验也丝毫不受影响！

---

## 步骤 2：芯片微观底层执行机制

### 1. 前缀缓存（Prefix Caching）与基数树（Radix Tree）
在多轮对话或复杂 Agent 场景中，大量请求共享相同的 System Prompt、Few-Shot 示例或检索增强上下文（RAG Context）：
- 传统推理：每次请求到来，即使前 3000 字一模一样，GPU 也必须从头执行一次完整的 Prefill，并把 KV Cache 重新算一遍；
- **Radix Tree 缓存机制（如 SGLang / vLLM）：** 在内存中用树状结构索引 Token 序列。新请求到达时，系统在 Radix Tree 中秒级检索匹配最长公共前缀；命中的历史物理 Block 被直接重用并加锁，**完全免除了对应前缀的全部浮点计算与显存写入**，首字延迟（TTFT）暴降为微秒级！

```
Radix Tree 前缀缓存结构:
[系统提示词 System Prompt: "You are a helpful assistant..."] (物理块 0 ~ 15)
                             │
            ┌────────────────┴────────────────┐
            ▼                                 ▼
   [知识库文档 A: "RAG Doc 1..."]    [知识库文档 B: "RAG Doc 2..."]
   (物理块 16 ~ 31)                  (物理块 32 ~ 47)
            │                                 │
            ▼                                 ▼
      [用户提问 1]                      [用户提问 2]
```

### 2. 分块预填（Chunked Prefill，如 Sarathi-Serve）
将长度极长的提示词输入切割成固定大小的块（例如每个 Chunk 包含 512 个 Token）：
- 在每次推理调度时，将“一个 512 大小的 Prefill 切片”与“正在生成的若干个 Decode 请求”**混合打包成同一个 Batch 执行**；
- 使得该 Batch 的算术强度刚好达到硬件平衡点，既充分榨干了 Tensor Core 算力，又彻底避免了巨型 Prefill 霸占 GPU 导致的自回归打字停顿。

---

## 步骤 3：跨组件相互耦合机制

1. **ITL（逐字延迟）方差的大幅抹平：**  
   未采用 Chunked Prefill 前，系统的 ITL 监控曲线上布满刺眼的高尖刺脉冲（每当有长提示词涌入就卡顿数百毫秒）；引入切片后，ITL 曲线平稳得像一条直线，SLA 保证率达到 99.9%。
2. **写时复制与 LRU 缓存驱逐：**  
   前缀缓存池使用引用计数与 LRU 机制管理。当显存紧张时，自动释放最久未被访问的公共前缀物理块，保证活跃服务的高可用。

---

## 步骤 4：精确性能数学公式

对于共享前缀长度为 $S_{\text{prefix}}$、新输入长度为 $S_{\text{new}}$ 的请求：

### 传统首字延迟（TTFT）：
$$\text{TTFT}_{\text{standard}} = \frac{2 \cdot P \cdot (S_{\text{prefix}} + S_{\text{new}})}{P_{\text{compute}}} + T_{\text{attn}}(S_{\text{prefix}} + S_{\text{new}})$$

### 前缀缓存命中时的首字延迟：
$$\text{TTFT}_{\text{cached}} = \frac{2 \cdot P \cdot S_{\text{new}}}{P_{\text{compute}}} + T_{\text{attn\_append}}(S_{\text{prefix}}, S_{\text{new}})$$

首字延迟缩短比例：

$$\Delta \text{TTFT} \approx \frac{S_{\text{prefix}}}{S_{\text{prefix}} + S_{\text{new}}}$$

当处理长 RAG 文档（如 $S_{\text{prefix}} = 8000$，新提问 $S_{\text{new}} = 100$）时：

$$\Delta \text{TTFT} \approx \frac{8000}{8100} \approx 98.7\% \text{ 的首字延迟被彻底消灭！}$$

---

## 步骤 5：具体微基准数字推导

以企业级 Agent 助手部署为例，系统预置了 $4096$ 个 Token 的长规则提示词，用户连续发起多轮追问：
- 模型：LLaMA-3 70B（H100 SXM，前向预填速度约 4000 Tokens/秒）

### 场景 A：无前缀缓存
每轮追问到来，GPU 都必须重新解析这 4096 个词的系统提示词：
- 每次额外计算耗时：$T = \frac{4096}{4000} \approx 1.02 \text{ 秒}$。
- 用户发送一条短消息，必须在屏幕前死等 **1 秒多** 才能看到第一个字出现！

### 场景 B：采用 Radix Tree 前缀缓存
这 4096 个 Token 的 KV Cache 早已保存在物理显存池中：
- 计算耗时：仅为用户当次提问的 20 个词，耗时不足 $0.005\text{ 秒}$（$5\text{ ms}$）！
- **首字延迟从 $1020\text{ ms}$ 断崖式下跌至 $5\text{ ms}$（提速超 200 倍！）**，用户体验达到丝滑的即时响应。

---

## 步骤 6：核心系统工程铁律

> 生产环境中最昂贵的算力浪费，是对完全相同的上下文进行无休止的重复计算。通过 Radix Tree 将历史 KV Cache 沉淀为可重用的物理数据资产，并用 Chunked 切片平抑长输入冲击，是构建现代化低延迟长文本推理服务的双子灯塔。
"""

# Module 6
chinese_files["module-6/23-moe-routing-capacity-factor.zh.md"] = r"""# 第 E23 章：MoE 系统架构：Top-K 门控与容量因子：激活参数与总参数解耦，专家容量超载丢弃策略

## 步骤 1：物理直觉

想象一个拥有一所包含 64 位专科医生的大型综合医院：
- **稠密模型（Dense）：** 每来一位感冒发烧的普通病人，医院居然把全院所有 64 位专科医生（心血管、眼科、神经科、皮肤科）同时召集进同一个诊室，所有人一起给病人把脉问诊！虽然诊断很周全，但医院每分钟的开销极其恐怖，诊室里人挤人彻底堵死。
- **混合专家模型（MoE）：** 医院门诊大厅设立了一位经验丰富的**分诊护士（门控路由 Top-K Gating）**。病人进门后，护士看一眼病情，快速挑选最擅长的 **2 位专家**（例如呼吸科专家 A 和 B）接诊，其余 62 位专家在各自办公室休息或接待其他病人！

每个病人的开销直接降低为原本的 $\frac{2}{64} \approx 3\%$！但这里隐藏着一个物理危机：**如果全城突然爆发流感，所有病人都挤着去找呼吸科专家，呼吸科诊室门口排起长龙，而其他诊室全部闲置（专家负载失衡）**。此时，医院必须设定一个**专家容量上限（Capacity Factor）**，超出负荷的病人必须被分流或暂缓挂号！

---

## 步骤 2：芯片微观底层执行机制

MoE（Mixture of Experts）将大模型中参数量最大的 FFN 层替换为一组由门控网络（Router）路由的独立专家模块：

$$
\mathbf{y} = \sum_{i \in \text{TopK}(G(\mathbf{x}))} g_i(\mathbf{x}) \cdot \text{Expert}_i(\mathbf{x})
$$

```
MoE 门控路由与专家调度:
输入 Token 序列: X [B*S, d]
       │
       ▼
[门控路由网络 Router: x @ W_gate] ──► 产出 Softmax 路由分数
       │
       ▼
[Top-K 算法挑选最高分的前 K 个专家 (如 Top-2)]
       │
       ├── Token 分流与重排 (Token Dispatch / Scatter)
       ▼
┌──────────────────┬──────────────────┬──────────────────┐
│  专家 0 (FFN_0)  │  专家 1 (FFN_1)  │  ... 专家 E-1    │
│  处理属于自己的   │  处理属于自己的   │                  │
│  Token 批次      │  Token 批次      │                  │
└────────┬─────────┴────────┬─────────┴────────┬─────────┘
         │                  │                  │
         └── Token 汇聚与加权累加 (Token Combine / Gather)
```

### 物理痛点：专家容量因子（Capacity Factor，CF）
GPU 硬件的 GEMM 算子极其依赖规则、固定尺寸的张量输入。如果专家 0 接收到了 100 个 Token，而专家 1 只分到了 2 个 Token，硬件流水线将发生严重的负载倾斜。  
为此，系统设置**专家容量（Expert Capacity）**：

$$\text{Capacity} = \left\lceil \frac{B \times S \times K}{E} \times \text{CF} \right\rceil$$

- 当某个专家分配到的实际 Token 数超出该阈值时，超出部分将被直接**丢弃（Dropped Tokens）**或通过残差连接直接绕过计算；
- 在模型训练中，必须引入**辅助负载均衡损失（Load Balancing Auxiliary Loss）**，在数学上惩罚门控将请求扎堆给少数专家的行为。

---

## 步骤 3：跨组件相互耦合机制

1. **显存总容量与激活参数的物理割裂：**  
   对于一个总参数量 236B、每次仅激活 21B 的 MoE 模型（如 DeepSeek-V2）：
   - 在**计算开销（FLOPs）**上：它仅等同于一个 21B 的轻量小模型；
   - 但在**显存容量（HBM Capacity）**上：它必须完整常驻整整 236B 的全部权重（即使没被调用的专家也必须在显存里占用空间）！
2. **训练反向传播的梯度稀疏性：**  
   未被激活的专家在当次迭代中梯度为 0，只有当批次累加足够大时，才能保证所有专家的参数得到充分更新。

---

## 步骤 4：精确性能数学公式

设模型包含 $E$ 个专家，每次激活 $K$ 个专家，FFN 参数量为 $P_{\text{ffn\_expert}}$：

全模型 FFN 的总静态参数量：

$$P_{\text{total\_moe}} = E \times P_{\text{ffn\_expert}}$$

单 Token 前向传播实际消耗的有效参数量（激活参数量）：

$$P_{\text{active\_moe}} = K \times P_{\text{ffn\_expert}} = P_{\text{total\_moe}} \times \left(\frac{K}{E}\right)$$

其单 Token 计算量比同规模稠密模型缩减为：

$$\text{FLOPs 压缩比} = \frac{K}{E}$$

当 $E = 64, K = 2$ 时：

$$\text{FLOPs 压缩比} = \frac{2}{64} = \frac{1}{32} \approx 3.125\%$$

**MoE 用完全相同的计算算力消耗，支撑起了高达 32 倍的庞大知识库容量！**

---

## 步骤 5：具体微基准数字推导

对比 16 个专家的 MoE 模型（总参数量 $16 \times 7\text{ B} \approx 112\text{ B}$，每次激活 $K = 2$ 个专家，等效激活参数量约为 $14\text{ B}$）：
- 硬件：单台 8 卡 H100（每卡 $80\text{ GB}$，总显存 $640\text{ GB}$）
- 序列批大小：$B \times S = 2048$ 个 Token

### 1. 静态显存占用：
- 全模型在 FP16 下必须完整装入：
  $$\text{Memory} \approx 112 \times 2 = 224 \text{ GB}$$
- 8 张卡完全可以容纳该静态权重（每卡分摊 $28\text{ GB}$）。

### 2. 算力开销对比：
- 若这是一个普通的 112B 稠密模型，每 Token 需做约 $224\text{ GFLOPs}$ 运算；
- 而该 MoE 模型每 Token 仅需做：
  $$\text{FLOPs} = 2 \times 14 \times 10^9 = 28 \text{ GFLOPs}$$
- **计算提速：单步推理耗时缩短了整整 $8\times$ 倍！**  
  系统在具备千亿模型智能深度的同时，享受着百亿级小模型的飞速生成体验。

---

## 步骤 6：核心系统工程铁律

> 混合专家模型的本质，是用低廉的静态显存容量去置换昂贵的动态浮点算力。处理 MoE 系统的核心不在于专家内部的 GEMM，而在于门控路由的负载均衡与容量因子截断控制，防止局部硬件在热点倾斜中陷入长尾阻塞。
"""

chinese_files["module-6/24-all-to-all-distributed-moe.zh.md"] = r"""# 第 E24 章：All-to-All 跨机调度与通信瓶颈：分布式专家并行中的交换网络压力与双流水线重叠优化

## 步骤 1：物理直觉

想象一场跨国学术研讨会，有 8 间分布在不同大楼的分会场（8 个 GPU 节点）：
- 会场 0 驻扎着物理学家，会场 1 驻扎着生物学家，会场 2 驻扎着数学家；
- 突然，会场 0 里的 100 位参会者各自提出了自己的问题：其中 20 个是生物学问题，30 个是数学问题，50 个是物理学问题；
- **全员大对调（All-to-All 跨机大挪移）：**
  - 所有会场的所有人，必须同时收拾书包走出大楼，在校园主干道上擦肩而过，各自奔向对应的大楼寻找专家；
  - 专家们在诊室解答完问题后；
  - 所有人**再次同时背上书包走上校道**，原路返回各自最初的会场大楼！

如果校园道路（网络交换机拓扑）不够宽阔，**成千上万人同时在道路上对冲，整座校园将在几秒钟内发生致命交通瘫痪**！

---

## 步骤 2：芯片微观底层执行机制

在超大规模 MoE 模型中，通常将不同的专家分布在不同的物理 GPU 上，这被称为**专家并行（Expert Parallelism, EP）**。  
在 EP 架构中，Token 必须根据门控路由结果，通过 **All-to-All 集合通信** 跨机投递到专家所在的 GPU，计算完毕后再通过第二次 All-to-All 原路汇聚：

```
分布式专家并行 (EP) 下的 All-to-All 通信流:
GPU 0 (持有专家 0, 1)                      GPU 1 (持有专家 2, 3)
Token 序列: [T_A -> 专0] [T_B -> 专2]        Token 序列: [T_C -> 专1] [T_D -> 专3]
     │                                           │
     └─────────────────────┬─────────────────────┘
                           │
             [第 1 次 All-to-All 集合通信: Token 分发]
             (GPU 0 将 T_B 送给 GPU 1; GPU 1 将 T_C 送给 GPU 0)
                           │
     ┌─────────────────────┴─────────────────────┐
     ▼                                           ▼
GPU 0 本地专家执行:                         GPU 1 本地专家执行:
- 专家 0 处理 [T_A]                         - 专家 2 处理 [T_B]
- 专家 1 处理 [T_C]                         - 专家 3 处理 [T_D]
     │                                           │
     └─────────────────────┬─────────────────────┘
                           │
             [第 2 次 All-to-All 集合通信: 结果归位]
             (将计算结果反向原路送回各自所属的上下文序列)
                           │
     ┌─────────────────────┴─────────────────────┐
     ▼                                           ▼
GPU 0 重新拼接原始序列                      GPU 1 重新拼接原始序列
```

### 核心物理瓶颈：全互联双向流量对冲
与 All-Reduce 的有序环形传输不同，All-to-All 要求**任意两张卡之间必须同时发起点对点数据传输**。对于一个由 64 台机器组成的集群，每个节点必须同时跨越数据中心核心交换机向其余 63 个节点并发打流，极易诱发交换机端口的“微突发拥塞（Incast / Microburst）”与严重丢包。

---

## 步骤 3：跨组件相互耦合机制

1. **计算与通信的双流水线重叠（DualPipe / Overlap）：**  
   为了防止 GPU 在 All-to-All 传输期间彻底空等，DeepSeek-V3 采用了精妙的双流水线机制：将当前批次的 Token 切分为两半。当第一半 Token 正在网络上执行跨卡 All-to-All 传输时，计算核心正在全速为上一半 Token 计算专家 GEMM，实现网络带宽与 Tensor Core 算力的完美重叠！
2. **节点内与跨节点分层路由：**  
   优先将经常被同时激活的专家放置在同一个机柜的 NVLink 域内，尽量将大体积的 Token 传输留在高带宽机内网络，减少穿透跨机跨柜交换机的流量。

---

## 步骤 4：精确性能数学公式

设专家并行度为 $P_{\text{ep}}$，每张卡处理的 Token 总数为 $S_{\text{local}}$，每个 Token 包含 $K$ 个激活专家，特征维度为 $d$。

在最坏的完全均匀分散场景下，每张卡需要向其余 $(P_{\text{ep}} - 1)$ 张卡发送的数据量为：

$$\text{Data}_{\text{sent}} = \frac{P_{\text{ep}} - 1}{P_{\text{ep}}} \times (S_{\text{local}} \cdot K \cdot d \cdot b) \text{ 字节}$$

一次完整的 MoE 前向传播需要进行 2 次 All-to-All 通信（Token 分发 + 结果聚合）：

$$\text{Total Comm Bytes} = 2 \times \text{Data}_{\text{sent}} \approx 2 \times (S_{\text{local}} \cdot K \cdot d \cdot b) \text{ 字节}$$

单层 MoE 在网络上产生的物理通信总耗时为：

$$T_{\text{all\_to\_all}} = \frac{\text{Total Comm Bytes}}{\text{BW}_{\text{net}}}$$

当集群跨机网络带宽 $\text{BW}_{\text{net}}$ 有限时，该通信耗时往往会数倍于本地专家 GEMM 的计算时间，导致集群性能跌入“通信深渊”。

---

## 步骤 5：具体微基准数字推导

以一个跨 16 台机器（每卡配 400 Gbps 网卡，有效跨机单向带宽 $\text{BW} = 45\text{ GB/s}$）运行的分布式 MoE 模型为例：
- $d = 7168$，$K = 4$ 个激活专家，精度 FP16（$b = 2$ 字节）
- 单卡局部批大小：$S_{\text{local}} = 2048$ 个 Token

1. **单次 All-to-All 通信量计算：**
   $$\text{Bytes} \approx 2048 \times 4 \times 7168 \times 2 \approx 1.174 \times 10^8 \text{ 字节} \approx 117.4 \text{ MB}$$
2. **单层 2 次 All-to-All 往返总流量：**
   $$2 \times 117.4 \text{ MB} \approx 234.8 \text{ MB}$$
3. **跨机网络物理耗时：**
   $$T_{\text{net}} = \frac{234.8 \times 10^6}{45 \times 10^9} \approx 0.0052 \text{ 秒} = 5.2 \text{ 毫秒}$$
4. **与本地专家计算耗时对比：**
   如果每个专家的 GEMM 计算耗时仅为 $3.0\text{ ms}$：
   - 通信耗时（$5.2\text{ ms}$）明显大于计算耗时（$3.0\text{ ms}$）！
   - 如果没有异步流水线重叠，整台 GPU **将有超过 60% 的时间在纯空等网络发包**！

---

## 步骤 6：核心系统工程铁律

> 分布式混合专家模型的真正死穴在数据中心网络。All-to-All 通信的网状对冲特性，使得任何跨节点带宽瓶颈都会瞬间放大数十倍；通过精细的双流水线切片将通信严密包裹在本地计算的阴影之下，是支撑超大规模 MoE 集群稳定运行的最高工程护城河。
"""

# Module 7
chinese_files["module-7/25-model-memory-budget.zh.md"] = r"""# 第 E25 章：大模型显存预算全景公式：训练 16P 静态显存、优化器状态与前向反向激活值显存的精准计算

## 步骤 1：物理直觉

想象你在规划一场耗资百亿的超级火箭发射工程（80GB GPU 显存规划）：
- **基底配重（模型静态权重）：** 火箭本身的钢结构框架，一克都不能少（参数占用）；
- **燃料与助推储备（优化器状态）：** 占整枚火箭体积近 70% 的庞大燃料罐（Adam 的两阶动量与高精度主权重），点火起飞前必须装得满满当当；
- **飞行空气动力载荷（反向传播激活值）：** 火箭穿越大气层时随高度剧烈起伏的动态空气阻力（前向传播记录的中间运算缓存）。

如果在点火升空的一瞬间（反向传播启动的峰值时刻），火箭总重量超出了一克允许的载重极限，整枚火箭就会在半空中当场粉碎爆炸（**CUDA Out-of-Memory / OOM**）！大模型系统工程师必须在动手写第一行代码前，用精密算盘精确算定每一个字节的物理去向！

---

## 步骤 2：芯片微观底层执行机制

在大模型训练与推理中，GPU 显存被四大板块严格割裂占用：

```
GPU 物理全局显存 (HBM) 全景剖析:
┌────────────────────────────────────────────────────────┐
│ 1. 静态模型权重 (Model Weights):                       │
│    FP16 / BF16 精度下固定占用: 2 * P 字节              │
├────────────────────────────────────────────────────────┤
│ 2. 静态梯度与优化器状态 (Gradients & Optimizer States): │
│    - 梯度张量 (FP16): 2 * P 字节                       │
│    - AdamW 优化器状态 (FP32 主权重 + 动量 m + 方差 v): │
│      (4 + 4 + 4) * P = 12 * P 字节                     │
│    ==> 静态三合一累计占用: 16 * P 字节!                │
├────────────────────────────────────────────────────────┤
│ 3. 动态前向激活值显存 (Forward Activations):            │
│    为了反向传播求导，前向传播必须保留的所有中间输出。   │
│    与序列长度 S、批大小 B、层数 L 呈强相关!            │
├────────────────────────────────────────────────────────┤
│ 4. 临时工作区与框架保留缓存 (Workspace & Overhead):    │
│    cuBLAS / cuDNN 算子临时显存、通信缓冲区 (~2-4 GB)   │
└────────────────────────────────────────────────────────┘
```

---

## 步骤 3：跨组件相互耦合机制

1. **激活值重计算（Activation Checkpointing）的以算力换显存：**  
   若保留所有层的中间激活值，显存会随层数和长序列迅速爆炸。通过**选择性重计算（Selective Recomputation）**，只保留每层开头的输入残差张量，在反向传播到达该层时当场重新执行一次 FlashAttention 和 SwiGLU，直接抹除 **$70\%\text{--}80\%$** 的激活值显存开销。
2. **结合 ZeRO 拆解 16P 静态重担：**  
   通过 ZeRO-3 将 16P 均摊给 64 张卡，原本单卡需要 1120GB 显存的 70B 模型瞬间缩水至 17.5GB，为海量动态激活值腾出了充裕的物理空间。

---

## 步骤 4：精确性能数学公式

对于含 $P$ 个参数的模型，在混合精度 AdamW 下训练的**全局静态显存公式**：

$$\text{Memory}_{\text{static}} = \underbrace{2 P}_{\text{Weights}} + \underbrace{2 P}_{\text{Gradients}} + \underbrace{12 P}_{\text{Optimizer}} = 16 P \text{ 字节}$$

单层 Transformer 在前向传播中若不采用重计算，其**动态激活值显存公式**为：

$$\text{Act}_{\text{layer}} = B S d \times \left( \underbrace{2}_{\text{RMSNorm}} + \underbrace{2 \cdot \frac{n_{\text{kv\_heads}}}{n_{\text{heads}}} + 2}_{\text{QKV 投影与 RoPE}} + \underbrace{5 \cdot \frac{d_{\text{ffn}}}{d}}_{\text{SwiGLU 3 矩阵与激活}} \right) \text{ 元素}$$

在全模型 $L$ 层累加后，总激活值显存（未开启 Checkpointing）为：

$$\text{Memory}_{\text{act\_full}} = 2 \times L \times \text{Act}_{\text{layer}} \text{ 字节 (FP16)}$$

而采用选择性重计算（仅重算注意力 Softmax 与 SwiGLU 激活）后，激活值显存缩减为：

$$\text{Memory}_{\text{act\_selective}} \approx 2 \times L \times (2 B S d) \text{ 字节}$$

---

## 步骤 5：具体微基准数字推导

以使用单台 8 卡 A100（每卡 $80\text{ GB}$，总显存 $640\text{ GB}$）进行 **LLaMA-3 8B**（$P = 8 \times 10^9$，$L = 32, d = 4096$）全量训练为例，批大小 $B = 4$，序列长度 $S = 4096$：

### 1. 计算 16P 全局静态显存：
$$\text{Memory}_{\text{static}} = 16 \times 8 \times 10^9 \text{ 字节} = 128 \text{ GB}$$
分配到 8 张卡上（采用 ZeRO-3 或 FSDP）：
$$\text{单卡静态显存} = \frac{128 \text{ GB}}{8} = 16 \text{ GB}$$

### 2. 计算动态激活值显存（若未开启重计算）：
单层单 Token 激活值元素数：
$$\text{Act}_{\text{layer}} \approx 4 \times 4096 \times 4096 \times (2 + 4 + 5 \times 3.5) \approx 6.71 \times 10^7 \times 23.5 \approx 1.57 \times 10^9 \text{ 元素}$$
全模型 32 层在 FP16 下的总激活值显存：
$$\text{Memory}_{\text{act}} = 2 \times 32 \times 1.57 \times 10^9 \text{ 字节} \approx 100.5 \text{ GB}!$$
*结论：单单激活值就超过了 80GB 卡的上限，直接 OOM 崩溃！*

### 3. 开启选择性重计算后：
$$\text{Memory}_{\text{act}} \approx 2 \times 32 \times (2 \times 4 \times 4096 \times 4096) \times 2 \text{ 字节} \approx 8.58 \text{ GB}$$
- 单卡总显存开销：
  $$\text{单卡总显存} = 16\text{ GB (静态)} + 8.58\text{ GB (激活值)} + 4\text{ GB (运行缓存)} \approx 28.58 \text{ GB}$$
- **实测结果：稳定运行于 80GB 显存之内，显存利用率健康平稳，彻底绝缘 OOM！**

---

## 步骤 6：核心系统工程铁律

> 显存预算不是靠运行时盲目试错摸出来的，而是靠首原则公式严密推导出来的。深刻吃透 16P 静态底座与序列激活值在反向传播中的峰值重叠，是每一个资深大模型系统工程师必须终生掌握的基本功。
"""

chinese_files["module-7/26-measuring-mfu-hfu-profiling.zh.md"] = r"""# 第 E26 章：真实硬件利用率度量：从理论峰值到 MFU、HFU 精准测量与 Nsight 工具链实战

## 步骤 1：物理直觉

想象你买了一辆仪表盘标称最高时速为 300 公里/小时的超级超跑（硬件标称峰值算力 Theoretical Peak FLOPS）：
- **硬件浮点利用率（HFU - Hardware FLOPs Utilization）：** 你的跑车在赛道上实际开到了 180 公里/小时（仪表盘速度占比 $60\%$）。但这 180 公里的时速里，有一部分是因为你在赛道上开错了路在原地兜圈子（包含了算子重计算、多余通信垫片等无效操作）；
- **模型浮点利用率（MFU - Model FLOPs Utilization）：** 严格按照地图上从起点到终点的**纯直线几何最短距离**，除以你实际消耗的时间算出的“绝对纯有效速度”。

在真实的工业界集群中，你向上级汇报、向投资人证明技术实力的唯一硬通货，从来不是你买了多少张卡，而是你的整个训练任务在物理上达到了多少 **MFU**！如果 MFU 低于 30%，意味着你花数千万元租用的算力集群，有 70% 的资金纯粹在机房里化作了毫无意义的热量！

---

## 步骤 2：芯片微观底层执行机制

在评估现代 LLM 训练效率时，必须严格区分以下三大概念：

```
硬件峰值算力 (Theoretical Peak FLOPS, P_peak):
芯片厂商宣传单上的物理理论上限 (例如 H100 SXM 在 BF16 密集计算下为 1,000 TFLOPS).

硬件浮点利用率 (Hardware FLOPs Utilization, HFU):
芯片上所有计算单元实际执行的所有乘加运算总量 (包含反向传播激活值重计算执行的重复 FLOPs)
除以 (P_peak * 实际耗时).
==> HFU 反映硬件做功有多忙碌, 但掩盖了算法层面的冗余重复做功!

模型浮点利用率 (Model FLOPs Utilization, MFU, Chowdhery et al., PaLM):
完全不考虑任何工程技巧与重复运算, 纯粹根据 Transformer 理论数学公式手算的
标准前向与反向最小必要浮点运算量 (每 Token 约 6P FLOPs)
除以 (P_peak * 实际耗时).
==> MFU 是衡量工程团队系统工程实力与代码效率的真正黄金标尺!
```

---

## 步骤 3：跨组件相互耦合机制

1. **激活重计算对 MFU 与 HFU 的撕裂：**  
   若开启全量重计算，芯片实际上多算了 2P 的前向 FLOPs。此时 Nsight 性能分析器测出的 **HFU 会虚高攀升**（比如显示 65%），但由于真实训练吞吐并没有变快，依据纯有效产出计算的 **MFU 可能只有 45%**！
2. **Nsight Systems (nsys) 性能剖析雷达：**  
   通过 Nsight 的时间线（Timeline），工程师能够一目了然看清：
   - 算子之间是否存在空闲的 CPU 调度间隙（CPU Launch Overhead）；
   - NCCL 集合通信是否被计算成功掩盖（Compute-Communication Overlap）；
   - 显存拷贝（HtoD / DtoH）是否阻塞了主计算流。

---

## 步骤 4：精确性能数学公式

对于含 $P$ 个非词表参数的模型，在包含 $N_{\text{gpus}}$ 张 GPU 的集群上训练：
- 理论上处理 1 个 Token 所需的标准最小浮点运算量为：
  $$\text{FLOPs per Token} = \underbrace{2 P}_{\text{Forward}} + \underbrace{4 P}_{\text{Backward}} = 6 P \text{ FLOPs}$$
  *(若考虑注意力机制，可微调为 $6 P + 12 L H S d$)*。

系统实测的吞吐量为每秒全局处理的 Token 数（记作 $T_{\text{tokens}}$，单位为 Tokens/second）：

系统实际实现的**有效计算吞吐（Achieved TFLOPS）**为：

$$\text{FLOPS}_{\text{achieved}} = \frac{T_{\text{tokens}} \times 6 P}{N_{\text{gpus}}}$$

由此，**模型浮点利用率（MFU）**的精准定义公式为：

$$\text{MFU} = \frac{\text{FLOPS}_{\text{achieved}}}{P_{\text{peak}}} = \frac{T_{\text{tokens}} \times 6 P}{N_{\text{gpus}} \times P_{\text{peak}}}$$

---

## 步骤 5：具体微基准数字推导

假设一个工程团队在 64 张 H100 SXM（每张卡 BF16 峰值算力 $P_{\text{peak}} = 1,000\text{ TFLOPS} = 10^{15}\text{ FLOPs/s}$）集群上全量训练 **LLaMA-3 70B**（有效非词表参数取 $P = 70 \times 10^9$）：
- 实测集群稳态训练吞吐量为：$T_{\text{tokens}} = 50,000 \text{ Tokens/second}$

### 步骤 5.1：计算单 Token 的标准理论有效运算量
$$\text{FLOPs per Token} = 6 \times 70 \times 10^9 = 4.2 \times 10^{11} \text{ FLOPs} = 420 \text{ GFLOPs}$$

### 步骤 5.2：计算集群实现的单卡平均有效算力
$$\text{单卡有效算力} = \frac{50,000 \times 4.2 \times 10^{11} \text{ FLOPs/s}}{64 \text{ 张卡}} \approx \frac{2.1 \times 10^{16}}{64} \approx 3.28 \times 10^{14} \text{ FLOPs/s} = 328 \text{ TFLOPS}$$

### 步骤 5.3：求解 MFU 真实硬件利用率
$$\text{MFU} = \frac{328 \text{ TFLOPS}}{1,000 \text{ TFLOPS}} = 32.8\%$$

### 步骤 5.4：优化后评估（工程团队重构内核后的进阶表现）
工程团队通过引入 FlashAttention-3、消除算子间隙并实施张量并行重叠，将吞吐推高至 $T_{\text{tokens}} = 85,000 \text{ Tokens/second}$：
$$\text{新单卡有效算力} = \frac{85,000 \times 4.2 \times 10^{11}}{64} \approx 557.8 \text{ TFLOPS}$$
$$\text{新 MFU} = \frac{557.8}{1000} \approx 55.8\%$$
- **商业与系统收益：** MFU 从 $32.8\%$ 跃升至 $55.8\%$，意味着在完全不增加一张硬件显卡的前提下，**模型预训练完成时间被硬生生砍掉了整整 $41\%$**，为公司直接节省数百万元的电费与算力账单！

---

## 步骤 6：核心系统工程铁律

> 离开 MFU 谈大模型算力优化，就像离开实际时速谈引擎转速一样虚无缥缈。MFU 是剥离了一切算法水分与工程假象之后，衡量系统是否真正把物理硅基芯片压榨至极限的唯一真理；突破 50% MFU，是大模型系统工程师从平庸迈向卓越的成人礼。
"""

# Labs
chinese_files["labs/01-roofline-profiler.zh.md"] = r"""# 实验 E1：GPU 屋顶模型性能分析器开发实战

## 实验目标与挑战

在实际工程开发中，90% 的初级工程师在遇到模型运行缓慢时，都会盲目尝试各种互不相干的优化技巧（量化、剪枝、多线程、重构循环）。但如果一个算子当前深陷在显存带宽受限区（Memory-Bound），任何尝试精简乘加代数运算的努力在物理上都是 100% 的无用功。

**本实验的核心任务：** 从零编写一个纯 Python/PyTorch 的轻量级**屋顶模型自动化性能分析器（Roofline Profiler）**。
1. 拦截目标 PyTorch 算子的底层前向执行；
2. 利用 `torch.profiler` 或底层计时器与张量形状推导，精确捕获其实际执行消耗的浮点运算量（FLOPs）与全局显存搬运字节数（Bytes）；
3. 计算该算子的实际算术强度 $I = \frac{\text{FLOPs}}{\text{Bytes}}$；
4. 结合当前物理 GPU（如 H100、A100 或 RTX 4090）的硬件规格，自动绘制该算子在屋顶模型性能边界上的真实坐标，并给出针对性的工业级架构优化建议。

---

## 动手实践代码框架

```python
import torch
import time

def profile_operator_roofline(op_fn, sample_inputs, hardware_peak_tflops, hardware_bw_gbs):
    # 分析指定算子的屋顶模型位置与瓶颈
    # hardware_peak_tflops: 物理单卡 FP16/BF16 峰值算力 (TFLOPS)
    # hardware_bw_gbs: 物理单卡显存吞吐带宽 (GB/s)
    # 1. 硬件平衡拐点
    hardware_balance_point = (hardware_peak_tflops * 1e12) / (hardware_bw_gbs * 1e9)
    print(f"[硬件诊断] 硬件临界拐点 I*: {hardware_balance_point:.2f} FLOPs/Byte")
    
    # 2. 预热 GPU
    for _ in range(10):
        _ = op_fn(*sample_inputs)
    torch.cuda.synchronize()
    
    # 3. 测量物理执行耗时
    start_event = torch.cuda.Event(enable_timing=True)
    end_event = torch.cuda.Event(enable_timing=True)
    
    iters = 100
    start_event.record()
    for _ in range(iters):
        _ = op_fn(*sample_inputs)
    end_event.record()
    torch.cuda.synchronize()
    
    avg_latency_ms = start_event.elapsed_time(end_event) / iters
    print(f"[基准测试] 实测平均延迟: {avg_latency_ms:.4f} ms")
    
    # 后续任务: 结合算子理论推导 FLOPs 与 Bytes，输出诊断报告!
```

---

## 思考与检验题
1. 运行实验代码测试 `torch.nn.functional.rms_norm`，观察其算术强度是否严格低于 2.0？
2. 增大批大小 $B$，观察标准矩阵乘法 `torch.matmul` 的算术强度是如何一步步从访存受限区跨越至计算受限区的。
"""

chinese_files["labs/02-flash-attention-kernel.zh.md"] = r"""# 实验 E2：极简 FlashAttention 算子实现（Triton / C++）

## 实验目标与挑战

在第 E03 和 E04 章中，我们深入剖析了 FlashAttention 的数学原理与硬件设计。没有亲自实现过在线 Softmax（Online Softmax）分块平铺的工程师，永远无法真正体会片上共享内存（SRAM）调度的精妙。

**本实验的核心任务：** 使用现代 GPU 算子编程语言 **OpenAI Triton**（或原生 CUDA/C++），从零实现一个极简但功能完全正确的 **FlashAttention 前向传播算子**。
1. 将输入的 $\mathbf{Q}, \mathbf{K}, \mathbf{V}$ 张量在序列维度切分成适配 SRAM 大小的局部 Tile（例如 $64 \times 64$ 或 $128 \times 64$）；
2. 在 Triton 核函数中，维护两个正在迭代更新的 SRAM 累加向量：行最大值向量 $m_i$ 与归一化分母向量 $l_i$；
3. 实现标准的在线 Softmax 缩放校正逻辑，将局部乘加结果累加至最终输出 $\mathbf{O}_i$；
4. 验证算子输出与 PyTorch 原生 `F.scaled_dot_product_attention` 的数值绝对对齐（误差控制在 $10^{-3}$ 以内）；
5. 与未熔合的朴素 PyTorch 注意力对比在长序列（$S = 2048, 4096, 8192$）下的显存占用峰值与执行速度。

---

## 动手实践代码框架

```python
import torch
import triton
import triton.language as tl

@triton.jit
def _flash_attn_fwd_kernel(
    Q, K, V, Out,
    stride_qz, stride_qh, stride_qm, stride_qk,
    stride_kz, stride_kh, stride_kn, stride_kk,
    stride_vz, stride_vh, stride_vn, stride_vk,
    stride_oz, stride_oh, stride_om, stride_ok,
    Z, H, N_CTX,
    BLOCK_M: tl.constexpr, BLOCK_DMODEL: tl.constexpr, BLOCK_N: tl.constexpr,
):
    # 识别当前线程块负责的批次、注意力头与序列行分块索引
    start_m = tl.program_id(0)
    offs_m = start_m * BLOCK_M + tl.arange(0, BLOCK_M)
    offs_n = tl.arange(0, BLOCK_N)
    offs_d = tl.arange(0, BLOCK_DMODEL)
    
    # 初始化在线 Softmax 状态变量 (位于片上寄存器)
    m_i = tl.zeros([BLOCK_M], dtype=tl.float32) - float("inf")
    l_i = tl.zeros([BLOCK_M], dtype=tl.float32)
    acc = tl.zeros([BLOCK_M, BLOCK_DMODEL], dtype=tl.float32)
    
    # 核心任务: 编写内层循环，流式分块加载 K 和 V，就地完成在线累加更新!
```

---

## 思考与检验题
1. 为什么在线 Softmax 必须在 FP32 精度下维护累加状态 $m_i$ 和 $l_i$，即使输入和输出都是 FP16/BF16？
2. 尝试在核函数中加入因果掩码（Causal Mask），观察为什么可以直接整块跳过右上角无用的 Tile，从而使运行时间瞬间减半？
"""

chinese_files["labs/03-paged-kv-cache.zh.md"] = r"""# 实验 E3：PagedAttention 分页 KV Cache 引擎从零开发实战

## 实验目标与挑战

vLLM 之所以能在一夜之间重塑全球大模型在线推理服务生态，其基石正是借鉴了经典操作系统的**分页虚拟内存（Virtual Paging）**思想。

**本实验的核心任务：** 使用纯 Python 与 NumPy/PyTorch，从零构建一个完整的 **PagedAttention 模拟管理引擎**。
1. **全局物理块管理器（Block Allocator）：** 预先分配一块固定的显存池，并切分为若干固定大小的物理 Block（例如每个 Block 存放 16 个 Token 的 KV 向量）；维护一个空闲链表（Free List）；
2. **逻辑到物理映射页表（Block Table）：** 为每个进入系统的并发请求维护一个动态增长的虚拟页表，支持随序列生成按需动态申请新的物理 Block；
3. **写时复制机制（Copy-on-Write）：** 模拟多分支采样场景（如束搜索 Beam Search），让子请求共享父请求的历史物理块，只有当发生分歧写入新 Token 时才触发物理块的深拷贝复制；
4. **并发压力模拟评测：** 模拟 100 条长短不一的随机请求并发到达，对比传统“静态连续预分配”与你的“分页引擎”在显存碎片浪费率与最大支持并发数上的物理表现。

---

## 动手实践代码框架

```python
class PhysicalBlockPool:
    def __init__(self, num_blocks, block_size, num_heads, head_dim):
        self.block_size = block_size
        self.free_blocks = list(range(num_blocks))
        # 预分配扁平物理显存池
        self.k_cache = torch.zeros(num_blocks, block_size, num_heads, head_dim)
        self.v_cache = torch.zeros(num_blocks, block_size, num_heads, head_dim)
        
    def allocate(self):
        if not self.free_blocks:
            raise MemoryError("物理显存池已耗尽 (OOM)!")
        return self.free_blocks.pop(0)

    def free(self, block_id):
        self.free_blocks.append(block_id)

class LogicalSequence:
    def __init__(self, seq_id, pool):
        self.seq_id = seq_id
        self.pool = pool
        self.block_table = []
        self.num_tokens = 0

    def append_token_kv(self, k_vec, v_vec):
        # 核心任务: 检查最后一个 Block 是否已满，若满则申请新 Block，并将 KV 写入物理显存池!
        pass
```

---

## 思考与检验题
1. Block 大小（`block_size`）对系统性能有什么物理影响？为什么主流系统大多选用 16 或 32，而不是 1 或 1024？
2. 在这个框架下，如何实现前缀缓存（Prefix Caching）的重用逻辑？
"""

chinese_files["labs/04-tensor-parallel-engine.zh.md"] = r"""# 实验 E4：双卡张量并行（Tensor Parallelism）引擎手工构建实战

## 实验目标与挑战

Megatron-LM 的“列切 + 行切”张量并行，是支撑超千亿大模型跨卡切分训练与推理的绝对主力支柱。

**本实验的核心任务：** 纯手工使用 PyTorch 集合通信库（`torch.distributed`），在两张物理 GPU（或通过单机多进程模拟多卡）上，完整构建一个标准 Transformer MLP 层的 **张量并行引擎**。
1. 实现 **列并行线性层（ColumnParallelLinear）**：将权重矩阵按列拆分，处理输入并验证其在不需要跨卡通信的前提下各自输出局部切片；
2. 实现 **行并行线性层（RowParallelLinear）**：将权重矩阵按行拆分，接收切片输入并计算出部分和（Partial Sum）；
3. 注入 `all_reduce` 集合通信：将两张卡计算出的部分和无缝累加聚合；
4. 编写全量对齐测试用例：在单卡上运行标准的非切分 MLP，将双卡并行引擎的最终输出与单卡标准答案进行绝对数值比对，证明两者的浮点输出完全严格对齐（误差控制在 $10^{-6}$ 以内）。

---

## 动手实践代码框架

```python
import os
import torch
import torch.distributed as dist
import torch.nn as nn

class ColumnParallelLinear(nn.Module):
    def __init__(self, in_features, out_features, world_size, rank):
        super().__init__()
        self.split_out_features = out_features // world_size
        self.weight = nn.Parameter(torch.empty(self.split_out_features, in_features))
        # 实际代码中需从完整权重中切取对应 rank 的切片
        
    def forward(self, x):
        # 列并行: 输入广播，各卡独立计算局部投影
        return torch.matmul(x, self.weight.t())

class RowParallelLinear(nn.Module):
    def __init__(self, in_features, out_features, world_size, rank):
        super().__init__()
        self.split_in_features = in_features // world_size
        self.weight = nn.Parameter(torch.empty(out_features, self.split_in_features))
        
    def forward(self, x):
        # 行并行: 接收切片，计算部分和，并通过 All-Reduce 聚合
        partial_sum = torch.matmul(x, self.weight.t())
        dist.all_reduce(partial_sum, op=dist.ReduceOp.SUM)
        return partial_sum
```

---

## 思考与检验题
1. 如果在列并行后加入偏置项（Bias），系统会出现什么数学错误？为什么偏置项通常只能安全地加在行并行的输出端？
2. 试推导在反向传播过程中，列并行与行并行各自分别会触发什么样的集合通信（提示：前向与反向在数学上互为对偶变换）？
"""

# Write all files to disk
written_count = 0
for rel_path, content in chinese_files.items():
    full_path = os.path.join(BASE_DIR, rel_path)
    os.makedirs(os.path.dirname(full_path), exist_ok=True)
    with open(full_path, "w", encoding="utf-8") as f:
        f.write(content.strip() + "\n")
    written_count += 1

print(f"Successfully scaffolded {written_count} Chinese engineering content files.")
