import os

BASE_DIR = os.path.abspath(os.path.join(os.path.dirname(__file__), "..", "docs"))

chapters = {}

# ==============================================================================
# Module 4: Distributed Systems & Parallel Scaling
# ==============================================================================

chapters["module-4/14-distributed-communication-primitives.zh.md"] = r"""# 第 E14 章：分布式集合通信原语：手拉手传数据与环状通信的数学之美

同学，你好！欢迎来到第四模块——分布式系统与大规模并行计算的壮丽殿堂！

在前面的课程中，我们所有的讨论都局限在单张显卡内部。但在这个千亿、万亿大模型的时代，没有任何一个顶尖模型能塞得进单张显卡里。

我们必须让几百张、几千张甚至上万张显卡并肩作战！  
而要让成千上万个大脑像交响乐团一样完美协同，最核心的命脉就是——**通信（Communication）**。

你可能经常听说 **All-Reduce**、**All-Gather**、**Reduce-Scatter** 这些高大上的名词。  
今天这堂课，老师就带你像指挥家一样，从最直观的“手拉手传水桶”开始，彻底揭开集合通信背后的数学之美！

---

## 步骤 1：物理直觉（操场围圈接力传水桶）

为了让你秒懂什么是分布式集合通信，老师把你和 3 位同学（共 4 个人，分别代表 GPU 0 到 GPU 3）叫到操场上，大家围成一个圆圈：

每个同学手里都端着一盆混有自己独特配料的水（每个 GPU 各自算出的梯度数据）：
1. **大家的目标（All-Reduce）：**  
   我们希望最终，**每一个同学手里的水盆里，都完完整整汇集了所有 4 个人的全部配料总和！**
2. **愚蠢的中心化做法（班长累死）：**  
   大家都把水端给站在中间的班长，班长把 4 盆水倒在一个大缸里搅拌好，再分成 4 份重新分发给大家。  
   - 结果：班长当场累垮（单点网络拥堵严重爆卡），而其他人只能在旁边插兜干瞪眼。
3. **聪明的环状接力做法（Ring All-Reduce）：**  
   大家手拉手围成一个环，每个人只跟自己右边的邻居说话！
   - **第一轮：散播汇总（Reduce-Scatter）**  
     大家把自己盆里的水切成 4 小碗，顺时针递给右边的人。每次接过来，就把邻居的水和自己的水倒在一起，再接着往右递！转满一圈后，**每个人手里都刚好有一份汇集了全员力量的完美切片！**
   - **第二轮：全员同步（All-Gather）**  
     大家再次顺时针递水，把刚才算好的完美切片广播给右边的人！又转了一圈后，**所有同学的手里都同时拥有了全部的完整混合配料！**

你看，**没有任何一个人是瓶颈，每个人的双手一刻不停地在收发，网络带宽被 100% 榨干拉满！** 这就是环状通信的极致优雅。

---

## 步骤 2：芯片微观底层执行机制

在 NVIDIA NCCL（集合通信库）底层，现代多卡集群主要通过两条物理高速公路连接：
1. **机内（Intra-Node）：NVLink**（双向带宽高达 $900 \text{ GB/s}$，比普通网线快十倍以上）；
2. **机间（Inter-Node）：InfiniBand / RoCE**（通过高速交换机，单网卡通常为 $400 \text{ Gbps} = 50 \text{ GB/s}$）。

```
【三大核心集合通信原语拓扑流向】

1. Reduce-Scatter (归约散播):
输入: 每个 GPU 各自持有大小为 S 的张量
过程: 节点间协同求和，并将最终求和结果均分切片
输出: 每个 GPU 仅保留求和后的 1/N 切片 (体积缩小为 S / N)

2. All-Gather (全员搜集广播):
输入: 每个 GPU 各自持有大小为 S / N 的局部碎片
过程: 环状轮转广播，互相搜集彼此的碎片
输出: 每个 GPU 都重新拼装出大小为 S 的完整张量

3. All-Reduce (全员归约同步):
本质就是前两者的串联组合: All-Reduce = Reduce-Scatter + All-Gather!
输入: 每个 GPU 持有独立张量 S
输出: 所有 GPU 都获得累加求和后的完整张量 S!
```

### Ring All-Reduce 的分块接力步骤：
将大小为 $M$ 字节的张量切分为 $N$ 个均等分块（$N$ 为 GPU 数量）：
- **Reduce-Scatter 阶段：** 经历 $N-1$ 步环状顺时针传递与就地累加，每步每张卡发送和接收 $\frac{M}{N}$ 字节；
- **All-Gather 阶段：** 经历 $N-1$ 步环状顺时针广播与覆盖，每步每张卡发送和接收 $\frac{M}{N}$ 字节。

---

## 步骤 3：跨组件相互耦合机制

1. **分布式并行的通用基石：**  
   - 在数据并行（DDP）中，每次反向传播结束时，都需要一次全卡 **All-Reduce** 同步梯度；
   - 在张量并行（Megatron TP）中，每个 Transformer 层的注意力与 FFN 内部都需要执行 **All-Reduce**；
   - 在 ZeRO-3 / FSDP 中，前向计算前用 **All-Gather** 临时拼出权重，反向计算后用 **Reduce-Scatter** 散播梯度。
2. **通信开销与集群规模（$N$）解耦的奇迹：**  
   请注意接下来的数学推导！环状算法最令人震撼的特性在于：**无论集群扩展到 8 卡、64 卡还是 1024 卡，每张显卡需要传输的数据总量几乎是恒定的！**

---

## 步骤 4：精确性能数学公式

老师带你推导 Ring All-Reduce 的传输总量公式：

每张卡在整整两阶段中，总共经历步数为：

$$\text{总步数} = (N - 1) + (N - 1) = 2(N - 1)$$

每一步中，每张卡向右邻居发送的字节数为 $\frac{M}{N}$。因此，每张卡发送的总网络流量为：

$$\text{单卡总传输字节数} = 2(N - 1) \times \frac{M}{N} = 2 \left(\frac{N - 1}{N}\right) M \text{ 字节}$$

当 GPU 节点数量 $N$ 很大时（如 $N \ge 8$）：

$$\frac{N - 1}{N} \approx 1 \implies \text{单卡总传输量} \approx 2M \text{ 字节}$$

**老师敲黑板划重点：**  
这意味着：**同步一个大小为 $M$ 的张量，每张显卡只需要在网线上发送大约 $2M$ 的数据，与显卡总数量 $N$ 几乎完全无关！**  
通信总耗时（设链路单向带宽为 $B$）：

$$T_{\text{ring}} = 2 \left(\frac{N - 1}{N}\right) \frac{M}{B} + 2(N - 1) \times \alpha$$

（其中 $\alpha$ 为网络握手的延迟开销）。

---

## 步骤 5：具体微基准数字推导

我们来算一算在真实的 **8 卡 H100 节点（NVLink 带宽 $B = 450 \text{ GB/s}$ 单向）** 上同步 **LLaMA-3 70B** 梯度的耗时！
- 模型参数量：$70 \times 10^9$
- 梯度精度：BF16（每参数 2 字节）
- 待同步的总梯度大小：$M = 70 \times 10^9 \times 2 = 140 \times 10^9 \text{ 字节} = 140 \text{ GB}$
- 显卡数量：$N = 8$

### 1. 计算单卡在 Ring All-Reduce 中需搬运的网络数据量
$$\text{Bytes} = 2 \times \left(\frac{8 - 1}{8}\right) \times 140 \text{ GB} = 2 \times \frac{7}{8} \times 140 = 1.75 \times 140 = \mathbf{245 \text{ GB}}$$

### 2. 计算在 NVLink 高速公路上的纯传输耗时
$$T_{\text{comm}} = \frac{245 \times 10^9 \text{ 字节}}{450 \times 10^9 \text{ 字节/秒}} \approx 0.544 \text{ 秒} = \mathbf{544 \text{ 毫秒}}$$

**老师带你分析：**  
在机内 NVLink 的极速互联下，半秒钟就能将 700 亿庞大参数的梯度在 8 张卡间完全同步一致！  
但如果这 8 张卡是通过慢速网线（如 PCIe 64 GB/s）连接，这个时间就会暴涨到近 **4 秒钟**，导致所有卡都在苦苦等网络，整个训练系统彻底停摆！

---

## 步骤 6：核心系统工程铁律

> **分布式系统的灵魂在于环形流水与去中心化。**  
> 深刻理解 All-Reduce = Reduce-Scatter + All-Gather 的二段式解构。用环状拓扑抹平节点规模差异，用高带宽硬件互联填补通信鸿沟，是大规模分布式集群高效运转的基石。
"""

chapters["module-4/15-tensor-parallelism-megatron.zh.md"] = r"""# 第 E15 章：张量并行（Megatron-LM）底层实现：横切纵切与每层两次 All-Reduce 的优雅之美

同学，你好！在掌握了集合通信原语后，今天老师要带你学习千亿大模型切分的最强神兵——**张量并行（Tensor Parallelism，简称 TP）**。

这是由 NVIDIA 的 Megatron-LM 团队在 2019 年发明并奠定行业标准的划时代架构。

你可能会纳闷：“老师，我们为什么非要费尽心思把一个大矩阵切成碎片扔进不同的显卡呢？直接一张卡算完不行吗？”  
答案很残酷：**单张显卡根本装不下！** 就算装得下，单卡的算力也不足以支撑低延迟的吐字生成。

那么，如何把矩阵切得既工整、又能把通信次数压缩到极致呢？今天老师就带你领略这套“一横一竖”的切分艺术！

---

## 步骤 1：物理直觉（木匠分头锯大木梁）

老师给你打个锯木头的生动比方：

现在工地上有一根长达 8 米的巨型木梁，需要经过两道工序加工：**先纵向刨花升维（第一级线性投影），再横向压实成型（第二级线性投影）**。
- **笨做法（频繁打电话核对）：**  
  两个木匠每刨一刀，就必须停下手里的活，打电话向对方汇报自己刨了多少毫米（频繁网络同步），干活 1 分钟，打电话 10 分钟。
- **Megatron 的绝妙切法（默契分工）：**  
  - **第一道工序（列并行 Column Parallel）：** 老师把木梁**竖着切成左右两半**，木匠 A 只管刨左半边，木匠 B 只管刨右半边。在整个刨花过程中，**两个人完全不需要说一句话（0 通信！）**，各自独立全力输出！
  - **第二道工序（行并行 Row Parallel）：** 接着把成型的模具**横着切开**。木匠 A 和木匠 B 各自将自己手头的半成品直接加工成最终尺寸的一半。  
  - **最后交差关头：** 两人只需在走廊碰一次头，把各自手头的一半成品拼在一起（仅需一次 All-Reduce 加法！），一张完美的整梁立刻交付！

你看，整整两道复杂的矩阵工序，**中间全程零交流，只在最后收官时碰一次头！**

---

## 步骤 2：芯片微观底层执行机制

在 Transformer 的核心结构中，主要包含两对线形层组合：
1. **多头注意力模块：** $Q, K, V$ 投影输入 $\to$ 注意力计算 $\to$ 输出投影 $O$；
2. **前馈网络模块：** Gate / Up 投影升维 $\to$ 激活相乘 $\to$ Down 投影降维。

Megatron-LM 设计了极其优雅的**列并行（Column Parallel）+ 行并行（Row Parallel）配对法则**：

```
【前馈网络 FFN 的 TP 切分全景流程 (以 2 卡为例: GPU 0 与 GPU 1)】

输入 X (无需切分, 两张卡持有完整的 X)
   │
   ├──────────────────────────────┬──────────────────────────────┐
   ▼                              ▼
[GPU 0: 列并行 Column Parallel]   [GPU 1: 列并行 Column Parallel]
权重切为左半块 W1_left [d x d_ffn/2]  权重切为右半块 W1_right [d x d_ffn/2]
在本地计算: Y0 = X @ W1_left         在本地计算: Y1 = X @ W1_right
在本地执行激活: Z0 = GeLU(Y0)        在本地执行激活: Z1 = GeLU(Y1)
(注意: 这一步完全在片上独立进行，两张卡之间完全不需要任何通信!)
   │                              │
   ▼                              ▼
[GPU 0: 行并行 Row Parallel]      [GPU 1: 行并行 Row Parallel]
权重切为上半块 W2_top [d_ffn/2 x d]   权重切为下半块 W2_bottom [d_ffn/2 x d]
在本地计算局部矩阵乘:                 在本地计算局部矩阵乘:
Out0 = Z0 @ W2_top                    Out1 = Z1 @ W2_bottom
   │                              │
   └──────────────┬───────────────┘
                  ▼
   【全卡执行单次 All-Reduce 集合通信!】
   最终输出 Out = Out0 + Out1 = X @ W1 @ W2 (数学逻辑绝对等价!)
```

### 注意力模块的天然切分：
在注意力模块中，由于有多个独立的注意力头（Heads），切分更加浑然天成：
- **$Q, K, V$ 投影采用列切分：** GPU 0 分配前一半头，GPU 1 分配后一半头；
- **各头在本地独立计算局部 FlashAttention：** 彼此完全不需要交换信息；
- **$O$ 投影采用行切分：** 各自将局部的头投影输出，最后通过**一次 All-Reduce** 完成合并。

---

## 步骤 3：跨组件相互耦合机制

1. **严格受制于机内 NVLink 的极高带宽：**  
   因为每一个 Transformer 层都硬性嵌入了 2 次 All-Reduce 通信（注意力 1 次 + FFN 1 次），**张量并行的大小（TP Degree）通常绝不超过单台机架内的 8 张卡**！如果跨机跑 TP，慢速的网卡延迟会瞬间将整个系统的速度拖垮 80% 以上。
2. **注意力头数（Heads）的整除硬约束：**  
   模型的总注意力头数 $H_q$ 和 KV 头数 $H_{\text{kv}}$ 必须能被张量并行卡数 $TP$ 整除。例如 GQA 只有 8 个 KV 头，那么该模型在无额外复杂处理下，最高只能开到 $TP = 8$。

---

## 步骤 4：精确性能数学公式

对于一个隐藏层维度为 $d$、FFN 维度为 $d_{\text{ffn}}$ 的大模型，当采用张量并行度为 $P_{\text{tp}}$ 时：

### 1. 单卡计算与显存参数分摊
每张卡上持有的权重参数量降为原本的：

$$\text{单卡参数量} = \frac{\text{总参数量}}{P_{\text{tp}}}$$

### 2. 每个 Transformer 层的通信量
每个层需要经历 **2 次 All-Reduce** 操作（每次传递大小为 $B \times S \times d$ 的激活张量）：

$$\text{单层前向通信字节数} = 2 \times \text{All-Reduce}(B \times S \times d \times 2 \text{ 字节})$$

代入 Ring All-Reduce 公式：

$$\text{单层单卡前向通信量} \approx 2 \times \left[ 2 \left(\frac{P_{\text{tp}}-1}{P_{\text{tp}}}\right) B S d \times 2 \right] = 8 \left(\frac{P_{\text{tp}}-1}{P_{\text{tp}}}\right) B S d \text{ 字节}$$

---

## 步骤 5：具体微基准数字推导

我们以 **LLaMA-3 70B**（80 层，$d = 8192$），在 8 卡 H100 节点上开启 $TP = 8$ 处理长文本（$B=1, S=4096$，精度 BF16）为例：

### 1. 计算单层前向单卡的通信数据量
- 激活张量尺寸：$1 \times 4096 \times 8192 \times 2 \text{ 字节} \approx 67.1 \text{ MB}$
- 每次 All-Reduce 传输量：
  $$2 \times \frac{8 - 1}{8} \times 67.1 \text{ MB} = 1.75 \times 67.1 \approx 117.4 \text{ MB}$$
- 单层有 2 次通信：$2 \times 117.4 \text{ MB} \approx 235 \text{ MB}$

### 2. 计算全模型 80 层的累积通信开销
- 全模型前向总通信数据量：
  $$\text{总通信量} = 80 \times 235 \text{ MB} \approx 18.8 \text{ GB}$$
- 在 H100 SXM NVLink（单向带宽 $450\text{ GB/s}$）上的纯传输时间：
  $$T_{\text{tp\_comm}} = \frac{18.8 \times 10^9 \text{ 字节}}{450 \times 10^9 \text{ 字节/秒}} \approx 0.0418 \text{ 秒} = \mathbf{41.8 \text{ 毫秒}}$$

**老师总结分析：**  
41.8 毫秒！在强大的 NVLink 支撑下，8 张卡紧密协作跑完 80 层 70B 大模型，通信开销被牢牢压制在可控范围内，单步生成速度依然能够飞快如风！

---

## 步骤 6：核心系统工程铁律

> **列切与行切成对配对，把通信锁在层与层的边界上。**  
> 永远利用线性层之间的非线性激活函数隔离通信。张量并行是机内卡间并行的第一法宝，坚决限制在 NVLink 高速机架内使用，绝不让高频 All-Reduce 跨越慢速机间网络。
"""

chapters["module-4/16-pipeline-parallelism-1f1b.zh.md"] = r"""# 第 E16 章：流水线并行与 1F1B 调度算法：消灭等待气泡与激活值显存的完美平衡

同学，你好！在上一节课中，我们学习了张量并行（TP）。但老师提到过：TP 因为通信极其频繁，只能在单台机器内部的 8 张卡之间施展。

如果我们的模型是 4050 亿参数的超超级大巨兽（比如 LLaMA-3 405B），需要动用 64 张甚至 512 张显卡，跨越几十个机柜，**这时候该怎么切分模型呢？**

今天，老师就带你认识大规模分布式集群的另一大台柱子——**流水线并行（Pipeline Parallelism，简称 PP）**，以及彻底改变了流水线效率的传奇调度算法——**1F1B（One Forward, One Backward）**！

---

## 步骤 1：物理直觉（汽车工厂装配流水线）

老师带你参观一家大型汽车制造流水线车间：

全厂有 4 个连续车间（代表 4 张跨机柜的 GPU）：
- 车间 1：负责组装底盘（模型第 1~8 层）；
- 车间 2：负责安装发动机和传动系统（第 9~16 层）；
- 车间 3：负责焊接车身和车门（第 17~24 层）；
- 车间 4：负责喷漆和质检（第 25~32 层）。

### 朴素流水线的巨大悲剧（可怕的“气泡 Bubble”）：
厂长早上只运来了一辆大卡车（单一大批次）：
- 车间 1 忙活时，车间 2、3、4 的工人们全都在插兜烤火；
- 车间 2 忙活时，车间 1、3、4 都在睡觉打呼噜……
- 整条流水线有 **75% 的时间都在闲置等待！** 工人闲着没事干的这种虚无时间，在工程上被称为**“流水线气泡（Pipeline Bubble）”**！

### 1F1B 的聪明调度（有节奏地穿插生产）：
厂长灵机一动：我们不要送整辆大车，而是把订单切成几十辆微型小汽车（**微批次 Micro-batches**）！
- 小车 1、2、3、4 像贪吃蛇一样源源不断开进流水线；
- 当车间 4 做完小车 1 的前向装配后，立刻开始做小车 1 的反向质检；
- **最绝妙的 1F1B 节奏来了：**  
  每个工位**做完一个前向任务（One Forward），立刻紧接着做一个反向任务（One Backward）**！
- 就像踩缝纫机一样，节奏紧密交织，工人们再也不用傻站着等！

---

## 步骤 2：芯片微观底层执行机制

我们把经典的 **1F1B（One Forward, One Backward）稳定稳态时序图** 画在黑板上（以 4 级流水线 $PP=4$ 为例）：

```
阶段 1: 预热阶段 (Warmup)       阶段 2: 1F1B 稳态运行 (Steady State)           阶段 3: 收尾阶段 (Cooldown)
GPU 3:          [F1][F2][F3][F4] ──► [B1][F5][B2][F6][B3][F7][B4] ────────► [B5][B6][B7][B8]
GPU 2:      [F1][F2][F3][F4] ──────► [F5][B1][F6][B2][F7][B3][B4] ────────► [B5][B6][B7][B8]
GPU 1:  [F1][F2][F3][F4] ──────────► [F5][B1][F6][B2][F7][B3][B4] ────────► [B5][B6][B7][B8]
GPU 0: [F1][F2][F3][F4] ───────────► [F5][B1][F6][B2][F7][B3][B4] ────────► [B5][B6][B7][B8]
       ◄── 气泡时间 ──►                                                  ◄── 气泡时间 ──►
```

### 为什么 1F1B 能够拯救显存？
在反向传播计算梯度时，必须用到前向传播保存的激活值。  
- 如果像传统的批处理那样把所有前向做完再做反向，显存里必须同时堆积全部微批次的激活值，显存当场爆炸！
- 在 **1F1B 调度下**，每完成一个小批次的反向（Backward），该批次占用的前向激活值显存就**当场释放销毁**！
- 因此，任何一个 GPU 显卡内部积压的未释放微批次数量，**被严格锁定在至多等于流水线级数 $PP$ 个以内！** 显存曲线被彻底压平拉直！

---

## 步骤 3：跨组件相互耦合机制

1. **跨机通信量极小（仅传递点对点激活值）：**  
   流水线并行相邻两级之间，只需要在网线上发送当前微批次的边界激活张量（Point-to-Point 通信），而不需要像 TP 那样全员广播。因此，**PP 非常适合跨越较慢的机架交换机网络**。
2. **气泡率对微批次数量（$M$）的依赖：**  
   流水线级数 $PP$ 越大，前后空等的“气泡”占比就越大。为了把气泡稀释到可以忽略的程度，训练时必须让微批次数量 $M$ 远大于流水线级数（通常要求 $M \ge 4 \times PP$）。

---

## 步骤 4：精确性能数学公式

设流水线级数为 $P_{\text{pp}}$，微批次总数为 $M$。假定前向单步耗时为 $t_f$，反向单步耗时通常为前向的 2 倍（$t_b = 2 t_f$）。

单微批次全流水总工作时间为：

$$T_{\text{work}} = M \times (t_f + t_b) = 3 M t_f$$

整个流水线在起步（预热）和收尾时的纯闲置空转总时间（气泡时间）为：

$$T_{\text{bubble}} = (P_{\text{pp}} - 1) \times (t_f + t_b) = 3 (P_{\text{pp}} - 1) t_f$$

流水线空转气泡率（Bubble Fraction，记作 $F_{\text{bubble}}$）的严密公式为：

$$F_{\text{bubble}} = \frac{T_{\text{bubble}}}{T_{\text{work}} + T_{\text{bubble}}} = \frac{(P_{\text{pp}} - 1)}{M + (P_{\text{pp}} - 1)}$$

**老师划重点：**  
当微批次数量 $M$ 足够大（例如 $M = 8 \times P_{\text{pp}}$）时：

$$F_{\text{bubble}} \approx \frac{P_{\text{pp}}}{8 P_{\text{pp}}} \approx 12.5\%$$

大部分宝贵算力都被成功抢夺了回来！

---

## 步骤 5：具体微基准数字推导

我们来算一算 **405B 超大模型** 在 64 台机器上开启流水线并行时的真实气泡损失！

设置参数：
- 流水线级数：$P_{\text{pp}} = 8$（整机切为 8 个大阶段）
- 全局大批次被切分成：$M = 32$ 个微批次

### 1. 计算理论气泡空转占比
$$F_{\text{bubble}} = \frac{8 - 1}{32 + 8 - 1} = \frac{7}{39} \approx \mathbf{17.9\%}$$
这意味着整套庞大的集群中，有 17.9% 的时间显卡在苦等工件送过来。

### 2. 计算 1F1B 带来的显存上限约束
在稳态运行中，单卡需要保存的激活值微批次数量上限为：

$$\text{最大保留批次数} = P_{\text{pp}} = 8 \text{ 份}$$

如果改用老式非 1F1B 调度，单卡必须在显存中同时保存 $M = 32$ 份完整的激活值！  
显存占用足足翻了 **4 倍**，80GB 显卡必定当场 OOM 暴毙！

---

## 步骤 6：核心系统工程铁律

> **用微批次喂饱流水线，用 1F1B 筑牢显存安全堤坝。**  
> 流水线并行是跨机柜切分超千亿参数的核心骨架。永远维持微批次数量与流水线级数的大比例，在微步交替中即时释放前向激活值，是化解流水线气泡与显存爆仓的不二法门。
"""

chapters["module-4/17-zero-fsdp-data-parallelism.zh.md"] = r"""# 第 E17 章：数据并行与零冗余优化（ZeRO / FSDP）：显存 16P 负担切分与无界扩展

同学，你好！在掌握了张量并行与流水线并行之后，今天老师要带你攻破大模型预训练中最普及、最具革命性的技术——**零冗余优化器（ZeRO，Zero Redundancy Optimizer）** 与 **FSDP（Fully Sharded Data Parallel，全分片数据并行）**！

你可能经常听人说：“以前训练模型，大家卡里都死死存着一份一模一样的模型权重；而微软的 ZeRO 一出来，直接把这层冗余给‘切碎均摊’了，显存直接释放了四分之三！”

你可能会好奇：“老师，为什么传统的训练会有那么大显存冗余？所谓的 **ZeRO-1、ZeRO-2、ZeRO-3** 到底分别切碎了什么？多出来的通信开销又是怎么回事？”

今天，老师就带你拿起手术刀，把大模型训练时的“显存大账本”彻底切开！

---

## 步骤 1：物理直觉（四人合租买一套大百科全书）

老师打一个大学生宿舍合租买书的比方：

宿舍里住了 4 位同学（4 张 GPU），大家都在准备考研（训练模型），需要天天翻阅一套厚达 80 卷的《中国大百科全书》（模型参数与优化器）：
- **传统数据并行（DDP，极度土豪且拥挤）：**  
  每个同学都花巨资买了一整套完整的 80 卷大百科全书，塞在自己的小书桌上。  
  - 结果：4 个人各自的书桌全被塞爆了（单卡显存爆满），连放笔记本和咖啡杯的空都没有，稍微多印一卷书全宿舍都得退学（OOM 崩溃）。
- **ZeRO-3 / FSDP（共享借阅，各管一摊）：**  
  4 位同学商量决定：我们每个人只买其中的 20 卷！
  - 同学 A 负责保管第 1~20 卷，同学 B 保管 21~40 卷，以此类推；
  - 今天复习第 1 章时，同学 A 扯开嗓子把前 20 卷的内容大声念给大家听（**All-Gather 广播**），大家听完记在草稿纸上算题；
  - 算完这一章，**大家当场把草稿纸撕掉扔了**！
  - 接着复习第 2 章时，轮到同学 B 念书……

你看，**每个人的书桌瞬间空出了整整 75% 的空间！原本装不下的超级大书，现在轻轻松松在合租宿舍里读完了！**

---

## 步骤 2：芯片微观底层执行机制

在混合精度预训练中（以 FP16/BF16 训练、AdamW 优化器为例），一个参数到底会产生多少显存负担？老师带你算这笔著名的 **16P 显存大账本**：

对于一个拥有 $\Phi$ 个参数（Parameters）的大模型：
1. **模型参数（P）：** BF16 格式，占 $2\Phi$ 字节；
2. **反向传播梯度（G）：** BF16 格式，占 $2\Phi$ 字节；
3. **AdamW 优化器状态（O）：** 必须用 FP32 保证数值稳定：
   - FP32 主参数备份：$4\Phi$ 字节；
   - FP32 动量一阶矩 Momentum：$4\Phi$ 字节；
   - FP32 方差二阶矩 Variance：$4\Phi$ 字节；
   - 优化器状态合计：$12\Phi$ 字节！

$$\text{静态训练总显存} = 2\Phi (\text{参数}) + 2\Phi (\text{梯度}) + 12\Phi (\text{优化器}) = \mathbf{16\Phi \text{ 字节！}}$$

```
【ZeRO 家族的三级切片手术刀】

传统 DDP (无分片):
每张卡独立持有: [ 12Φ 优化器状态 ] [ 2Φ 梯度 ] [ 2Φ 参数 ] = 16Φ 纯冗余!

ZeRO-Stage 1 (仅分片优化器状态):
每张卡持有: [ 12Φ / N 优化器状态 ] [ 2Φ 梯度 ] [ 2Φ 参数 ]
==> 显存立减至: 4Φ + 12Φ / N (当 N=8 时，显存降为 5.5Φ，立省 65%!)
==> 额外通信代价: 0! (通信量与传统 DDP 完全相同)

ZeRO-Stage 2 (分片优化器 + 分片梯度):
每张卡持有: [ 12Φ / N 优化器状态 ] [ 2Φ / N 梯度 ] [ 2Φ 参数 ]
==> 显存降至: 2Φ + 14Φ / N (当 N=8 时，显存降为 3.75Φ，立省 76%!)
==> 额外通信代价: 0! (仅将反向 All-Reduce 换为 Reduce-Scatter)

ZeRO-Stage 3 / FSDP (全分片: 优化器 + 梯度 + 模型参数全切碎!):
每张卡仅持有: [ 16Φ / N ] !
==> 显存降至原来的 1 / N ! 显存容量天花板被彻底打破!
==> 额外通信代价: 前向多一次 All-Gather 广播参数，反向多一次 All-Gather 广播参数 (通信量增加 50%)。
```

---

## 步骤 3：跨组件相互耦合机制

1. **以 50% 额外通信换取无界显存：**  
   ZeRO-3 在前向传播每一层时，必须先通过 All-Gather 把切碎的权重即时拼出来，算完当前层立刻销毁；在反向传播时，再 All-Gather 拼一次算梯度，算完立刻销毁，并用 Reduce-Scatter 散播梯度。这一过程比传统 DDP 增加了 50% 的网络流量。
2. **预取与计算完全重叠（Prefetch Overlap）：**  
   现代 FSDP 实现的精髓在于：**当当前层正在进行前向计算时，后台的通信流已经在悄悄向网线索取下一层的切片参数了（Prefetch）**！只要计算时间大于通信时间，这 50% 的额外网络开销就会被**完全隐藏在物理时间之外**！

---

## 步骤 4：精确性能数学公式

设模型参数量为 $\Phi$，集群 GPU 卡数为 $N$：

### 1. 单卡静态显存占用对比公式
- 传统 DDP：$M_{\text{DDP}} = 16 \Phi$ 字节
- ZeRO-1：$M_{\text{ZeRO-1}} = 4\Phi + \frac{12\Phi}{N}$ 字节
- ZeRO-2：$M_{\text{ZeRO-2}} = 2\Phi + \frac{14\Phi}{N}$ 字节
- ZeRO-3 / FSDP：$M_{\text{ZeRO-3}} = \frac{16\Phi}{N}$ 字节

### 2. 单步训练通信总量对比（以单向网络传输度量）
- 传统 DDP / ZeRO-1 / ZeRO-2：单步仅需同步梯度一次（All-Reduce），总传输量为：
  $$\text{Comm}_{\text{DDP}} = 2 \Phi \text{ (反向 Reduce-Scatter)} + 0 = 2 \Phi \text{ 字节}$$
- ZeRO-3 / FSDP：
  $$\text{Comm}_{\text{ZeRO-3}} = \underbrace{\Phi}_{\text{前向 All-Gather}} + \underbrace{\Phi}_{\text{反向 All-Gather}} + \underbrace{\Phi}_{\text{反向 Reduce-Scatter}} = 3 \Phi \text{ 字节}$$

**通信量增加比率：** $\frac{3\Phi - 2\Phi}{2\Phi} = 50\%$。

---

## 步骤 5：具体微基准数字推导

我们来为训练 **LLaMA-3 70B**（$\Phi = 70 \times 10^9$）算一笔震撼人心的显存账本！

单卡物理容量：NVIDIA H100 SXM（80 GB 显存）。
全模型 16P 静态显存底座：
$$16 \Phi = 16 \times 70 \times 10^9 = 1,120 \times 10^9 \text{ 字节} = \mathbf{1,120 \text{ GB}!}$$

### 1. 尝试使用传统数据并行（DDP）：
每张卡必须独立吃下整整 $1,120 \text{ GB}$！单卡才 80 GB，直接超额 **14 倍**！连 1 步都跑不起来！

### 2. 启用 ZeRO-3 / FSDP（部署在 64 张 H100 集群上，$N = 64$）：
每张卡均摊分配 16P 静态负担：

$$\text{单卡静态显存} = \frac{1,120 \text{ GB}}{64} = \mathbf{17.5 \text{ GB}!}$$

**老师带你看奇迹：**  
原本需要 1120 GB 的巨无霸，被 64 张卡均匀拆分后，单卡竟然只占用了区区 **17.5 GB**！  
在 80GB 的显卡上，不仅装得下，还剩下了整整 **62.5 GB** 的超大空间，用于尽情放置长文本和更大 Batch 的激活值！

---

## 步骤 6：核心系统工程铁律

> **把静止的冗余切成碎片，用动态的流动换取空间。**  
> ZeRO/FSDP 是打通参数规模与物理卡数限制的最优解。在高速网络环境下，毫不犹豫开启权重与优化器全分片，通过通信与计算流水线重叠隐藏开销，让千亿模型在通用多卡集群上自由呼吸。
"""

chapters["module-4/18-ring-attention-context-parallelism.zh.md"] = r"""# 第 E18 章：上下文与序列并行（Ring Attention）：环状接力打破百万长文本单卡显存屏障

同学，你好！在第四模块的最后一课，老师要带你领略当今大模型最前沿的长文本神技——**Ring Attention（环状注意力，序列/上下文并行）**！

你可能在科技新闻里见过：“某某前沿大模型支持 100 万甚至 1000 万超长 Token 上下文！”

你可能会好奇：“老师，前面我们不是推导过吗？哪怕用上了 FlashAttention，上百万 Token 的 Query、Key、Value 向量光是加载进单卡显存，就会彻底挤爆显卡！**他们到底是用什么魔法，把上百万字的小说切给几十张显卡同时算注意力的呢？**”

答案就在于 UC 伯克利团队发明的这套极其天才的环状接力算法——**Ring Attention**！

---

## 步骤 1：物理直觉（传阅信件的读书俱乐部）

老师打一个读书沙龙的比方：

现在有 4 位读者（4 张 GPU），手头有一本长达 100 万字的历史巨著，被平均切成了 4 卷：
- 读者 0 拿着第 1 卷（第 0~25 万字）；
- 读者 1 拿着第 2 卷（第 25~50 万字）；
- 读者 2 拿着第 3 卷（第 50~75 万字）；
- 读者 3 拿着第 4 卷（第 75~100 万字）。

现在，每个人都要为自己手头这一卷书，写出针对**全书所有内容**的读后感（计算完整全局注意力）：
- **读者们怎么办？围成一个圆圈！**
  1. 每个人把自己的提问草稿（Query）稳稳当当地放在自己面前的书桌上，**一动不动**；
  2. 每个人把手头的历史参考档案（Key/Value）复印一份，**顺时针递给右边的同学**；
  3. 当右边的档案送过来时，读者立刻在自己桌上把自己的 Query 和送来的 Key/Value 做交叉对比，并利用我们在第 E04 章学过的 **在线 Softmax 修正公式**，当场融入当前的读后感中！
  4. 算完这一轮，大家再顺时针递给下一位……

转满一整圈后，**每个人都稳稳坐在自己的位子上，却相当于完完整整把全书 100 万字的所有章节逐一审阅了一遍！**

---

## 步骤 2：芯片微观底层执行机制

Ring Attention（Liu et al., 2023）的核心硬件执行机制为：**将序列维度（Sequence Dimension）沿集群 GPU 数量 $N$ 均匀切分，形成环状通信管道！**

```
【Ring Attention 环状执行数据流 (以 4 张 GPU 为例)】

初始状态:
  GPU 0 持有: Q0, K0, V0 (长序列的前 1/4)
  GPU 1 持有: Q1, K1, V1
  GPU 2 持有: Q2, K2, V2
  GPU 3 持有: Q3, K3, V3

执行第 0 步 (本地即时计算):
  各卡在片上计算局部 FlashAttention:
  GPU 0: Attn(Q0, K0, V0) | GPU 1: Attn(Q1, K1, V1) ...

执行第 1 步 (计算与通信完全异步重叠!):
  在后台通过点对点异步通信: K, V 顺时针旋转传递给下一张卡!
  GPU 0 收到 K3, V3 ──► 在前台计算 Attn(Q0, K3, V3) 并在线更新局部输出 O0!
  GPU 1 收到 K0, V0 ──► 在前台计算 Attn(Q1, K0, V0) 并在线更新局部输出 O1!

执行第 2 步与第 3 步:
  持续旋转传递，经过 N-1 步循环后，
  所有 GPU 上的输出向量 O 自动收敛为全序列真实注意力的数学精确解!
```

### 为什么它能实现零通信等待？
因为单块切片的注意力计算时间通常在数十毫秒级别，而通过高速网卡发送一份局部的 $K, V$ 切片只需要几毫秒。  
**传输下一块数据的耗时，被完全包庇、掩盖在当前块密集的张量核心计算轰鸣声中！** 通信开销在物理时间上变成了“隐形人”！

---

## 步骤 3：跨组件相互耦合机制

1. **单卡显存占用与全序列长度解耦：**  
   单卡上的显存占用不再由全序列长度 $S$ 决定，而仅仅取决于切片长度 $\frac{S}{N}$！只要集群加的卡足够多，理论上可以支持**无限长的上下文窗口（无界百万 Token）**！
2. **因果掩码（Causal Mask）的三角形块剪枝：**  
   在自回归解码或因果预训练中，注意力矩阵是一个下三角阵。在 Ring Attention 旋转中，那些完全位于未来的上三角块（Future Blocks）可以直接在通信或计算中**整块跳过（Skipped）**，进一步节省一半的计算量！

---

## 步骤 4：精确性能数学公式

设全序列长度为 $S$，集群卡数为 $N$，每个 GPU 处理的序列切片为 $S_{\text{local}} = \frac{S}{N}$。

### 1. 单卡显存占用
单卡只需常驻局部的 Query，以及当前正在计算与正在接收的各两组 Key/Value 缓冲区：

$$\text{Memory}_{\text{ring}} \propto O\left(\frac{S}{N} \times d\right)$$

### 2. 隐藏通信的物理判据
设网络双向点对点带宽为 $B_{\text{p2p}}$，单卡每步传输 $K, V$ 切片的通信耗时为：

$$T_{\text{comm}} = \frac{2 \times \frac{S}{N} \times d \times 2}{B_{\text{p2p}}} = \frac{4 S d}{N B_{\text{p2p}}}$$

单卡在片上执行该分块 FlashAttention 的计算做功耗时为：

$$T_{\text{comp}} \approx \frac{4 \times \left(\frac{S}{N}\right)^2 \times d}{P_{\text{peak}}}$$

只要满足：

$$T_{\text{comp}} \ge T_{\text{comm}} \implies \frac{S}{N} \ge \frac{P_{\text{peak}}}{B_{\text{p2p}}}$$

**通信就会被 100% 完全隐藏！系统达到最高理想吞吐！**

---

## 步骤 5：具体微基准数字推导

我们来为真实的 **100 万（1,000,000）Token 超长文本** 算一笔硬件账本！

环境：32 张 H100 显卡（$N = 32$），模型为 LLaMA-3 70B（单头维度 $d=128$，头数 $H=64$，机间网卡带宽 $50\text{ GB/s}$）。

### 1. 计算单卡分摊的切片长度
$$S_{\text{local}} = \frac{1,000,000}{32} = \mathbf{31,250 \text{ Token}}$$
单卡只需要处理 3.1 万字，这在单张 80GB 的 H100 显卡上完全是小菜一碟！

### 2. 计算每步通信数据量与耗时
- 单步需搬运当前切片的 K 和 V（以 GQA 8 头计算）：
  $$\text{Bytes} = 2 \times 31250 \times (8 \times 128) \times 2 \approx 128 \text{ MB}$$
- 在 $50\text{ GB/s}$ 的 InfiniBand 网卡上传输耗时：
  $$T_{\text{comm}} = \frac{128 \times 10^6}{50 \times 10^9} \approx 0.00256 \text{ 秒} = \mathbf{2.56 \text{ 毫秒}}$$

### 3. 计算单步本地计算耗时
- 单步浮点运算量约为 $18 \text{ GFLOPs}$，在单卡 Tensor Core 上耗时约为 **$5 \sim 8 \text{ 毫秒}$**。
- **老师请你看结论：**  
  计算耗时（$5\text{ ms}$）明显大于网络通信耗时（$2.56\text{ ms}$）！  
  这意味着**网络传输完全在后台无声无息地跑完，主算力核心没有浪费哪怕一微秒去等数据！百万长文本计算完美达成！**

---

## 步骤 6：核心系统工程铁律

> **把序列切碎围成环，让数据在环上转，让算力在片上做。**  
> Ring Attention 证明了分布式并行的最高哲学：只要将通信延迟完美隐匿在计算阴影之中，集群的物理卡数就能直接转化为破解百万长文本显存天堑的无限阶梯。
"""

# ==============================================================================
# Module 5: High-Throughput Inference & Online Serving
# ==============================================================================

chapters["module-5/19-prefill-vs-decode-regimes.zh.md"] = r"""# 第 E19 章：两大推理工况深度解耦：Prefill 计算密集 vs Decode 访存密集的物理本质

同学，你好！欢迎来到第五模块——高吞吐推理引擎与在线调度的实战世界！

如果你刚从学术界转到工业界做大模型部署，你一定会经历这样一场认知上的“大地震”：

在论文里，大模型前向传播只是一个平平无奇的函数调用 `output = model(input)`。  
但在真实的生产级推理系统（如 vLLM、TGI、TensorRT-LLM）中，工程师们却用近乎极端的手段，把这个过程强行劈成了两个水火不容的世界：
- 一个叫做 **Prefill 阶段（提示词预填）**
- 另一个叫做 **Decode 阶段（自回归逐字生成）**

你可能会好奇：“老师，输入和输出不都是矩阵乘法吗？为什么非要大费周章地把它们解耦开来区别对待？”

今天这堂课，老师就带你从芯片的物理底座出发，彻底看清这两个工况之间宛如冰与火一般的本质差异！

---

## 步骤 1：物理直觉（暴食狂饮与细水长流）

老师打一个宴席餐厅上菜的比方：

- **Prefill 阶段（狂暴吃大餐的贵宾）：**  
  一开席，后厨一下子送进来整整一车 4000 斤的整猪整牛（用户一口气发来的 4000 字长提示词）。  
  这时候，所有的切菜大厨（Tensor Core）必须齐齐出动，挥刀如飞，案板切得震天响！  
  大厨们的手速决定了**什么时候能端上第一道凉菜（首字延迟 TTFT，Time to First Token）**。在这个阶段，**大厨切菜的力气（算力）是最大的瓶颈！**
- **Decode 阶段（慢品清茶的老大爷）：**  
  第一道菜上齐后，顾客开始剔牙聊天，每隔两分钟才慢悠悠向服务员要一颗小花生米（每次自回归只蹦出 1 个 Token）。  
  可麻烦的是，为了这一颗小花生米，服务员必须**每次都把远在地下二层、重达几十公斤的整套调料箱（全模型 700 亿权重和历史 KV Cache）完整搬到桌前**，蘸一滴酱油，再把箱子搬回地下室！  
  大厨在旁边无聊得哈欠连天，而服务员来回跑腿累得吐血。这时候，**走廊搬运速度（显存带宽）成了唯一的绝对瓶颈！**

你看，**一个把主厨累死，一个把搬运工跑死！** 如果把他们生硬地塞在同一张桌子上伺候，整间餐馆必然乱成一锅粥！

---

## 步骤 2：芯片微观底层执行机制

我们把这两大阶段在芯片上的微观物理参数画在黑板上，进行刺眼的鲜明对比：

```
┌─────────────────────────────────┬─────────────────────────────────┐
│     Prefill 阶段 (提示词预填)   │    Decode 阶段 (自回归逐字生成) │
├─────────────────────────────────┼─────────────────────────────────┤
│ 1. 物理形态: 输入矩阵庞大       │ 1. 物理形态: 输入向量极薄       │
│    (Batch x Seq_Len, 达数千行)  │    (仅 1 行: 新生成的单个 Token)│
│                                 │                                 │
│ 2. 核心工况: 计算密集型         │ 2. 核心工况: 访存密集型         │
│    (Compute-Bound)              │    (Memory-Bound)               │
│                                 │                                 │
│ 3. 算术强度 I: 极高             │ 3. 算术强度 I: 极低             │
│    (通常 > 200~1000 FLOPs/Byte) │    (仅 1~2 FLOPs/Byte!)         │
│                                 │                                 │
│ 4. 芯片表现: Tensor Core 打满   │ 4. 芯片表现: HBM 显存总线塞满   │
│    利用率可达 70%~80%           │    算力核心利用率常年低于 3%    │
│                                 │                                 │
│ 5. 决定性业务指标:              │ 5. 决定性业务指标:              │
│    首字延迟 TTFT                │    逐字生成延迟 ITL             │
│    (Time to First Token)        │    (Inter-Token Latency)        │
└─────────────────────────────────┴─────────────────────────────────┘
```

---

## 步骤 3：跨组件相互耦合机制

1. **混部时的资源打架（Interference & Head-of-Line Blocking）：**  
   如果你在同一个 GPU 上同时处理这两个任务，当一个长文本的 Prefill 请求突然杀入时，它会瞬间霸占 Tensor Core 几百毫秒。这会导致正在顺畅吐字的 Decode 用户遭遇严重的**跳字卡顿（ITL 尖峰剧烈抖动）**，用户体验瞬间崩塌！
2. **现代推理架构的 Prefill-Decode 分离部署（PD Disaggregation）：**  
   正因如此，前沿工业界（如 Moonshot、DeepSeek）开始全面推行“PD 分离架构”：用专门的计算卡集群专职搞 Prefill，算完生成的 KV Cache 通过极速网络直接投递给专职搞 Decode 的带宽卡集群，两队人马彻底分道扬镳、各司其职！

---

## 步骤 4：精确性能数学公式

对于一个拥有 $P$ 个参数的模型，服务单请求：

### 1. Prefill 阶段的物理耗时公式（计算受限）
设提示词长度为 $S$：

$$T_{\text{prefill}} \approx \frac{2 \times P \times S}{P_{\text{peak}}^{\text{dense}}}$$

主要由芯片的**峰值浮点算力 $P_{\text{peak}}$** 决定。

### 2. Decode 阶段单步耗时公式（访存受限）
每次仅生成 1 个 Token，但必须遍历全部模型权重和历史累积的 KV 缓存：

$$T_{\text{decode\_step}} \approx \frac{\text{权重显存} + \text{历史 KV Cache 显存}}{\text{BW}_{\text{HBM}}} = \frac{P \times \text{sizeof(dtype)} + \text{Size}_{\text{kv}}}{\text{BW}_{\text{HBM}}}$$

主要由芯片的**物理显存带宽 $\text{BW}_{\text{HBM}}$** 绝对支配！

---

## 步骤 5：具体微基准数字推导

拿出计算器，老师带你在 **NVIDIA H100 (80GB, 算力 1000 TFLOPS, 带宽 3.35 TB/s)** 上为 **LLaMA-3 70B** 算一笔对比账！

模型权重：$70 \times 10^9$ 参数，BF16 占用 $140\text{ GB}$（假设已通过 TP 切分，单卡分担 $17.5\text{ GB}$ 权重）。

### 1. Prefill 阶段（用户输入 4096 长提示词，单卡）
- 单卡浮点运算量：
  $$\text{FLOPs} = \frac{2 \times 70 \times 10^9 \times 4096}{8} = 7.168 \times 10^{13} \text{ FLOPs} \approx 71.7 \text{ TFLOPs}$$
- 在单卡峰值算力（$1000 \text{ TFLOPS}$，取 $70\%$ 实际利用率）下的首字延迟（TTFT）：
  $$T_{\text{TTFT}} = \frac{71.7 \times 10^{12}}{700 \times 10^{12}} \approx 0.102 \text{ 秒} \approx \mathbf{102 \text{ 毫秒}}$$

### 2. Decode 阶段（单用户逐字生成，单步，暂忽略微量 KV）
- 每次仅蹦出 1 个字，运算量极小，但单卡必须全量加载一次 $17.5\text{ GB}$ 权重：
- 逐字单步物理延迟（ITL）：
  $$T_{\text{ITL}} = \frac{17.5 \times 10^9 \text{ 字节}}{3.35 \times 10^{12} \text{ 字节/秒}} \approx 0.00522 \text{ 秒} \approx \mathbf{5.22 \text{ 毫秒}}$$
- 对应生成速度：每秒最多只能吐出 $1000 / 5.22 \approx \mathbf{191 \text{ 字/秒}}$。

**老师带你细看算力利用率：**  
在 Decode 阶段，单步实际做功仅为 $\frac{2 \times 70 \times 10^9 \times 1}{8} = 17.5 \text{ GFLOPs}$。  
在 5.22 毫秒里，实际利用的算力只有：

$$P_{\text{real}} = \frac{17.5 \times 10^9}{0.00522} \approx 3.35 \text{ TFLOPS}$$

算力利用率只有：

$$\frac{3.35 \text{ TFLOPS}}{1000 \text{ TFLOPS}} \approx \mathbf{0.335\%！}$$

**高达 99.6% 的 Tensor Core 算力全部在冰冷地休眠！**

---

## 步骤 6：核心系统工程铁律

> **工况不同，绝不混为一谈。**  
> 永远铭记 Prefill 追逐算力峰值、Decode 索求显存带宽。在系统架构设计中通过动静批处理分离、算子优化与 PD 物理节点解耦，是消灭资源争抢、筑牢极致低延迟服务体验的基石。
"""

chapters["module-5/20-continuous-batching-scheduling.zh.md"] = r"""# 第 E20 章：连续批处理与动态调度引擎：像地铁流水线一样消灭填充留白

同学，你好！在上一节课中，我们看到单用户 Decode 时，芯片算力利用率竟然跌到了令人心碎的千分之三！

你当时的第一反应一定是：“老师，既然单用户喂不饱算力，**那我们多找几十个用户拼成一个批次（Batch）一起跑不就行了吗？**”

你的想法非常敏锐！把 Batch 增大，正是摊薄权重搬运成本、拉高算力利用率的唯一正道。  
但是，在传统的批处理系统中，有一个极其让人抓狂的死穴——**木桶短板效应与静态填充气泡！**

今天这节课，老师就带你看看伯克利 Orca 和 vLLM 是如何发明**连续批处理（Continuous Batching）**，让大模型服务吞吐像地铁流水线一样暴涨数倍的！

---

## 步骤 1：物理直觉（大巴车死等与地铁随到随走）

老师打一个交通客运的比方：

- **传统静态批处理（包租长途大巴车）：**  
  一辆能坐 4 个人（Batch Size = 4）的大巴车开进站台：
  - 乘客 A 只需要去前门（回答只需要 5 个字）；
  - 乘客 B 却要去千里之外的终点站（回答需要长达 2000 字的长篇小说）；
  - 大巴车规定：**所有人必须同乘一辆车，谁也不准提前下车！**
  - 结果：才过了两秒钟，乘客 A 就说完了；但大巴车必须硬生生在路上继续颠簸半小时，**乘客 A 只能傻坐在车上干瞪眼，空位也绝不准新人上车！**  
  整辆大巴车有 70% 的座位在大部分时间里都是空的（在显存里填满了无用的零 Padding）！
- **连续批处理（高效地铁流水线）：**  
  站台改成了**无限循环穿梭的地铁**：
  - 地铁每开一步（每生成一个 Token 的 Iteration），门就打开一次；
  - 乘客 A 跑完 5 步完工了？**请立刻下车！**  
  - 站台外面排队等待的新乘客 C，**在下一毫秒顺步立刻跳上这节空出来的车厢**！

你看，**地铁上的每一个座位永远坐得满满当当，没有任何一个人在空等，没有任何一个座位在浪费！**

---

## 步骤 2：芯片微观底层执行机制

在连续批处理（也称为 Iteration-Level Scheduling）引擎中，调度器彻底告别了固定时间维度的粗暴静态拼凑，而是下沉到了**单步生成（Per-Iteration）**的微观颗粒度：

```
【传统静态批处理 (Static Batching - 极度浪费)】
请求 1 (长文本): [T0][T1][T2][T3][T4]...[T1000] ──► 漫长生成中...
请求 2 (极短回答): [T0][T1][T2] [PAD][PAD]...[PAD] ──► 必须全程陪跑 1000 步!
请求 3 (中等回答): [T0]...[T200] [PAD]...[PAD] ──► 同样被迫陪跑 800 步!
==> 显卡被毫无意义的 [PAD] 填充符塞满，算力和显存被彻底浪费！

【连续批处理调度引擎 (Continuous / Cellular Batching)】
每个时间步 (Step / Iteration)，调度器动态拉齐所有活跃请求:
Step t:   [Req 1 第 50 字]  [Req 2 第 3 字 (即将结束)]  [Req 3 第 100 字]
           │
           ▼ (Req 2 生成了 <EOS> 终止符，当场注销并释放其 KV Cache 块!)
Step t+1: [Req 1 第 51 字]  [新来的 Req 4 插入预填!]   [Req 3 第 101 字]
           │
           ▼ (永不停歇，每个时钟周期都在做有效输出!)
```

### 底层张量算子的变革——解除 Padding：
为了支撑这种动态进出，底层矩阵乘法摒弃了过去要求规整矩形张量的限制，全面改用基于指针偏移列表的 **变长注意力内核（Ragged / Paged Attention 内核）**。把所有序列的当前 Token 在物理内存上一字排开成一条紧密的向量，**彻底消灭任何填充零符！**

---

## 步骤 3：跨组件相互耦合机制

1. **与 PagedAttention 显存池的完美共舞：**  
   连续批处理之所以在过去很难实现，是因为老系统一旦动态踢出或插入请求，连续显存空间就会碎成蜂窝煤。而第 E05 章学过的 **PagedAttention 虚拟页表**，刚好为连续批处理提供了天造地设的物理底座：任何请求随时走，物理块当场归还物理池；新请求随时来，抓起几个空块立刻上车！
2. **算术强度与硬件利用率的大幅提升：**  
   通过维持恒定高水位线的并发 Batch Size，每次将权重从 HBM 运到片上时，都能同时为 32 到 64 个 Token 提供服务，**算术强度从 1 暴增到 32~64 FLOPs/Byte**，直接让 GPU 摆脱冰冷低效的休眠状态！

---

## 步骤 4：精确性能数学公式

设在一个批次中，包含 $B$ 个请求，各个请求的真实生成长度为 $S_1, S_2, \dots, S_B$。

### 1. 传统静态批处理的总时钟开销
系统总步数被最长的那根木桶短板死死绑架：

$$N_{\text{static}} = \max(S_1, S_2, \dots, S_B)$$

总有效计算量占比（显存与算力有效率）：

$$\eta_{\text{static}} = \frac{\sum_{i=1}^B S_i}{B \times \max(S_1, \dots, S_B)}$$

在真实生产中，当请求长短不一时，$\eta_{\text{static}}$ 常常低至 **$20\% \sim 30\%$**！

### 2. 连续批处理的总时钟开销
完全消除留白，总有效步数等于总 Token 数除以平均并发槽位数：

$$\eta_{\text{continuous}} \approx 100\% \quad (\text{除微小调度判定开销外，几乎零气泡})$$

---

## 步骤 5：具体微基准数字推导

我们来算一笔小账，看看效率能翻多少倍！

假设当前并发批大小 $B = 4$，来了 4 个不同任务的请求：
- 请求 1（问个天气）：需要生成 $S_1 = 20$ 个 Token；
- 请求 2（写首短诗）：需要生成 $S_2 = 50$ 个 Token；
- 请求 3（写段代码）：需要生成 $S_3 = 200$ 个 Token；
- 请求 4（写篇长篇报告）：需要生成 $S_4 = 1000$ 个 Token。

### 1. 静态批处理表现
- 全系统必须死死执行：$\max(20, 50, 200, 1000) = \mathbf{1000 \text{ 步}}$；
- 消耗的槽位总量：$4 \times 1000 = 4000$ 个槽位；
- 真正有用的 Token 生成数：$20 + 50 + 200 + 1000 = 1270$ 个；
- **浪费率：**
  $$\text{资源浪费} = \frac{4000 - 1270}{4000} = \mathbf{68.25\%！}$$
  将近七成的算力和显存白白打水漂！

### 2. 连续批处理表现
- 请求 1 在第 20 步下车，槽位立刻让给队列里的请求 5；
- 请求 2 在第 50 步下车，槽位立刻让给请求 6；
- 槽位利用率**始终维持在 100% 满负荷**！
- 系统端到端吞吐量（Tokens/s）相比静态批处理直接**飙升整整 3.15 倍！**

---

## 步骤 6：核心系统工程铁律

> **把调度粒度打碎进每一个 Iteration。**  
> 绝不容忍以最长序列为基准的静态批处理浪费。通过步级别的动态上车、下车机制与分页虚拟显存深度协同，让推理引擎的每一条并发流水线永远保持饱满。
"""

chapters["module-5/21-speculative-decoding.zh.md"] = r"""# 第 E21 章：投机解码：让轻量小模型大胆猜，让千亿大模型并行核验

同学，你好！在前面我们推导过：大模型在生成时，每生成一个新词，都要把成百上千亿个参数从显存里搬运一遍。

这时候，很多聪明的同学会忍不住拍大腿：“老师，既然每次搬运参数都要花好几毫秒，那如果每次我们能一口气蹦出 3 个甚至 5 个词，这几毫秒不就花得超值了吗？”

你可能会问：“可是大模型是自回归的呀，第 2 个词必须等第 1 个词生成了才能算，怎么可能‘一口气生成好几个词’呢？”

这就是 Google 和 DeepMind 的天才研究员们在 2022 年向全世界亮出的魔法手杖——**投机解码（Speculative Decoding）**！

今天，老师就带你拆解这个让生成速度直接狂飙 2 到 3 倍、而且**输出质量数学绝对无损**的神仙算法！

---

## 步骤 1：物理直觉（大作家与打字小书童）

老师给你打个文豪写作的比方：

- **文豪亲自单打独斗（传统大模型解码）：**  
  文豪（千亿参数大模型，肚子里学富五车，但动作极其沉稳缓慢）坐在桌前：
  想 1 秒钟，写出“我们”；  
  再想 1 秒钟，写出“必须”；  
  再想 1 秒钟，写出“高度”；  
  再想 1 秒钟，写出“重视”……  
  虽然文笔绝顶，但每憋出一个字都要花整整 1 秒钟。
- **配上轻快的小书童（投机解码模式）：**  
  文豪雇佣了一个手脚极其麻利的小书童（小巧轻快的 1B 参数小模型，动笔如飞）：
  1. **小书童大胆抢答：** 小书童在 0.1 秒内，一口气在草稿纸上猜着往下写了 4 个词：“必须 / 高度 / 重视 / 发展”；
  2. **大文豪一眼扫过（并行核验）：** 文豪戴上老花镜，根本不需要逐字苦思，**只需将这 4 个词打包成一个矩阵乘法，一瞬间扫视核对（Prefill 工况，计算密集，极其迅速！）**；
  3. **裁决时刻：** 文豪点头说：“‘必须’对，‘高度’对，‘重视’也对；但第四个词我不打算用‘发展’，我想用‘安全’！”
  4. 于是，文豪当场通过了前 3 个词，并在第 4 个词的位置亲自纠偏！

你看，**原本需要文豪枯坐 4 秒钟的事情，现在文豪只需抬手看一眼，在 1 秒多钟内直接收割了 4 个词！**

---

## 步骤 2：芯片微观底层执行机制

投机解码之所以在物理硬件上能实现暴击加速，是因为它**把慢速的访存密集型 Decode，巧妙转化成了高效的计算密集型短 Prefill！**

```
【投机解码单轮执行闭环时序】

1. 草稿生成阶段 (Drafting):
   轻量级草稿小模型 (如 LLaMA-3 1B) 独立执行 K 步自回归自生:
   产出候选 Token 序列: [x1, x2, x3, x4] (花费微量时间)

2. 目标大模型并行验证 (Target Model Verification):
   目标大模型 (如 LLaMA-3 70B) 一口气将 [当前词, x1, x2, x3, x4] 拼接输入!
   注意: 这是一次小规模的 Prefill 矩阵乘法!
   全模型 700 亿参数依然只从 HBM 读取了一次，
   但在片上 Tensor Core 冲压机里，同时算出了这 4 个位置的全部转移概率分布!

3. 拒绝采样裁决机制 (Rejection Sampling):
   按顺序逐字比对小模型与大模型的输出概率:
   如果 P_target(x) >= P_draft(x)，直接无条件接受!
   如果小模型猜错了，以一定概率拒绝并当场由大模型根据修正分布采样出正确词，
   丢弃后续所有猜想，开启下一轮循环!
```

### 为什么数学上完全绝对无损？
很多初学者最担心的问题是：“小模型水平那么差，大模型会不会被带偏变笨？”  
老师明确告诉你：**绝对不会！**  
Leviathan et al. (2023) 在数学上给出了极其严密的证明：通过精巧的**拒绝采样概率修正公式**，投机解码最终吐出的每一个 Token 的概率分布，**与千亿大模型独立逐字生成的原始概率分布完全数学严格一致！**

---

## 步骤 3：跨组件相互耦合机制

1. **草稿模型的摆放拓扑：**  
   草稿模型通常只有主模型的几十分之一大小。在单卡推理时，小模型可以直接常驻在同一张显卡显存中；在多卡分布式集群中，小模型甚至可以部署在专门的独立辅助卡上。
2. **接受率（Acceptance Rate $\alpha$）对硬件提速的决定性影响：**  
   小模型猜得越准，平均每步接受的 Token 数量就越多。如果两个模型领域契合度极高（$\alpha > 80\%$），加速比可直奔 3 倍；但如果小模型在生僻领域胡猜导致屡屡被拒，系统就会退化为纯粹的大模型单步执行。

---

## 步骤 4：精确性能数学公式

设草稿小模型每轮推测生成 $K$ 个 Token。小模型生成单 Token 耗时为 $t_{\text{draft}}$，大模型验证这 $K$ 个 Token 的单次前向耗时为 $t_{\text{target}}$。

设小模型预测的平均接受率为 $\alpha$（$0 \le \alpha \le 1$）。

单轮循环中，期望被接受的有效 Token 数量为：

$$\mathbb{E}[\text{Accepted}] = \sum_{i=1}^K \alpha^i + 1 \quad (\text{最后保底附赠由大模型裁决纠偏的 1 个 Token})$$

单轮循环的硬件总物理耗时为：

$$T_{\text{round}} = K \times t_{\text{draft}} + t_{\text{target}}$$

投机解码相比大模型原始逐字解码的实际理论加速比（Speedup Ratio）为：

$$\text{Speedup} = \frac{\mathbb{E}[\text{Accepted}] \times t_{\text{target}}}{K \times t_{\text{draft}} + t_{\text{target}}}$$

---

## 步骤 5：具体微基准数字推导

我们来为业界最经典的组合——**主模型 LLaMA-3 70B + 草稿模型 LLaMA-3 8B** 在 8 卡 H100 节点上手算一笔账！

已知实测单步参数：
- 70B 目标模型单次前向耗时：$t_{\text{target}} = 10 \text{ 毫秒}$
- 8B 草稿模型单步前向耗时：$t_{\text{draft}} = 1.5 \text{ 毫秒}$
- 设定草稿步数：$K = 4$
- 实测两者的语义重合接受率：$\alpha = 0.75$（75% 的词都能猜中）

### 1. 计算单轮期望产出 Token 数量
$$\mathbb{E}[\text{Accepted}] = \alpha + \alpha^2 + \alpha^3 + \alpha^4 + 1 = 0.75 + 0.5625 + 0.4219 + 0.3164 + 1 \approx \mathbf{3.05 \text{ 个 Token}}$$
单轮下来，平均一口气蹦出 3 个字！

### 2. 计算单轮硬件总耗时
$$T_{\text{round}} = 4 \times 1.5 \text{ ms} + 10 \text{ ms} = 6 \text{ ms} + 10 \text{ ms} = \mathbf{16 \text{ 毫秒}}$$

### 3. 计算生成每个 Token 的均摊耗时
$$T_{\text{per\_token}} = \frac{16 \text{ 毫秒}}{3.05} \approx \mathbf{5.25 \text{ 毫秒}}$$

### 4. 对比未优化的 70B 单独运行：
原本 70B 单独生成每个字需要死死耗费 $10\text{ 毫秒}$。  
现在的端到端加速比为：

$$\text{Speedup} = \frac{10 \text{ ms}}{5.25 \text{ ms}} \approx \mathbf{1.905 \times！}$$

**整整快了近一倍！** 而且没有牺牲哪怕一丁点模型的智慧与逻辑！

---

## 步骤 6：核心系统工程铁律

> **用廉价的算力跑腿，换昂贵的显存解脱。**  
> 投机解码是打破单步访存受限的最精妙设计。在算力富余的先进硬件上，利用小模型前瞻探索、大模型并行判别，是在数学零精度损失的前提下撬动多倍生成帧率的核心利器。
"""

chapters["module-5/22-chunked-prefill-prefix-caching.zh.md"] = r"""# 第 E22 章：分块预填与前缀缓存：平抑逐字延迟抖动与基数树零开销复用

同学，你好！欢迎来到高吞吐推理引擎的最后一课！

在前面的学习中，我们了解了连续批处理和投机解码。  
但在大模型服务上线后，你的运维监控报警面板上，常常会出现两条刺眼的折线：
1. **ITL（逐字延迟）突然坐过山车：** 原本每秒吐 30 个字顺顺畅畅，突然某个瞬间卡顿了整整半秒钟，用户破口大骂；
2. **显存总被重复的问题塞满：** 上百个用户在并发提问，但每个人问题前面都挂着一模一样长达 3000 字的“背景系统提示词（System Prompt）”。

为了解决这两个致命的痛点，现代推理引擎祭出了两项工业级王牌技术——**分块预填（Chunked Prefill）** 与 **前缀缓存（Prefix Caching）**！

今天，老师就带你彻底吃透它们背后的设计机理！

---

## 步骤 1：物理直觉（大面包切片与图书馆公共字典）

老师打两个生活里的小比方：

### 比方一：分块预填（大面包切成小薄片）
想象你正在流水线上把一根长达两米、重达十斤的巨型法棍大面包（长达 8000 字的 Prefill 提示词）塞进切片包装机。
- **原始做法（一口闷）：**  
  你把整根十斤的大面包硬往机器里塞，切片机瞬间被卡死，整条流水线上的其他小饼干（正在 Decode 逐字吐字的小请求）全部被强行停工整整两秒钟！所有老顾客都在破口大骂！
- **分块预填（聪明切片）：**  
  你拿出一把小刀，把十斤大面包**切成均匀的 512 克小薄片**。每次传送带流动时，机器处理一块小面包，顺便带上几块小饼干；在下一个节奏，再放进第二块小薄片……整条流水线平缓流畅，卡顿彻底消失！

### 比方二：前缀缓存（公共参考书）
- **传统做法（每个人背一本辞海）：**  
  全班 50 位同学都要做英语阅读理解，背景资料是同一本厚达 1000 页的《牛津大辞典》。老师让每个人都在自己的书包里完整背一本大辞典进教室，教室当场被撑爆！
- **前缀缓存（Radix Tree 共享辞海）：**  
  讲台上直接放一本公用的《牛津大辞典》，并且按字母建好了树状索引目录（基数树 Radix Tree）。所有同学抬头共同阅读讲台上的同一本大字典，**谁也不用在自己书包里重复占地方，显存瞬间清空！**

---

## 步骤 2：芯片微观底层执行机制

### 1. 分块预填（Chunked Prefill）的执行微观流：
在 Sarathi / vLLM 调度器中，设定一个批次允许的最大物理 Token 计算预算（例如 $\text{Budget} = 1024$）：

```
【单 Iteration 内部的混合调度切片】
当前批次 Token 预算 = 1024
正在并发跑 Decode 的请求有 32 个 (消耗 32 个 Token 预算)
剩余可用预算: 1024 - 32 = 992 Token!

此时新来了一个长度为 4000 的长预填请求 Req_New:
调度器决不一次性喂入 4000!
而是切出当前第一片: Chunk 0 (长度 992)
==> 将 992 个 Prefill Token 与 32 个 Decode Token 混合打包进单次 GEMM!
==> 算力被完美喂饱，同时单步耗时严格被限制在安全阈值 (如 15 ms) 之内!
==> 彻底抹平了传统全量 Prefill 带来的剧烈 ITL 延迟尖峰!
```

### 2. 前缀缓存（Radix Tree 树状前缀命中机制）：
推理引擎在 CPU 内存中维护一棵以 Token ID 为路径分支的**基数树（Radix Tree）**：

```
根节点 ROOT
  │
  └── "你是一位专业的金融审计师，请根据以下报表..." (节点 A: 命中 1024 个 Token)
        ├── 问报表 1 ──► 逻辑块直接复用节点 A 的物理 KV 缓存! (仅算后续新字)
        └── 问报表 2 ──► 同样直接复用节点 A 的物理 KV 缓存!
```
当新请求到达时，系统以 $O(K)$ 速度在树上完成前缀匹配。只要命中了公共系统提示词，**对应的历史 KV Cache 物理块直接挂载到当前请求的页表上，Prefill 计算时间直接瞬间归零！**

---

## 步骤 3：跨组件相互耦合机制

1. **彻底平抑服务等级协议（SLA）指标：**  
   分块预填消除了长提示词对在线自回归生成的抢占式霸凌，将系统的 **P99 逐字生成延迟（P99 ITL）平稳压低了 80% 以上**，这是生产级云服务必须达标的硬门槛。
2. **算力与显存的双向大收割：**  
   在多轮对话（Agent 复杂调用、代码库多轮问答）中，前缀缓存的命中率常常高达 **70%~90%**。这意味着绝大多数请求根本不需要重复计算长上下文，系统吞吐量往往能实现 **3 到 5 倍的暴增**！

---

## 步骤 4：精确性能数学公式

对于一个长为 $S_{\text{prompt}}$ 的提示词，在分块大小为 $C$ 的策略下，被切分为 $M = \lceil S_{\text{prompt}} / C \rceil$ 个块。

单步调度混合耗时严格受控于：

$$T_{\text{step}} \le \frac{2 \times P \times (C + B_{\text{decode}})}{P_{\text{peak}}}$$

在前缀缓存命中长度为 $S_{\text{match}}$ 的场景下，实际需要计算的提示词前向开销骤降为：

$$S_{\text{effective}} = S_{\text{prompt}} - S_{\text{match}}$$

节省的纯浮点计算量为：

$$\Delta \text{FLOPs} = 2 \times P \times S_{\text{match}}$$

---

## 步骤 5：具体微基准数字推导

我们来为一家**智能客服云平台**算一笔真实的降本提速大账！

平台背景：
- 使用 **LLaMA-3 70B** 模型
- 统一挂载了一篇长达 $S_{\text{prompt}} = 3000$ Token 的详细企业规章制度系统提示词
- 每秒钟有 10 位新用户发起咨询对话。

### 1. 没有前缀缓存（Naive Prefill）时的算力黑洞：
- 每一秒钟，系统仅仅为了反复理解这篇一模一样的企业规章制度，就要做功：
  $$\text{重复做功} = 10 \times (2 \times 70 \times 10^9 \times 3000) = 4.2 \times 10^{15} \text{ FLOPs} = \mathbf{4,200 \text{ TFLOPs！}}$$
- 这需要整整 **5 张顶配 H100 显卡** 100% 满负荷轰鸣，仅仅用来对付这篇重复的废话！

### 2. 启用基数树前缀缓存（Prefix Caching）后：
- 第一次读完后，该 3000 个 Token 的 KV Cache 被永久常驻在缓存池中；
- 后续所有用户的这 3000 个字**前向计算做功直接归零（0 FLOPs！）**；
- 5 张 H100 的昂贵算力当场被彻底省下来，首字延迟（TTFT）从将近 1 秒钟**暴跌至 20 毫秒以内**！用户体验获得云泥之别的震撼提升！

---

## 步骤 6：核心系统工程铁律

> **把长任务切成小薄片，把相同的前缀留在树上。**  
> 分块预填是保障在线服务毫秒级平稳吐字的安全阀，前缀缓存则是降解多轮对话算力开销的最强复利池。将二者融合进现代推理引擎，是打造商业级 AI 基础设施的必修课。
"""

written = 0
for rel_path, content in chapters.items():
    full_path = os.path.join(BASE_DIR, rel_path)
    os.makedirs(os.path.dirname(full_path), exist_ok=True)
    with open(full_path, "w", encoding="utf-8") as f:
        f.write(content.strip() + "\n")
    written += 1

print(f"Successfully refined {written} chapters for Module 4 and Module 5 with teacher voice.")
