import os

BASE_DIR = os.path.abspath(os.path.join(os.path.dirname(__file__), "..", "docs"))

chapters = {}

# ==============================================================================
# Module 2: Linears, Activations & Kernel Fusion
# ==============================================================================

chapters["module-2/07-gemm-tensor-cores.zh.md"] = r"""# 第 E07 章：规模化 GEMM 与 Tensor Core 矩阵乘：脉动阵列冲压机与显存对齐法则

同学，你好！欢迎来到第二模块的第一课！

你可能经常在显卡宣传册上看到这样的广告词：“全新一代 GPU，搭载第 4 代 Tensor Core，算力高达 2,000 TFLOPS！”

你可能会问老师：“老师，这个被称为 AI 引擎心脏的 **Tensor Core（张量核心）** 到底是个什么怪物？为什么普通的 CUDA 核心只能一个数字一个数字慢吞吞地算，而 Tensor Core 一启动，算力就能瞬间暴涨整整十倍以上？”

今天，老师就带你走进芯片工厂，看看这台**超高速脉动冲压机**到底是如何工作的！

---

## 步骤 1：物理直觉（铁匠打铁与汽车冲压车间）

为了让你秒懂普通计算核心与 Tensor Core 的区别，老师打一个制造业的比方：

- **普通标量/向量核心（CUDA Core）：老铁匠手工打铁**  
  老铁匠站在铁砧前，左手夹着一块铁，右手抡起铁锤：叮（一次乘法）、当（一次加法）。打完一颗铁钉，再去打下一颗。虽然他手艺精湛、什么形状都能敲（支持各种复杂分支逻辑），但一次只能做 1 件事。
- **Tensor Core（张量核心）：重型汽车冲压机床**  
  车间里立着一台重达千吨的液压冲压机。工人们不再一锤一锤敲，而是把整块 $16 \times 16$ 大小的整块钢板推进去，机械臂“轰隆”一声巨响齐齐压下，**一瞬间把 256 个零件的乘加融合运算（$D = A \times B + C$）在几个时钟周期内全部冲压成型！**

你看，冲压机虽然威力极其毁天灭地，但它有一个极其严苛的脾气：**模具尺寸必须绝对精准匹配！**

如果你的零件尺寸是长 16、宽 16，刚好卡进模具槽里，冲压机就能满负荷连续疯狂轰鸣；  
但如果你的零件长了个毛边，比如尺寸是 17 或者 15，零件卡不进模具槽，冲压机就会当场卡壳停机！工人不得不把冲压机切回慢吞吞的手工锯模式，速度瞬间暴跌 90%！

这就是为什么在 AI 工业界，工程师们会对**“16 字节对齐”、“维度必须是 8 或 16 的倍数”**有种近乎强迫症一般的执着。

---

## 步骤 2：芯片微观底层执行机制

在 NVIDIA Volta、Ampere、Hopper 架构中，Tensor Core 的微观硬件执行采用的是**流水线矩阵乘累加（MMA，Matrix Multiply-Accumulate）指令**：

```
MMA 指令基本做功单元: D = A x B + C
输入矩阵分块: A 尺寸 [M x K], B 尺寸 [K x N], 累加器 C/D 尺寸 [M x N]

【Warp 线程束协同分块机制 (以 16 x 8 x 16 为例)】
Warp 内的 32 个线程并不各自独立算一个大矩阵，
而是像交织的齿轮一样，把一个 16 x 16 的微小矩阵切片分配进 32 个线程的内部寄存器中:
  - 线程 0~3 负责持有矩阵 A 的第 0 行片段与矩阵 B 的第 0 列片段；
  - 硬件内部的微型脉动阵列在 1 个周期内完成乘法广播并进入加法累加器树；
  - 经过多级流水线，生成高精度的 FP32 或 FP16 结果存入寄存器堆。

【三级 GEMM 分块平铺层次 (Tiling Hierarchy)】
┌──────────────────────────────────────────────────────────────────┐
│ 1. 网格级分块 (Grid-level): 整个大 GEMM 矩阵切分为 128x256 的大块 │
│    分配给不同的 SM (Streaming Multiprocessor) 独立并行处理        │
├──────────────────────────────────────────────────────────────────┤
│ 2. 线程块级分块 (Block-level): 每个 SM 从全局 HBM 搬运数据至      │
│    片上共享内存 (SRAM), 维持持续的循环流水线 (Pipelines)          │
├──────────────────────────────────────────────────────────────────┤
│ 3. 线程束级分块 (Warp-level): 每个 Warp 独占从 SRAM 抓取 16x16 块 │
│    直接送进 Tensor Core 的 MMA 指令冲压流水线                      │
└──────────────────────────────────────────────────────────────────┘
```

### 为什么维度必须对齐？
- Tensor Core 的硬件指令（例如 `mma.sync.aligned.m16n8k16`）严格要求矩阵的内层跨度 $K$ 必须是 16 的倍数（在 FP16 精度下对应 32 字节，恰好填满单次寄存器向量读取）。
- 如果大模型的隐藏层维度 $d_{\text{model}}$ 没有被 16 或 128 整除，底层编译器就无法生成高效的 MMA 矢量指令，只能降级为低速标量指令，或者插入大量低效的零填充（Padding），造成极大的算力浪费。

---

## 步骤 3：跨组件相互耦合机制

1. **大模型超参数设计的“硬件暗门”：**  
   你仔细观察过主流开源大模型（如 LLaMA、Mistral、Qwen）的结构参数吗？它们的隐藏层维度（如 4096、8192）、注意力头数（如 32、64）、单头维度（128）**无一例外全是 16 和 128 的整倍数**！这不是巧合，而是架构师们为了完美契合 Tensor Core 冲压模具而特意裁定的物理黄金尺寸。
2. **张量切分与分布式通信的契约：**  
   当进行多卡张量并行（Tensor Parallelism）时，矩阵会被横向或纵向切分。如果并行卡数是 8，切分后的子矩阵必须依然能被 16 整除，否则单卡上的 Tensor Core 执行效率就会骤降。

---

## 步骤 4：精确性能数学公式

对于一个通用的矩阵乘法（GEMM）：

$$\mathbf{C} = \mathbf{A} \times \mathbf{B}$$

其中 $\mathbf{A} \in \mathbb{R}^{M \times K}, \mathbf{B} \in \mathbb{R}^{K \times N}, \mathbf{C} \in \mathbb{R}^{M \times N}$。

### 1. 浮点运算量（FLOPs）
每个位置的输出需要做 $K$ 次乘法和 $K$ 次加法（共 $2K$ 次浮点做功），全矩阵总运算量为：

$$\text{FLOPs}_{\text{GEMM}} = 2 \times M \times N \times K$$

### 2. 在理想分块下的 HBM 搬运量（Bytes，设精度为 2 字节 FP16）
若片上 SRAM 完美复用，每块数据从 HBM 仅读取一次：

$$\text{Bytes}_{\text{GEMM}} = 2 \times (M \times K + K \times N + M \times N) \text{ 字节}$$

### 3. GEMM 的理论算术强度（Operational Intensity）
$$I_{\text{GEMM}} = \frac{2 \times M \times N \times K}{2 \times (MK + KN + MN)} = \frac{M \times N \times K}{MK + KN + MN}$$

当 $M, N, K$ 都非常巨大（如均等于 $D$）时：

$$I_{\text{GEMM}} \approx \frac{D^3}{3 D^2} = \frac{D}{3} \text{ FLOPs/Byte}$$

**老师划重点：**  
随着矩阵尺寸 $D$ 的扩大，算术强度呈线性上升！只要矩阵足够大，$I_{\text{GEMM}}$ 就会轻松跨过屋顶模型的硬件平衡点 $I^*$，让 Tensor Core 全力发挥出理论最高性能！

---

## 步骤 5：具体微基准数字推导

拿出计算器，我们来算一算 **LLaMA-3 70B** 在 Prefill 阶段的一个线性层 GEMM！

参数设置：
- 批大小与序列长度乘积：$M = 4096$
- 输入维度：$K = 8192$
- 输出维度：$N = 8192$
- 硬件：NVIDIA H100 SXM（FP16 峰值算力 $1,000 \text{ TFLOPS}$，HBM 带宽 $3.35 \text{ TB/s}$，平衡拐点 $I^* \approx 298.5$）

### 1. 计算总做功 FLOPs
$$\text{FLOPs} = 2 \times 4096 \times 8192 \times 8192 \approx 5.497 \times 10^{11} \text{ FLOPs} \approx 550 \text{ GFLOPs}$$

### 2. 计算理想 HBM 搬运量
- 输入矩阵 A：$4096 \times 8192 \times 2 \approx 67.1 \text{ MB}$
- 权重矩阵 B：$8192 \times 8192 \times 2 \approx 134.2 \text{ MB}$
- 输出矩阵 C：$4096 \times 8192 \times 2 \approx 67.1 \text{ MB}$
- 总访存：$\text{Bytes} = 67.1 + 134.2 + 67.1 = 268.4 \text{ MB} \approx 0.268 \text{ GB}$

### 3. 计算实际算术强度 $I$
$$I = \frac{5.497 \times 10^{11} \text{ FLOPs}}{2.684 \times 10^8 \text{ Bytes}} \approx 2048 \text{ FLOPs/Byte}$$

### 4. 判断硬件工况
因为 $2048 \gg 298.5$（是平衡拐点的将近 7 倍！），该算子深深落入**纯计算受限区间（Compute-Bound）**！
- 理论物理纯计算耗时：
  $$T_{\text{compute}} = \frac{5.497 \times 10^{11}}{1.0 \times 10^{15}} \approx 0.00055 \text{ 秒} = 550 \;\mu\text{s} = 0.55 \text{ ms}$$
此时，Tensor Core 的冲压机被彻底喂饱，硬件利用率可以冲上 70%~80% 的极高水准！

---

## 步骤 6：核心系统工程铁律

> **顺应 Tensor Core 的冲压模具，把矩阵做大、排整齐。**  
> 永远确保张量维度是 16 与 128 的倍数以触发硬件级 MMA 向量指令；在 Prefill 阶段尽可能合并 Batch 增大矩阵尺寸，让算术强度跨过平衡拐点，榨干芯片的每一滴浮点算力。
"""

chapters["module-2/08-swiglu-ffn-in-silicon.zh.md"] = r"""# 第 E08 章：SwiGLU 与前馈网络在芯片上的执行：三矩阵投影与反向传播激活值治理

同学，你好！在前面我们探讨了注意力机制。很多同学以为注意力是大模型算力开销的最大头，但老师今天告诉你一个颠覆常识的事实：

**在整个 Transformer 模型中，真正吃掉全网 60% 到 66% 显存参数和前向浮点计算量的，不是注意力机制，而是紧跟其后的前馈网络（Feed-Forward Network，FFN）！**

而在现代前沿开源大模型（LLaMA 系列、DeepSeek、Qwen、Mistral）中，大家普遍摒弃了传统的 ReLU/GELU 激活，全员拥抱了一个名字有点拗口的结构——**SwiGLU（Swish Gated Linear Unit）**。

你可能会好奇：“老师，为什么大家都换成了 SwiGLU？多出来的那张投影矩阵，在显卡底层会带来怎样沉重的显存代价呢？”

今天，老师就带你深入前馈网络的芯片执行内幕！

---

## 步骤 1：物理直觉（进食双通道与门控水龙头）

为了让你彻底看清传统 FFN 与 SwiGLU 的区别，老师打个厨房水龙头的比方：

- **传统 FFN（经典单通道）：**  
  食材送进厨房后，只有一条传输带：先经过一个大功率挤压机（膨胀线性层），然后通过一个筛子（ReLU/GELU 激活函数过滤），最后再压平装盘（收缩线性层）。这个流程简单粗暴，但表达能力比较单一。
- **SwiGLU（双通道门控调节）：**  
  食材一进门，立刻被兵分两路送进**两条并行的流水线**：
  - **一路叫做 Up 投影（主食材管道）：** 负责把食材特征升维放大；
  - **另一路叫做 Gate 投影（水龙头门控管道）：** 它计算出一组平滑的 0 到 1 之间的旋转阀门开度（Swish 激活）；
  - **在十字路口两两相乘：** 门控管道的水龙头旋转，精准决定主食材管道的每一滴调料流出多少；
  - **最后汇流进 Down 投影：** 重新压回原维度输出。

你看，SwiGLU 的语言理解能力极其细腻强大，但代价也显而易见：**原本只要 2 个大矩阵，现在变成了 3 个大矩阵！**

原本只需维护一份半成品，现在在芯片里必须同时维护两份巨大的中间张量，并在内存里把它们点对点相乘！

---

## 步骤 2：芯片微观底层执行机制

我们把 SwiGLU 的数学公式写在黑板上：

$$\text{FFN}_{\text{SwiGLU}}(\mathbf{x}) = \left(\text{Swish}(\mathbf{x}\mathbf{W}_{\text{gate}}) \odot (\mathbf{x}\mathbf{W}_{\text{up}})\right)\mathbf{W}_{\text{down}}$$

其中：
- $\mathbf{W}_{\text{gate}} \in \mathbb{R}^{d \times d_{\text{ffn}}}$
- $\mathbf{W}_{\text{up}} \in \mathbb{R}^{d \times d_{\text{ffn}}}$
- $\mathbf{W}_{\text{down}} \in \mathbb{R}^{d_{\text{ffn}} \times d}$
- 现代通常设置中间隐藏层维度 $d_{\text{ffn}} \approx \frac{8}{3} d$（例如当 $d=4096$ 时，$d_{\text{ffn}} = 11008$ 或 $14336$）。

```
【朴素实现 vs 算子水平拼接熔合】

朴素执行路线 (严重浪费 HBM 带宽):
  x ──► GEMM 1 ──► 写入中间张量 A 到 HBM ──►┐
  x ──► GEMM 2 ──► 写入中间张量 B 到 HBM ──►┴─► 从 HBM 读出 A, B ──► 执行 Swish 并逐元素乘 ──► 写入 C 到 HBM ──► GEMM 3 ──► 输出

算子水平拼接熔合优化 (Horizontal Fusion):
把 W_gate 与 W_up 在内存中横向拼接为一个宽矩阵:
  W_fused = [W_gate | W_up] 尺寸为 [d x (2 * d_ffn)]

执行微观数据流:
  x ──► 单次超大 GEMM 冲压 ──► 在片上 SRAM/寄存器中直接拆分并执行 Swish(A) * B ──► 仅将最终相乘结果写出 ──► GEMM 3
```

### 为什么水平熔合如此重要？
1. **打满 Tensor Core 冲压机：** 单次大矩阵乘法可以让 GPU 的网格调度器分配更多的线程块（Thread Blocks），避免两次独立小矩阵带来的内核启动气泡。
2. **消灭两份巨量中间特征的显存往返：** 逐元素逐位相乘（$\odot$）直接在片上完成，彻底省去了将 $\text{Swish}(\mathbf{x}\mathbf{W}_{\text{gate}})$ 与 $\mathbf{x}\mathbf{W}_{\text{up}}$ 写入全局显存再重新读出的开销！

---

## 步骤 3：跨组件相互耦合机制

1. **占据全模型 66% 参数量的大头：**  
   每个 Transformer 层包含 1 个注意力模块和 1 个 FFN 模块。注意力层包含 $Q, K, V, O$ 四个 $d \times d$ 矩阵（共 $4d^2$ 参数）。而 SwiGLU 拥有 3 个 $d \times \frac{8}{3}d$ 矩阵（共 $3 \times \frac{8}{3}d^2 = 8d^2$ 参数）！**FFN 的参数量整整是注意力层的 2 倍！**
2. **反向传播显存激活值的巨大包袱：**  
   在模型训练反向传播求导时，为了求出输入 $\mathbf{x}$ 的梯度，必须保存前向传播时的中间激活值。如果不做激活值重计算（Activation Recomputation），前馈层将成为训练时爆显存的头号元凶。

---

## 步骤 4：精确性能数学公式

设序列长为 $S$，批大小为 $B$，隐藏维度为 $d$，中间膨胀维度为 $d_{\text{ffn}}$：

### 1. 前向浮点运算量（FLOPs）
- Gate 投影：$2 B S d d_{\text{ffn}}$
- Up 投影：$2 B S d d_{\text{ffn}}$
- Down 投影：$2 B S d_{\text{ffn}} d$
- 激活函数与相乘：约 $4 B S d_{\text{ffn}}$（相比矩阵乘法可忽略）

$$\text{FLOPs}_{\text{SwiGLU}} \approx 6 B S d d_{\text{ffn}}$$

代入常用的 $d_{\text{ffn}} = \frac{8}{3} d$：

$$\text{FLOPs}_{\text{SwiGLU}} \approx 6 B S d \left(\frac{8}{3} d\right) = 16 B S d^2$$

### 2. 对比注意力机制线性投影
注意力层 4 个矩阵投影的计算量为：$4 \times 2 B S d^2 = 8 B S d^2$。  
**SwiGLU 线性部分的算力开销恰好是注意力线性投影的整整 2 倍！**

---

## 步骤 5：具体微基准数字推导

我们以 **LLaMA-3 8B** 单层的前向计算为例，算一算具体的硬件账本：
- $d = 4096$
- $d_{\text{ffn}} = 14336$
- 批大小 $B = 1$，上下文预填长度 $S = 2048$
- 精度：FP16（每元素 2 字节）

### 1. 计算三个权重矩阵的显存静态体积
- $\mathbf{W}_{\text{gate}}$ 大小：$4096 \times 14336 \times 2 \approx 117.4 \text{ MB}$
- $\mathbf{W}_{\text{up}}$ 大小：$4096 \times 14336 \times 2 \approx 117.4 \text{ MB}$
- $\mathbf{W}_{\text{down}}$ 大小：$14336 \times 4096 \times 2 \approx 117.4 \text{ MB}$
- **单层 SwiGLU 权重总大小：** $352.2 \text{ MB}$（全模型 32 层仅 FFN 权重就占去 $11.27\text{ GB}$！）。

### 2. 计算前向计算总做功
$$\text{FLOPs} = 6 \times 1 \times 2048 \times 4096 \times 14336 \approx 7.219 \times 10^{11} \text{ FLOPs} \approx 722 \text{ GFLOPs}$$

### 3. 未熔合 vs 熔合的中间访存开销对比
- **未熔合状态：**  
  计算 Gate 得到中间张量（$2048 \times 14336 \times 2 \approx 58.7 \text{ MB}$），写回 HBM；  
  计算 Up 得到中间张量（$58.7 \text{ MB}$），写回 HBM；  
  启动激活核函数，重新从 HBM 读出这两份数据（$117.4 \text{ MB}$），在片上算完相乘，再将结果写回 HBM（$58.7 \text{ MB}$）；  
  总计带来额外的无用 HBM 读写：$58.7 \times 4 = 234.8 \text{ MB}$！
- **水平熔合状态：**  
  中间这 $234.8 \text{ MB}$ 的往返搬运在芯片片上被**彻底抹除，直接降为 0！**

---

## 步骤 6：核心系统工程铁律

> **把 Gate 与 Up 投影捆在一起冲压，在片上完成门控开合。**  
> FFN 占据了全模型绝大多数的计算和参数量。必须通过权重水平拼接与算子熔合消灭中间激活值的显存回写，以最庞大的矩阵乘法喂饱底层张量核心。
"""

chapters["module-2/09-fused-rmsnorm-residuals.zh.md"] = r"""# 第 E09 章：层归一化与残差连接的底层熔合：消除两趟显存往返跑腿的艺术

同学，你好！在前面我们学习的都是像注意力、矩阵乘这种动辄几百 GFLOPs 的“大块头”算子。

今天这节课，老师要带你把目光投向两个在数学上看起来极其轻量、不起眼的“小跟班”——**RMSNorm（均方根归一化）** 与 **Residual Connection（残差连接）**。

你可能会轻视它们：“老师，残差连接不就是一个简单的向量加法 `x = x + y` 吗？归一化不就是除以一个方差吗？这能耗费几个时间？有什么好优化的？”

如果你这么想，那就掉进大坑里了！老师告诉你一个残酷的性能真相：**在未优化的模型中，这两个看起来毫不起眼的小算子，由于频繁往返读写显存，竟然能偷走整个模型 20% 到 30% 的运行时间！**

今天老师就带你看看，如何用“算子熔合（Kernel Fusion）”的神奇熨斗，把这两趟显存往返跑腿彻底抹掉！

---

## 步骤 1：物理直觉（洗完衣服顺手熨平）

老师打一个生活里洗衣服的比方：

你的日常工作流程包含两道工序：**清洗脱水（残差相加）** 和 **电熨斗熨平（归一化）**。
- **未熔合的笨做法（来回折腾）：**  
  1. 你把洗好的湿衣服从洗衣机拿出来，叠得整整齐齐，特意走很远的路放进地下储藏室的衣柜里（写入 HBM）；  
  2. 刚放好，你走回房间洗了洗手；  
  3. 过了两秒钟，你又特意走下楼去地下室，把这件衣服重新翻出来抱回房间（从 HBM 读出）；  
  4. 拿出电熨斗把它熨平，然后再次走下楼，把它塞回地下室衣柜（再次写入 HBM）！  
  你看，你根本不是在洗衣服，你纯粹是在走廊上当搬运工，把自己累得半死！
- **算子熔合（Fused）的聪明做法：**  
  衣服刚从洗衣机拿出来，**直接拿在手里一抖，顺手抄起桌上的电熨斗当场熨平**！衣服只进出储藏室一次，中间的折腾全省了！

这就是**算子熔合**的真谛：让数据在片上寄存器里多呆一会儿，顺手把该做的事全做了，绝不把半成品写进慢速显存。

---

## 步骤 2：芯片微观底层执行机制

在 Transformer 的每个子层（Attention 和 FFN）周围，都有残差连接和归一化：

$$\mathbf{x}_{\text{norm}} = \text{RMSNorm}(\mathbf{x} + \mathbf{y})$$

其中 RMSNorm 的数学定义为：

$$\text{RMS}(\mathbf{z}) = \sqrt{\frac{1}{d}\sum_{i=1}^d z_i^2 + \epsilon}, \quad \text{RMSNorm}(\mathbf{z}) = \frac{\mathbf{z}}{\text{RMS}(\mathbf{z})} \odot \mathbf{w}_{\text{scale}}$$

```
【未熔合执行模式 (极其低效)】
HBM 显存 ◄── 1. 读取 x, y ──► 片上执行残差加法 (x + y) ──► 2. 将 z 完整写回 HBM
HBM 显存 ◄── 3. 重新读取 z ──► 片上执行平方累加、求根、缩放 ──► 4. 将 x_norm 写回 HBM
==> 共计触发 2 次完整读取 + 2 次完整写入 = 4 次跨越慢速总线!

【Fused RMSNorm + Residual 深度熔合模式】
HBM 显存 ◄── 1. 单次合并读取 x, y ──► 片上高速寄存器与共享内存:
                                       - 线程束在寄存器就地执行: z = x + y
                                       - 利用 Warp Shuffle 指令跨线程极速完成求和累加
                                       - 在寄存器内就地除以 RMS 并乘缩放系数 w
                                       - (可选: 将反向传播所需的残差 z 顺手写回一次)
HBM 显存 ◄── 2. 将最终 x_norm 写回 HBM
==> 显存读写往返直接被腰斩减半!
```

### Warp Shuffle（线程束内洗牌）的硬件加速机制：
在计算一整行的平方和 $\sum z_i^2$ 时，传统做法是在共享内存里加锁互斥或者反复读写。  
而在现代 CUDA / Triton 内核中，线程束内部的 32 个线程可以直接利用硬件内置的 **Warp Shuffle（寄存器直接交换指令 `__shfl_down_sync`）**，在短短几个时钟周期内，像折纸一样用树状规约完成求和，连共享内存都不需要碰！

---

## 步骤 3：跨组件相互耦合机制

1. **为前后的 GEMM 算子清空显存总线：**  
   残差与归一化算子是纯粹的访存密集型算子（算术强度极低，$I \approx 0.5\text{ FLOP/Byte}$）。如果不熔合，它们会像幽灵一样在两个大矩阵乘法之间频繁抢占显存总线带宽，引发严重的流水线挂起等待。
2. **隐藏反向传播保存开销：**  
   在模型训练中，残差相加的结果 $\mathbf{z}$ 必须被保存以用于反向传播残差梯度的直通传递。熔合内核可以在完成归一化的同时，以宽总线矢量化（128-bit store）顺手写回一份，实现最高效的总线吞吐。

---

## 步骤 4：精确性能数学公式

设输入隐藏层维度为 $d$，批大小和序列长乘积为 $M = B \times S$：

### 1. 浮点运算量（FLOPs）
- 残差相加：$M \times d$
- 平方与累加：$2 M \times d$
- 开方与除法：$M + M \times d$
- 权重缩放乘法：$M \times d$

$$\text{FLOPs}_{\text{total}} \approx 5 M d \text{ FLOPs}$$

### 2. 未熔合情况下的 HBM 访存量（Bytes，FP16 精度）
- 读 $\mathbf{x}, \mathbf{y}$：$4 M d$ 字节
- 写 $\mathbf{z}$：$2 M d$ 字节
- 读 $\mathbf{z}$：$2 M d$ 字节
- 写 $\mathbf{x}_{\text{norm}}$：$2 M d$ 字节

$$\text{Bytes}_{\text{unfused}} = 10 M d \text{ 字节}$$

### 3. 熔合内核的 HBM 访存量
- 读 $\mathbf{x}, \mathbf{y}$：$4 M d$ 字节
- 写 $\mathbf{x}_{\text{norm}}$（及按需写 $\mathbf{z}$）：$2 M d \text{ (或 } 4 M d \text{)}$ 字节

$$\text{Bytes}_{\text{fused}} = 6 M d \text{ 字节 (纯推理阶段)}$$

**显存带宽消耗瞬间节省整整 40%！**

---

## 步骤 5：具体微基准数字推导

我们以 **LLaMA-3 70B**（$d = 8192$），在 Prefill 阶段 $M = 4096$ 为例：
- 元素总数：$M \times d = 4096 \times 8192 \approx 33.55 \times 10^6$ 个元素。
- 芯片：NVIDIA H100 SXM（带宽 $3.35\text{ TB/s}$）。

### 1. 未熔合实测耗时
- 搬运量：$10 \times 33.55 \times 10^6 \times 2 \approx 671 \text{ MB}$
- 加上两次内核启动的固定系统开销（每次约 $4 \;\mu\text{s}$）：
  $$T_{\text{unfused}} = \frac{671 \times 10^6}{3.35 \times 10^{12}} + 8 \;\mu\text{s} \approx 200 \;\mu\text{s} + 8 \;\mu\text{s} = 208 \;\mu\text{s}$$

### 2. 深度熔合内核耗时
- 搬运量直接骤降至：$6 \times 33.55 \times 10^6 \times 2 \approx 402 \text{ MB}$
- 仅需 1 次内核启动（$4 \;\mu\text{s}$）：
  $$T_{\text{fused}} = \frac{402 \times 10^6}{3.35 \times 10^{12}} + 4 \;\mu\text{s} \approx 120 \;\mu\text{s} + 4 \;\mu\text{s} = 124 \;\mu\text{s}$$

**老师带你看战果：**  
单层单次就能省下 **$84 \;\mu\text{s}$**！  
LLaMA-3 70B 整整有 80 个层，每层有 2 个归一化（共 160 次）！  
在一次完整前向传播中，光是熔合这一个算子，就能为全卡净省下：

$$160 \times 84 \;\mu\text{s} \approx 13.44 \text{ 毫秒！}$$

在毫秒必争的大模型推理服务中，这简直是一笔从天而降的巨大红利！

---

## 步骤 6：核心系统工程铁律

> **永远顺手熨平衣服。**  
> 绝不给访存密集型算子单独安排独立的 HBM 往返。把残差相加与层归一化深度熔合进单次内核中，用寄存器直接消化中间特征，是消灭微小算子带宽侵蚀的黄金法则。
"""

chapters["module-2/10-rope-kernel-fusion.zh.md"] = r"""# 第 E10 章：RoPE 旋转位置编码片上即时计算：摆脱查找表，在心算中完成二维旋转

同学，你好！在第二模块的最后一课，老师要带你攻克一个在长文本大模型中极其核心的算子——**RoPE（Rotary Position Embedding，旋转位置编码）**。

你在前面的数学篇里已经推导过：RoPE 的精妙之处在于把词向量两两配对，看作复数平面上的向量，然后根据当前 Token 所在的相对位置，乘以一个旋转角度。

但很多初学者在把它写成系统代码时，常常想当然地认为：“既然每个位置的旋转角度 $\cos(m\theta)$ 和 $\sin(m\theta)$ 都是固定的，那我何不在显存里开辟一个大表格，把所有位置的三角函数提前算好存进去，用的时候直接查表，岂不是最快？”

老师要郑重地提醒你：**在现代现代超高速 GPU 芯片上，“查表法”不仅不快，反而慢得像蜗牛！**

今天这节课，老师就为你揭开为什么“宁可在片上现算三角函数，也绝不查表”的硅基硬件逆直觉真相！

---

## 步骤 1：物理直觉（老木匠的心算与远途翻账本）

老师打一个生动形象的比方：

假设你是一位木匠师傅，正在流水线上给成千上万根木条切割倾斜角：
- **查表法（去隔壁图书馆翻书）：**  
  每拿起一根木条，你都停下手头的锯子，摘下手套，跑到隔壁的档案室，从厚厚的一万页表格中翻出第 3542 页，查出对应的正弦角度数值，抄在纸上，再跑回车间开始锯木头。虽然查表不需要动脑子算，但你在路上跑腿的时间，足够你锯完 100 根木条了！
- **即时计算法（片上心算）：**  
  你把常用的几个三角公式直接背在脑子里。拿起木条的瞬间，你在脑子里飞快心算 2 秒钟，算出角度立刻下刀，一步都不需要挪动！

在 GPU 的物理世界里：**计算（动脑）极其廉价，但访存（跑腿）昂贵至极！**

---

## 步骤 2：芯片微观底层执行机制

RoPE 对 Query 和 Key 向量的作用机制为：将维度按照两两一组（$2i, 2i+1$）配对，在二维平面上进行旋转变换：

$$
\begin{bmatrix}
x_{2i}' \\
x_{2i+1}'
\end{bmatrix}
=
\begin{bmatrix}
\cos(m\theta_i) & -\sin(m\theta_i) \\
\sin(m\theta_i) & \cos(m\theta_i)
\end{bmatrix}
\begin{bmatrix}
x_{2i} \\
x_{2i+1}
\end{bmatrix}
$$

展开后的标量代数形式为：

$$x_{2i}' = x_{2i}\cos(m\theta_i) - x_{2i+1}\sin(m\theta_i)$$
$$x_{2i+1}' = x_{2i}\sin(m\theta_i) + x_{2i+1}\cos(m\theta_i)$$

```
【方案 A：全局显存查找表预计算 (Naive Precomputed Table)】
全局显存 (HBM) 存放巨大的 Cos/Sin 矩阵 [Max_Seq, Head_Dim]
CUDA 线程执行:
  1. 从 HBM 加载 x[2i], x[2i+1] (消耗带宽)
  2. 从 HBM 远程加载 cos_table[m, i], sin_table[m, i] (再次消耗珍贵带宽!)
  3. 计算旋转并写回 HBM
==> 显存带宽消耗暴涨 2 倍! 极度访存受限!

【方案 B：片上寄存器即时计算 + QK 内核熔合 (On-the-Fly Fusion)】
根本不查表！
CUDA 线程在将 Q 向量从上一层 GEMM 产出并保存在寄存器堆时:
  1. 直接使用硬件内置的快速特殊函数单元 (SFU) 计算:
     theta = 10000^(-2i / d)
     angle = m * theta
     __sincosf(angle, &sin_val, &cos_val)  <-- 单个时钟周期硬件指令!
  2. 在寄存器内瞬间完成乘加旋转;
  3. 接着直接送入下一级 FlashAttention 的片上 SRAM，彻底不落显存!
```

### SFU（Special Function Unit，特殊函数单元）的威力：
现代 GPU 的每个 SM 中都配备了专门用于硬件级求倒数、开平方、求正弦和余弦的硬件单元（SFU）。一条 `__sincosf` 硬件内联指令，可以在短短几个周期内同时输出正弦和余弦值，其速度远比从 HBM 翻山越岭搬运数据快上百倍！

---

## 步骤 3：跨组件相互耦合机制

1. **直接熔进 QKV 投影之后或 FlashAttention 之前：**  
   如果将 RoPE 单独做成一个独立的 PyTorch 算子，每一层都需要对巨大的 $Q$ 和 $K$ 张量进行一次完整的全局显存读写。将其熔合后，RoPE 的时间消耗几乎被**完全隐藏在内存加载的间隙中**，几乎相当于“零成本附赠”。
2. **长文本扩展外推（LongRoPE / YaRN）：**  
   在进行长上下文外推时，我们需要动态改变旋转基频 $\theta$。如果是查表法，每次改变序列长度都必须重新在显存中分配并初始化巨大的表格；而即时计算法只需在寄存器中修改一行浮点比例因子，具有无与伦比的动态灵活性。

---

## 步骤 4：精确性能数学公式

设批大小为 $B$，头数为 $H$，序列长为 $S$，单头维度为 $d$：

### 1. 浮点运算量（FLOPs）
每个元素需要 2 次乘法和 1 次加减法，加上硬件三角函数的等效做功：

$$\text{FLOPs}_{\text{RoPE}} \approx 6 \times B \times H \times S \times d$$

### 2. HBM 显存访存量对比（以 FP16 精度度量，读写 Q 和 K）
- **查表法：** 需要读 Q/K（$4 B H S d$ 字节）、读 Cos/Sin 表格（$4 B H S d$ 字节）、写回输出（$4 B H S d$ 字节）：
  $$\text{Bytes}_{\text{table}} = 12 B H S d \text{ 字节}$$
- **片上即时计算熔合：** 仅读 Q/K、在片上旋转、写回输出：
  $$\text{Bytes}_{\text{fused}} = 8 B H S d \text{ 字节}$$

访存量直接**削减了整整 33.3%**，完全消除了对查找表显存空间的侵占！

---

## 步骤 5：具体微基准数字推导

我们以 **LLaMA-3 8B** 在长上下文 $S = 32,768$（32K 长文本）下的 Q/K 旋转为例：
- 参数：$H = 32, d = 128, B = 1$
- 元素总数：$1 \times 32 \times 32768 \times 128 = 134,217,728$ 个数字（约 $1.34$ 亿个数据）。
- 硬件：NVIDIA A100（带宽 $2.0 \text{ TB/s}$）。

### 1. 查表法耗时
- 搬运数据量：$12 \times 1.34 \times 10^8 \times 2 \approx 3.22 \text{ GB}$
- 显存跑腿耗时：
  $$T_{\text{table}} = \frac{3.22 \times 10^9}{2.0 \times 10^{12}} \approx 1.61 \text{ 毫秒}$$

### 2. 即时计算熔合耗时
- 搬运数据量降为：$8 \times 1.34 \times 10^8 \times 2 \approx 2.15 \text{ GB}$
- 显存跑腿耗时：
  $$T_{\text{fused}} = \frac{2.15 \times 10^9}{2.0 \times 10^{12}} \approx 1.07 \text{ 毫秒}$$
- **单次前向净省：** $0.54\text{ 毫秒}$！  
  乘以全模型 32 层，单次推理就能**挽回整整 17 毫秒的宝贵延迟**！

---

## 步骤 6：核心系统工程铁律

> **宁可让芯片多动脑心算，也绝不让总线跨步跑腿。**  
> 算力过剩、带宽匮乏是现代硅基芯片的永恒宿命。丢掉笨重的预计算静态查找表，利用片上硬件特殊函数单元即时解算位置旋转，是长文本算子设计的最佳实践。
"""

# ==============================================================================
# Module 3: Number Formats & Quantization
# ==============================================================================

chapters["module-3/11-number-formats-fp8-bf16.zh.md"] = r"""# 第 E11 章：硅基芯片中的数值格式：FP32、FP16、BF16 与 FP8 的阶码尾数取舍哲学

同学，你好！欢迎来到第三模块——数值格式与量化的奇妙微观世界！

在数学里，我们谈到一个实数（比如 $\pi = 3.1415926\dots$），它拥有无穷无尽的小数位。但在真实的计算机硬件里，由于芯片面积和电荷存储是有限的，每一个数字都必须被压缩进一个固定长度的二进制小盒子里。

你可能经常听说：“现在大模型预训练必须用 **BF16**，绝对不能用普通的 **FP16**；而在最新的 H100/Blackwell 芯片上，大家都在疯狂搞 **FP8**……”

你可能会好奇：“老师，不都是 16 位和 8 位的小盒子吗？换个叫法，难道模型在芯片里的命运就会截然不同吗？”

问得太精辟了！今天这堂课，老师就带你拿起高倍放大镜，拆开浮点数的**符号位、阶码位与尾数位**，看看这背后的取舍哲学！

---

## 步骤 1：物理直觉（科学计数法的尺子与放大镜）

老师给你打一个日常量尺寸的比方：

所有的浮点数，本质上就是我们初中物理学过的**科学计数法**：

$$\text{数字} = \pm 1.\text{尾数} \times 2^{\text{指数}}$$

在这里，有两个决定命运的指标：
1. **你的视野有多宽广？（阶码位 Exponent，指数）：**  
   决定了你能测量的**动态范围（Dynamic Range）**。指数位越多，你能记录的数字就能从宇宙原子的大小（极小值）一路跨越到整个银河系的直径（极大值），不容易发生**数值下溢（变成 0）或数值溢出（变成 $\infty$ 崩溃）**。
2. **你的刻度有多精细？（尾数位 Mantissa，小数位）：**  
   决定了你的**测量精度（Precision）**。尾数位越多，小数点后能记录的有效数字就越多，误差就越小。

现在，假设芯片工程师只给你一个总长为 16 位的小盒子供你分配：
- **方案 A（FP16）：追求精雕细琢，但心胸狭窄**  
  工程师给了 5 位阶码、10 位尾数。小数位非常精细，但由于指数位太少，它的视野非常狭窄（最大只能记录到 65504）。在训练大模型时，只要梯度稍稍剧烈抖动一下，数字就会当场爆表变成 `NaN`（非数崩溃）！
- **方案 B（BF16）：胸怀宽广，不拘小节（Google 的神来之笔）**  
  工程师大手一挥：给阶码整整 8 位，尾数只留 7 位！它的动态范围和 32 位浮点数（FP32）**完全一模一样大（最大可达 $10^{38}$）**！虽然粗心了一点点，但它在惊涛骇浪的大模型训练梯度下，**永远坚挺、绝不溢出！**

---

## 步骤 2：芯片微观底层执行机制

我们把常见数值格式的微观二进制位分布拆解在黑板上：

```
32 位单精度浮点数 (FP32, 4 字节):
[ 符号 S: 1位 ] [ 阶码 Exponent: 8位 ] [ 尾数 Mantissa: 23位 ]
动态范围: ~10^(-38) 到 ~10^(38) | 尾数精度: 约 7 位有效十进制小数

16 位半精度浮点数 (FP16, 2 字节):
[ S: 1位 ] [ Exponent: 5位 ] [ Mantissa: 10位 ]
动态范围: 仅 6 x 10^(-5) 到 65504 | 极易发生训练梯度溢出!

16 位大脑浮点数 (BF16 - Bfloat16, 2 字节):
[ S: 1位 ] [ Exponent: 8位 (与 FP32 完全相同!) ] [ Mantissa: 7位 ]
动态范围: 与 FP32 完全一致! 硬件层面上只需将 FP32 的后 16 位直接截断即可!

--------------------------------------------------------------------------------

全新一代 8 位浮点数 (FP8, 仅 1 字节! 显存占用直接减半):
1. FP8 E4M3 (用于前向权重与激活):
   [ S: 1位 ] [ Exponent: 4位 ] [ Mantissa: 3位 ]
   拥有更多尾数位，提供相对更高的计算精度，最大值约为 448。

2. FP8 E5M2 (用于反向传播梯度):
   [ S: 1位 ] [ Exponent: 5位 ] [ Mantissa: 2位 ]
   牺牲精度以获得更大的动态范围（与 FP16 相同），确保反向微小梯度不至于消失归零。
```

---

## 步骤 3：跨组件相互耦合机制

1. **彻底终结混合精度缩放（Loss Scale）的黑魔法：**  
   在过去使用 FP16 训练大模型时，工程师必须写大量极其脆弱的代码，动态监测损失值大小，人为地乘上一个放大因子（Loss Scaling），防止反向传播梯度变成 0。而 **BF16 凭借其巨大的 8 位阶码，彻底消灭了 Loss Scale，让大模型预训练稳如泰山！**
2. **硬件算力吞吐的成倍跃升：**  
   在 NVIDIA Hopper 架构（H100）中：
   - FP16/BF16 密集峰值算力为：$1,000 \text{ TFLOPS}$；
   - 而换成 FP8 时，由于位宽缩小了一半，Tensor Core 的脉动阵列冲压机可以在单位周期内塞进双倍的乘加运算，峰值算力直接**暴涨到惊人的 $2,000 \text{ TFLOPS}$！**

---

## 步骤 4：精确性能数学公式

对于任意一个给定的浮点数格式，其在二进制下的真值解算公式为：

$$V = (-1)^S \times 2^{E - \text{Bias}} \times \left(1 + \sum_{i=1}^M b_i 2^{-i}\right)$$

其中：
- $S$ 为符号位（Sign）；
- $E$ 为阶码无符号整数值，$\text{Bias} = 2^{\text{Bits}_E - 1} - 1$ 为偏置常数；
- $M$ 为尾数位长，有效数字基数隐含最高位的 $1$（规格化数）。

最大可表示有限数值（Max Representable Value）为：

$$V_{\max} \approx 2^{E_{\max} - \text{Bias}} \times 2$$

---

## 步骤 5：具体微基准数字推导

我们来算一算不同数值格式在存储大模型时的真实显存差异！

以拥有 **700 亿参数（70B）** 的模型为例：

### 1. 采用 FP32（单精度，4 字节/参数）
$$\text{显存体积} = 70 \times 10^9 \times 4 \text{ 字节} = 280 \text{ GB}$$
需要整整 4 张 80GB 的顶配 A100 显卡才能勉强装下这具“骨架”！

### 2. 采用现代标准 BF16（半精度，2 字节/参数）
$$\text{显存体积} = 70 \times 10^9 \times 2 \text{ 字节} = 140 \text{ GB}$$
显存体积直接腰斩！只需 2 张 80GB 显卡即可常驻。

### 3. 采用前沿 FP8（8 位浮点，1 字节/参数）
$$\text{显存体积} = 70 \times 10^9 \times 1 \text{ 字节} = 70 \text{ GB}$$
**奇迹发生了：原本需要整整一个机架多卡并行的 70B 庞然大物，现在竟然可以稳稳当当塞进单张 80GB 的 H100 显卡中！**

---

## 步骤 6：核心系统工程铁律

> **训用 BF16 筑牢动态底座，推理拥抱 FP8 榨干翻倍算力。**  
> 永远记住尾数决定精度、阶码决定生死。预训练首选 BF16 规避数值溢出灾难；在支持新一代 Tensor Core 的芯片上全面推进 FP8，用减半的显存换取翻倍的算力与并发。
"""

chapters["module-3/12-quantization-strategies-w8a8-w4a16.zh.md"] = r"""# 第 E12 章：量化策略实战：W8A8、W4A16 与 INT4 在推理各阶段的硬件表现

同学，你好！在上一节课中，我们拆解了浮点数的格式。今天，老师要带你学习大模型落地部署中最核心的独门秘籍——**模型量化（Quantization）**。

你在业界看技术博客时，一定经常看到这些让人犯晕的字母代号：“我们模型采用了 **W4A16** 量化！”、“我们在集群上部署了 **W8A8** 方案！”

你可能会好奇：“老师，为什么一会儿对权重（Weight）做 4 位、对激活（Activation）保留 16 位，一会儿又都要做成 8 位？这两个阶段到底有什么讲究？”

今天这堂课，老师就结合前面的**屋顶模型（Roofline）**，带你从物理底层一眼看破 W4A16 与 W8A8 的选型真谛！

---

## 步骤 1：物理直觉（大石块装车与流水线加速）

老师打一个修房子搬石块的比方：

- **W4A16（仅权重量化 Weight-Only）：**  
  仓库里的石块原本每个重 16 斤（FP16 权重）。现在我们找来石匠，把它们敲碎压紧，做成每个只重 4 斤的超轻微型砖（INT4/FP4 压缩）。  
  但在工地砌墙干活时（Tensor Core 矩阵乘），工人必须先把这块轻砖**当场扔进水里泡发恢复成 16 斤**再放上墙。  
  - **这在什么时候管用？** 在**搬运工极度挨饿、推车推不过来**的时候（自回归 Decode 阶段）！因为搬运工运送这批砖省了 75% 的力气！  
  - **在什么时候没用？** 在砌墙工人切砖忙得满头大汗的时候（预填 Prefill 阶段）！因为砖块到了现场还要泡发，工人的实际工作量一点都没少。
- **W8A8（权重与激活同时量化）：**  
  我们不仅把石块做成 8 斤的小砖，**连工人的砌刀、灰浆、图纸全套工具全部换成了小号 8 位规格**！  
  不仅搬运工省了一半力气，砌墙工人切砖冲压的手速（INT8 Tensor Core 算力）也直接**暴增翻倍**！

---

## 步骤 2：芯片微观底层执行机制

我们把这两种方案在芯片里的计算数据流画在黑板上：

```
【W4A16 仅权重量化 (Weight-Only Quantization)】
全局显存 (HBM)                          片上共享内存 (SRAM / 寄存器)
     │                                               │
     ├── 权重 W 以 4-bit 紧凑存储 (省 75% 显存!) ──► │ 1. 从 HBM 快速搬运进片上
     │                                               │ 2. 硬件执行即时反量化 (De-quant):
     │                                               │    W_fp16 = (W_int4 - zero_point) * scale
     ├── 激活 X 依然保持 16-bit 浮点 ──────────────► │ 3. 在 Tensor Core 中执行:
     │                                               │    FP16 矩阵乘 (X_fp16 @ W_fp16)
==> 核心价值: 打破自回归 Decode 阶段的显存带宽瓶颈，但算力上限仍为 FP16 峰值!

--------------------------------------------------------------------------------

【W8A8 权重与激活全量化 (Weight-Activation Quantization)】
全局显存 (HBM)                          片上共享内存 (SRAM / 寄存器)
     │                                               │
     ├── 权重 W 以 INT8 存储 (省 50% 显存) ────────► │ 1. 搬运进片上
     ├── 激活 X 在运行时动态量化为 INT8 ───────────► │ 2. 直接喂入 INT8 Tensor Core!
     │                                               │ 3. 执行纯整型矩阵乘 (INT8 GEMM)
     │                                               │    硬件算力峰值直接翻倍 (2x TFLOPS)!
==> 核心价值: 同时加速 Prefill (算力密集) 与 Decode (带宽密集)，但需要应对异常值 (Outliers)!
```

### 为什么 W8A8 很难？——异常值（Outliers）的困扰
在大模型运行中，激活张量里往往潜伏着几个数值极大的“刺头”（异常值，数值可能是普通通道的 100 倍）。如果粗暴地按最大值把所有通道压缩进 $[-128, 127]$ 的 INT8 小格子里，绝大部分普通数值就会被挤压成 0，模型当场胡言乱语！  
因此，像 **SmoothQuant** 这样的经典算法，巧妙地通过数学等价变换，把激活值上的刺头“平滑迁移”到了权重矩阵上，才使得 W8A8 真正工业级可用。

---

## 步骤 3：跨组件相互耦合机制

1. **工况与阶段决定选型（Prefill 还是 Decode？）：**  
   - 如果你的业务主要是处理数万字长提示词的阅读理解（Prefill 为主），系统处于**计算密集型**，使用 W4A16 毫无意义，必须采用 **W8A8 或 FP8** 才能利用翻倍的 Tensor Core 算力降低首字延迟（TTFT）。  
   - 如果你的业务主要是小批量用户的聊天答疑（Decode 为主），系统处于**极度访存受限**，使用 **W4A16（如 AWQ、GPTQ）** 可以在消费级显卡（如单张 RTX 4090）上以极高帧率吐字。
2. **反量化指令开销（Dequant Overhead）：**  
   在 W4A16 中，把 4 位整数还原成 16 位浮点数的代数变换指令必须极其精简。现代内核通常将反量化逻辑深度熔合进 GEMM 循环的加载流水线中，绝不单独占用独立周期。

---

## 步骤 4：精确性能数学公式

对于矩阵乘法 $\mathbf{Y} = \mathbf{X} \mathbf{W}$，设 $\mathbf{W} \in \mathbb{R}^{K \times N}$：

### 1. W4A16 的权重传输带宽收益
权重占用的显存字节数为：

$$\text{Bytes}_{W4} = K \times N \times 0.5 \text{ 字节} + \text{Scale/Bias 辅助开销}$$

相比原生 FP16（$2KN$ 字节），权重加载总线带宽需求降为：

$$\text{带宽节约比} = \frac{0.5}{2.0} = 25\% \implies \text{理论提速上限 } 4 \times \text{ (在纯 Decode 阶段)}$$

### 2. W8A8 的算力翻倍收益
在 NVIDIA 架构中，INT8 Tensor Core 的理论浮点做功指令吞吐为 FP16 的整整 2 倍：

$$P_{\text{peak}}^{\text{INT8}} = 2 \times P_{\text{peak}}^{\text{FP16}}$$

---

## 步骤 5：具体微基准数字推导

我们来做一次实战场景选型推算！

场景：在 **RTX 4090 (24GB 显存，带宽 $1.0\text{ TB/s}$)** 上部署 **LLaMA-3 8B** 进行单人聊天：
- 模型参数量：$8 \times 10^9$ 个参数。

### 1. 尝试以未量化的 FP16 运行：
- 静态权重显存：$8 \times 10^9 \times 2 \approx 16 \text{ GB}$；
- 剩余显存只有 $8\text{ GB}$，稍有长上下文就会引发显存告急；
- 每生成 1 个 Token，必须全量加载一次 16 GB 权重：
  $$T_{\text{step}} = \frac{16 \times 10^9 \text{ 字节}}{1.0 \times 10^{12} \text{ 字节/秒}} = 0.016 \text{ 秒} = 16 \text{ 毫秒}$$
- 理论最高生成帧率：$1000 / 16 \approx 62.5 \text{ Tokens/s}$。

### 2. 采用 W4A16（AWQ 4-bit 量化）运行：
- 静态权重显存直接暴跌至：$8 \times 10^9 \times 0.5 \approx 4 \text{ GB}$！
- 整整腾出了 $20\text{ GB}$ 的超大空间用于高并发 KV Cache！
- 每次生成 1 个 Token，只需搬运 4 GB 权重：
  $$T_{\text{step}} = \frac{4 \times 10^9}{1.0 \times 10^{12}} = 0.004 \text{ 秒} = 4 \text{ 毫秒}$$
- **生成帧率瞬间狂飙至：** $1000 / 4 \approx \mathbf{250 \text{ Tokens/s}}$！  
  整整快了 4 倍，体验从卡顿跳字变成了如丝般顺滑！

---

## 步骤 6：核心系统工程铁律

> **Decode 看带宽选 W4A16，Prefill 看算力冲 W8A8。**  
> 永远不要脱离屋顶模型去谈量化收益。只有深刻理解算子此时此刻是在等搬运工（Memory-bound）还是等冲压机（Compute-bound），才能在精度损失与硬件加速之间做出最精准的工程决策。
"""

chapters["module-3/13-kv-cache-quantization.zh.md"] = r"""# 第 E13 章：KV Cache 显存量化压缩：给长上下文对话背上轻量化背包

同学，你好！在前面的课程中，我们把模型的权重（Weights）成功压缩到了 8 位甚至 4 位。

但如果你在大模型生产线上值过班，你就会发现一个令人尴尬的残酷事实：**即使你把 70B 模型的权重压缩到了极致，一旦成千上万个用户带着上万字的长文本蜂拥而入，显卡依然会毫不留情地爆卡崩溃！**

为什么？因为此时吃掉显存大头的，根本不是静止不动的模型权重，而是**随着对话长度疯狂滚雪球膨胀的动态 KV Cache 缓存！**

你可能会问老师：“老师，权重可以压缩，那我们能不能也给这个不断膨胀的 KV Cache 动个手术，把它也压缩成低比特呢？”

当然能！今天这堂课，老师就带你学习大模型超长上下文服务的降本神器——**KV Cache 显存量化压缩技术**！

---

## 步骤 1：物理直觉（登山者的脱水蔬菜包）

老师打一个登山探险的比方：

在自回归生成的漫长旅途中，历史上下文的 Key 和 Value 就是探险家背在身后的**干粮背包**。
- **未量化的 FP16 缓存（带全套生鲜食材）：**  
  每走一步（每吐一个 Token），你都在背包里塞进新鲜的大土豆、带水的新鲜牛肉。背了 500 步还行，背到 8000 步时，你的背包沉达上百斤，你直接被当场压垮在雪山上（OOM 崩溃）。
- **量化后的 FP8/INT4 缓存（脱水蔬菜与压缩肉干）：**  
  在把食材塞进背包之前，你用脱水机把水分抽干（量化映射到 8 位或 4 位），体积和重量立减 50% 到 75%！  
  当你晚上扎营要吃晚餐时（计算注意力），再抓一把脱水蔬菜扔进开水里泡发恢复（即时反量化）。

你看，探险家的负重大幅减轻，原本只能走 2000 米的体力，现在足够支撑你登上一万米的长文本珠穆朗玛峰！

---

## 步骤 2：芯片微观底层执行机制

KV Cache 量化的核心挑战在于：**它是在推理运行时动态产生的，没有办法像模型权重那样提前花几个小时离线标定校准！**

因此，系统必须在生成每一个 Token 的极短时间内，即时完成动态量化并打包写入显存：

```
【单步自回归中的 KV Cache 量化执行流水线】
当前 Token 的 Key / Value 向量计算完成 (原始 FP16 格式)
     │
     ├── 1. 动态范围统计 (Per-Token 或 Per-Head):
     │      找出当前向量的最大绝对值: max_val = max(|V|)
     │      计算动态缩放比例: scale = max_val / 127.0 (针对 INT8)
     │
     ├── 2. 片上即时截断量化:
     │      V_quant = clip(round(V / scale), -128, 127)  (存为 1 字节 INT8)
     │
     ├── 3. 写入 PagedAttention 物理缓存池:
     │      仅向 HBM 写入尺寸缩水一半的字节数据 + 紧凑的 scale 标量!
     ▼
后续注意核函数 (Flash-Decoding / PagedAttention) 读取时:
     从 HBM 快速搬运经过减半的紧凑字节进入片上 SRAM，
     在片上寄存器利用 scale 即时解压，直接喂入 Tensor Core 算子!
```

### 两种量化粒度的精度权衡：
1. **Per-Tensor（整张量缩放）：** 全模型用一个尺度，硬件最容易做，但由于长序列中不同位置的 Token 重要度差异极大，精度极易崩塌；
2. **Per-Token / Per-Head（单 Token 单头细粒度缩放）：** 每个 Token 的每个注意力头独立保存一个极其微小的浮点缩放系数（Scale）。虽然多占了极微量的辅助存储，但**几乎能实现对 FP16 原始精度的 99.9% 完美无损还原！**

---

## 步骤 3：跨组件相互耦合机制

1. **服务最大并发数与显存容量的倍增：**  
   将 KV Cache 从 FP16（2 字节）降至 FP8（1 字节），同样的物理显存所能容纳的并发会话数直接**翻倍**；若进一步采用 INT4，并发量将暴增 **4 倍**！这直接将大模型在线服务的每百万 Token 运营成本（TCO）斩落一半以上。
2. **Flash-Decoding 算子内核的解压重构：**  
   底层的注意力解码内核必须直接原生支持非对称或对称反量化。通过利用 Triton 或 CUDA 编写高度熔合的解压核函数，让数据在搬出显存后、进入计算前的纳秒级空隙中完成解压，不损失任何显存带宽。

---

## 步骤 4：精确性能数学公式

对于一个采用 $L$ 层、$H_{\text{kv}}$ 个 KV 头、单头维度为 $d$ 的大模型，当服务序列长度为 $S$ 时：

未量化状态下的全量 KV Cache 大小为：

$$\text{KV}_{\text{FP16}} = 2 \times L \times S \times H_{\text{kv}} \times d \times 2 \text{ 字节}$$

采用 INT8 / FP8 量化后：

$$\text{KV}_{\text{FP8}} = 2 \times L \times S \times H_{\text{kv}} \times d \times 1 \text{ 字节} + \text{Scale 辅助存储} \approx \frac{1}{2} \text{KV}_{\text{FP16}}$$

采用 INT4 量化后：

$$\text{KV}_{\text{INT4}} = 2 \times L \times S \times H_{\text{kv}} \times d \times 0.5 \text{ 字节} + \text{Scale 辅助存储} \approx \frac{1}{4} \text{KV}_{\text{FP16}}$$

---

## 步骤 5：具体微基准数字推导

我们来为一台生产级 **8 卡 H100 (80GB)** 线上集群算一笔震撼的大账！

运行模型：**LLaMA-3 70B**（80 层，$H_{\text{kv}} = 8, d = 128$），服务 100 个并发用户的 8K 长文本对话（$S = 8192$）：

### 1. 原始 FP16 下的 KV Cache 显存海啸：
- 单个用户的 KV 显存：
  $$2 \times 80 \times 8192 \times 8 \times 128 \times 2 \text{ 字节} \approx 2.68 \text{ GB}$$
- 100 个并发用户的总显存：
  $$100 \times 2.68 \text{ GB} = \mathbf{268 \text{ GB}!}$$
- **老师请你看清楚这个数字：光是装用户的聊天记忆，就要吃掉整整 268 GB 显存！**  
  这意味着整个 8 卡集群（总显存 $640\text{ GB}$）在减去 $140\text{ GB}$ 模型权重后，一大半的显存全被缓存淹没了，系统随时在暴跌边缘试探。

### 2. 启用 FP8 动态量化压缩后：
- 显存直接腰斩：
  $$268 \text{ GB} \times 0.5 = \mathbf{134 \text{ GB}!}$$
- **全集群当场省出整整 134 GB 的黄金显存！**

### 3. 启用 INT4 极致压缩后：
- 显存降至：
  $$268 \text{ GB} \times 0.25 = \mathbf{67 \text{ GB}!}$$
- 仅仅用了原先四分之一的代价，同样的集群现在足以轻松支撑 **400 个并发用户**同时在线长对话！

---

## 步骤 6：核心系统工程铁律

> **长文本时代，谁能管住 KV Cache 的腰围，谁就能赢得商业化落地。**  
> 永远不要让历史缓存以昂贵的 16 位浮点在显存中泛滥。采用 Per-Token 细粒度的 FP8 或 INT4 压缩，是在保证模型逻辑严密性的前提下，实现长序列服务成本断崖式下降的核心胜负手。
"""

written = 0
for rel_path, content in chapters.items():
    full_path = os.path.join(BASE_DIR, rel_path)
    os.makedirs(os.path.dirname(full_path), exist_ok=True)
    with open(full_path, "w", encoding="utf-8") as f:
        f.write(content.strip() + "\n")
    written += 1

print(f"Successfully refined {written} chapters for Module 2 and Module 3 with teacher voice.")
